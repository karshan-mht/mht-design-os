/* GENERATED by evals/gen-docs.js — do not edit by hand.
   Regenerate:  node evals/gen-docs.js
   Verify:      node evals/gen-docs.js --check

   32 documents, 332KB of markdown, inlined so
   system/docs/index.html can render them without fetch() — which Chrome blocks
   over file://. The .md files remain the source of truth; this is a mirror and
   --check fails when it falls behind. */
window.MHT_DOCS = [{"path":".claude/skills/README.md","title":"Skills","section":"Skills","bytes":1902,"markdown":"# Skills\n\nProcedures for working on this repo. Each one references the authoritative\nfiles rather than copying them, so a skill cannot drift from the system it\ndescribes.\n\n| Skill | Use when |\n|---|---|\n| [drift-check](drift-check/SKILL.md) | Before committing, after moving files, or when asked whether anything has drifted |\n| [new-component-contract](new-component-contract/SKILL.md) | Documenting a component, or before changing one whose intent is unclear |\n| [add-icon](add-icon/SKILL.md) | Adding or changing an icon |\n| [new-prototype](new-prototype/SKILL.md) | Building a new screen, flow or surface |\n\n## Why only four\n\nThe restructure plan listed six candidates — design, design-to-code, design\nreview, accessibility review, component creation, drift detection. Four were\nwritten. The other two were not, on purpose.\n\n**design review** and **accessibility review** are mostly judgment, not\nprocedure. What a skill could usefully say about them already lives closer to\nthe work: the accessibility expectations are in each component contract, and\n[system/AUTHORITY.md](../../system/AUTHORITY.md) covers how to weigh sources\nwhen they disagree. A skill that only said \"check contrast and keyboard access\"\nwould be a maintenance cost pretending to be guidance.\n\nThe four that exist all describe something with a real, repeating, repo-specific\nprocedure — a sequence with commands in it, where getting the order wrong causes\na concrete failure. That is the bar. Add a fifth when a task has actually\nrecurred and has that shape, not before.\n\n## These are committed\n\n`.claude/settings.local.json` stays gitignored — it is local permission state.\n`.claude/skills/` is tracked, because a procedure that exists on one machine is\nnot a procedure. `evals/lint-links.js` checks the links inside them, since they\nreference repo paths heavily and are exactly the kind of link that rots after a\nmove.\n"},{"path":".claude/skills/add-icon/SKILL.md","title":"Add an icon","section":"Skills","bytes":3291,"markdown":"---\nname: add-icon\ndescription: Add or change an icon. Use when given an SVG to add, when asked which icon to use, or when an icon needs to change color, size or category.\n---\n\n# Add an icon\n\nTwo sets with different jobs. Pick the right one first — they are deliberately\nnot interchangeable.\n\n| | `system/icons/editorial/` | `system/icons/ui/` |\n|---|---|---|\n| For | Symptoms, anatomy, care, support, living | Interface chrome: nav, tabs, actions |\n| Style | Line art, stroke 3.4 on a 128 grid | Solid shapes |\n| Size | Display — **below ~44px it renders as a hairline** | 16–44px |\n| Count | 112 | 28 |\n\nIf it is a glyph in a button or a nav row, it is **ui**. If it illustrates a\ntopic, it is **editorial**.\n\n## Procedure\n\n1. **Drop the file** into the right folder. Name it kebab-case; the filename\n   becomes the registry key.\n\n2. **Make it inherit color.** Every path must use `currentColor`, not a\n   literal — `stroke=\"currentColor\"` for editorial, `fill=\"currentColor\"` for\n   ui. A baked fill will not re-skin when the site theme changes, which is the\n   bug class `evals/lint-tokens.js` exists to catch.\n\n   Exceptions are narrow: a knockout punched through a filled shape, and\n   third-party brand marks. Both stay literal and need a reason in the lint.\n\n3. **Categorise it** (editorial only) in\n   [system/icons/categories.js](../../../system/icons/categories.js) — add the\n   slug to exactly one category and give it a display name. This is authored\n   data, not derived; the grouping is a judgment about what the icon is for.\n\n4. **Regenerate and verify.**\n   ```bash\n   node evals/gen-icon-registry.js\n   node evals/gen-icon-registry.js --check\n   node evals/lint-tokens.js\n   ```\n   The `--check` run fails if an editorial icon is uncategorised, so a new icon\n   cannot silently vanish from the sheet.\n\n5. **Look at it.** Open `system/icons/sheet.html`, switch themes, and check it\n   at the small sizes. The sheet renders from the registry, so it is a specimen\n   of the real file, not a copy.\n\n## Adding a UI glyph\n\nUI glyphs live on a **24x24 canvas with a 20-unit live area** — the artwork's\nlongest side is 20 units, centred. Every one of the 28 matches, so they render\nat a consistent optical size wherever they are used.\n\nA new glyph has to match. If it does not, normalise it:\n\n1. Measure its bounding box in a browser with `getBBox()` — parsing curve\n   geometry any other way is guesswork.\n2. Add the measurement to `evals/ui-icon-bboxes.json`.\n3. Run `node evals/normalize-ui-icons.js`.\n\nThat file is a point-in-time snapshot, not something generated on demand, which\nis why step 2 is manual. An already-normalised file is detected by its\n`data-normalized=\"24\"` attribute and left alone.\n\n## Notes\n\n- Files are the source of truth. `registry-*.js` is generated — never hand-edit.\n- The registries are split per set on purpose: the prototype loads only the 28\n  UI glyphs (53KB) and never the 112 editorial icons (160KB it would not use).\n- `icon()` in `main.js` normalizes a camelCase request (`tabHome`) to the kebab\n  key, so existing call sites keep working.\n- Design tools drop inherited paint on import. The sheet's **COPY** button bakes\n  a literal hex onto every shape before copying, so `currentColor` in the source\n  costs nothing there.\n"},{"path":".claude/skills/drift-check/SKILL.md","title":"Drift check","section":"Skills","bytes":4053,"markdown":"---\nname: drift-check\ndescription: Run the repo's five verification checks and interpret the results. Use before committing UI or design-system work, after moving files, or when asked whether anything has drifted.\n---\n\n# Drift check\n\nOne command runs everything:\n\n```bash\nnode evals/check-all.js\n```\n\nNon-zero exit if any **blocking** check fails. Advisory checks print findings\nand never fail the run — they report things that may be legitimate, and a check\nthat cries wolf gets switched off.\n\nIndividually, if you need one:\n\n```bash\nnode evals/lint-tokens.js          # color outside the token layer      BLOCKING\nnode evals/lint-links.js           # references that do not resolve      BLOCKING\nnode evals/gen-component-index.js --check                             # BLOCKING\nnode evals/gen-icon-registry.js --check                               # BLOCKING\nnode evals/gen-token-manifest.js --check                              # BLOCKING\nnode evals/gen-asset-manifest.js --check                              # BLOCKING\nnode evals/lint-parity.js          # Figma mapping + coverage            BLOCKING\nnode evals/lint-dead-code.js       # unrendered blocks, uncalled fns     advisory\n```\n\n## Reading the results\n\n| Output | Meaning |\n|---|---|\n| `PASS` | Clean. |\n| `FAIL` | Blocking. Both color checks and the two `--check` generators are blocking. |\n| `NOTE` | Coverage gaps from `lint-parity`. Informational — do **not** silence by inventing entries. |\n\n## When a check fails\n\n**lint-tokens** — a hex or `rgb()` outside `system/tokens/`. Add a token, or a\nhue to a theme file. If it genuinely must stay literal (a third-party brand\nmark, a knockout through a filled shape, pure black/white at alpha), add it to\n`EXCEPTIONS` in the lint **with a reason**. An unexplained exception is how a\nlint stops being verification.\n\n**lint-links** — a moved or renamed file. Fix the reference, not the check.\n`decisions/DECISIONS.md` is skipped deliberately: it is append-only, so paths\ninside older entries are historical records, not broken links.\n\n**gen-component-index --check** — a CSS block was added, removed or renamed.\nRun `node evals/gen-component-index.js` to regenerate. Never hand-edit\n`INDEX.md`.\n\n**gen-docs --check** — a markdown file changed. Regenerate; the viewer reads a\ngenerated mirror because Chrome blocks `fetch()` of local files.\n\n**gen-token-manifest / gen-asset-manifest --check** — `tokens.css` or\n`system/assets/` changed. Regenerate. The asset one also reports a file that\nmatches no grouping rule: add a rule rather than letting it fall into \"Other\".\n\n**gen-icon-registry --check** — an icon file changed, or one is uncategorised.\nRun `node evals/gen-icon-registry.js`. If an editorial icon is missing from\n`system/icons/categories.js`, add it there — the grouping is authored, not\nderived.\n\n**lint-dead-code** — a CSS block nothing renders, or a function nothing calls.\nAdvisory, because parked code is a legitimate state. The question to answer is\n*parked or dead?* — if parked, say so at the top of its contract; if dead,\ndelete it. This check exists because `system/components/uplevel/docs.md` was\nwritten as though the level-up pill shipped, when `renderUplevel()` has never\nbeen called.\n\n**lint-parity** — a mapped block no longer exists, or a path is wrong. If a\ncomponent was renamed, update `system/parity.json`. Never add a parity entry for\na Figma node you have not actually read: a guessed mapping looks authoritative\nand is worse than a reported gap.\n\n## Also worth checking, but not automated\n\n- Render the change at mobile (375px) and desktop widths.\n- Tab through it. Focus must be visible — `main.css` has a global\n  `:focus-visible` rule, so a control that loses it is doing something unusual.\n- Swap the theme (`system/tokens/themes/`) and confirm brand color follows.\n  Artwork with a baked fill will not, which is the bug class the color lint\n  exists to prevent.\n\nSee [system/AUTHORITY.md](../../../system/AUTHORITY.md) when two sources\ndisagree — there is no universal winner.\n"},{"path":".claude/skills/new-component-contract/SKILL.md","title":"New component contract","section":"Skills","bytes":2683,"markdown":"---\nname: new-component-contract\ndescription: Write a contract for a CSS block in the prototype. Use when documenting a component, when asked what a component is for or how it behaves, or before changing a component whose intent is unclear.\n---\n\n# New component contract\n\nA contract says what a component is *for* and what was *intended*. The CSS\nalready says what it does today. Per\n[system/AUTHORITY.md](../../../system/AUTHORITY.md), restating CSS values here\nis waste — and worse, a second copy that can drift.\n\n## Procedure\n\n1. **Find the block** in [system/components/INDEX.md](../../../system/components/INDEX.md).\n   Use the name exactly as listed, without the leading dot. The index is\n   generated, so if the block is missing, the CSS changed — regenerate first.\n\n2. **Read the implementation before writing.** Both the CSS rules and the\n   markup in `prototypes/navigation/main.js`. Writing from the class name alone\n   produces a plausible, wrong document.\n\n3. **Copy the template.**\n   ```bash\n   cp system/components/_TEMPLATE.md system/components/<block>.md\n   ```\n\n4. **Fill it in.** Use the preference grammar (Preferred / Allowed / Avoid /\n   Prohibited / Exception / Rationale) from AUTHORITY.md. Delete sections that\n   do not apply — an empty heading is worse than no heading.\n\n5. **State what is missing.** In the States table, mark states that do **not**\n   exist as explicitly absent. This is where contracts earn their keep: every\n   contract written in this repo so far has surfaced a real defect, and all of\n   them came from asking \"what should the hover state be?\" rather than from\n   reading code.\n\n6. **Regenerate and verify.**\n   ```bash\n   node evals/gen-component-index.js\n   node evals/lint-links.js\n   ```\n\n## Rules\n\n- **Do not fix what you find while documenting, silently.** If the contract\n  surfaces a bug, fix it as a separate, stated change — and say so in the\n  contract and in `decisions/DECISIONS.md`.\n- **Do not resolve a contradiction by guessing.** When the code and a comment\n  disagree and the repo cannot settle it, record it as a known discrepancy and\n  surface it. There is a worked example in\n  [uplevel.md](../../../system/components/uplevel.md) — a\n  `font-weight: 400` commented `/* Lato Semibold */`, left unresolved on\n  purpose.\n- **Measure claims.** If you assert a contrast ratio or a size, compute it.\n  A wrong number in a contract is worse than no number.\n- **One contract per pair where they share a base rule.** The module buttons\n  share one; `mod-btn-secondary/docs.md` is a pointer.\n\n## Good next candidates\n\nOrder by reuse, not size. Anything in the `primitive` tier of the index with no\ncontract yet.\n"},{"path":".claude/skills/new-prototype/SKILL.md","title":"New prototype","section":"Skills","bytes":2511,"markdown":"---\nname: new-prototype\ndescription: Add a new prototype, or a new page to an existing one. Use when asked to build a screen, flow or surface, or to put an existing mockup into the repo.\n---\n\n# New prototype\n\nA prototype here is not a throwaway. It is built from the real system, so the\nwork can feed back into components and decisions rather than being rebuilt.\n\n## Before building\n\n1. **Check what exists.** [system/components/INDEX.md](../../../system/components/INDEX.md)\n   for blocks, `system/icons/sheet.html` for icons, `product/` for what the\n   surface is supposed to do. Reuse beats reinvention — and a new block that\n   duplicates an existing one is the main way a design system rots.\n2. **Read the product doc** for the surface if there is one, in `product/`.\n\n## Structure\n\nPut it under `prototypes/<name>/`. Every page loads three stylesheets, **in this\norder**:\n\n```html\n<link rel=\"stylesheet\" href=\"../../system/tokens/tokens.css\" />\n<link rel=\"stylesheet\" href=\"../../system/tokens/themes/menopause.css\" />\n<link rel=\"stylesheet\" href=\"your-styles.css\" />\n```\n\nAdjust the depth to where the page actually sits. If it needs the UI glyphs,\nload `system/icons/registry-ui.js` **before** any script that calls `icon()`.\n\n## Rules\n\n- **No color in your stylesheet.** Everything comes from the token layer.\n  `evals/lint-tokens.js` blocks a hex or `rgb()` outside `system/tokens/`.\n- **No build step.** It must work opened straight from `file://`. That rules\n  out `fetch()` of local JSON — data files are `.js` assigning to `window`,\n  which is why `launcher/manifest.js` is not `manifest.json`.\n- **Keyboard focus** comes free from the global `:focus-visible` rule if you\n  use real `<button>` and `<a>` elements. Use them.\n- **Honour `prefers-reduced-motion`** for anything that animates.\n\n## Register it\n\nAdd one entry to [launcher/manifest.js](../../../launcher/manifest.js) under\nthe Prototypes section. That is the whole registration — never edit\n`index.html` markup to add a prototype.\n\n```js\n{ \"title\": \"...\", \"desc\": \"...\", \"href\": \"prototypes/<name>/index.html\",\n  \"kind\": \"page\", \"status\": \"live\" }\n```\n\n`status` is `live`, `draft` or `planned`. A `planned` entry renders as a\ndashed, non-clickable card, so the roadmap is visible without dead links — use\nit rather than leaving something unlisted.\n\n## Finish\n\n```bash\nnode evals/lint-tokens.js\nnode evals/lint-links.js\n```\n\nThen render it at 375px and desktop, tab through it, and swap the theme to\nconfirm brand color follows.\n"},{"path":"MANUAL.md","title":"MHT Design OS — Manual","section":"Root","bytes":4270,"markdown":"# MHT Design OS — Manual\n\nThe spec docs, as a flat index. Start with the root [README.md](README.md) for\nthe project overview and quick start, or open `index.html` for the same map as\na browsable launcher.\n\n**`system/`** is the design system — what is true about color, type, patterns,\nicons and brand. **`product/`** is the product knowledge — what each surface is\nand how it behaves. **`prototypes/`** is what was built with both.\n\n## system/ — the design system\n\n- **[tokens/tokens.md](system/tokens/tokens.md)** — token roles, color, type scale and\n  spacing, written implementation-agnostically so another project can map them\n  onto its own naming.\n- **[tokens/themes/README.md](system/tokens/themes/README.md)** — the two sites\n  (ThisIsMenopause, Legacy), what belongs in a theme, both loading models, and\n  how to add another site.\n- **[foundation/design.md](system/foundation/design.md)** — the design language: color\n  tokens, type scale + letter-spacing, spacing/radius/motion, the icon-tinting\n  system, and chrome asset provenance.\n- **[patterns/navigation.md](system/patterns/navigation.md)** — the global-nav chrome: top\n  nav, slide-out panel, level-up pill and global footer — behavior, component\n  structure, and Figma node ids.\n- **[tokens/reference.html](system/tokens/reference.html)** — the live token sheet.\n- **[icons/sheet.html](system/icons/sheet.html)** — the 112-icon line set, with search and\n  size/stroke controls.\n- **[motion/ai-pulse-spec.html](system/motion/ai-pulse-spec.html)** — the Ask AI pulse\n  motion study.\n- **brand/** — [brand source of truth](system/brand/brand-source-of-truth.md)\n  and [site design language](system/brand/site-design-language.md). The MHT\n  Style Guide PDF was removed on 2026-09-18 as outdated; the Figma file\n  (`zV2gbuNONeoyeRVcFUFWeZ`) is the live source. A direct link goes here once\n  added.\n\n## product/ — the product surfaces\n\n- **[landing.md](product/landing.md)** — the Splash Landing surface: the six\n  content-module sections (Checker, Listicles, Articles, Experts, Factoid,\n  Community), the splash CTA deep-link screens, and the landing's styling.\n- **[advisors.md](product/advisors.md)** — the Medical Advisory Committee page.\n- **[library.md](product/library.md)** — the Library surface (labelled \"Resources\" in the\n  panel): topic pages, Topic Center / Article Show, and the panel topic icons.\n- **[community.md](product/community.md)** — the Community surface: Posts / Q&A / Groups /\n  Meet-Others / All-Community list pages and their detail screens.\n- **[topic-hub.md](product/topic-hub.md)** — the Topic Hub: a per-concern hub aggregating\n  Q&A, conversations, groups and resources.\n- **[account.md](product/account.md)** — the Account surface: the profile dropdown and its\n  destination screens.\n- **[onboarding.md](product/onboarding.md)** — Sign Up Start and Registration Step, every\n  CTA that opens them, and the auth transitions not yet wired.\n\n## prototypes/\n\n- **[navigation/ARCHITECTURE.md](prototypes/navigation/ARCHITECTURE.md)** — the\n  `data-persona` render model, screen types and state machine, responsive\n  layout, the four personas, and the per-persona screen inventory.\n\n## decisions/\n\n- **[DECISIONS.md](decisions/DECISIONS.md)** — the chronological decisions log for the\n  whole project, interleaved in time order, plus what's \"on the horizon\". It is\n  **append-only**: entries record what was true when written and are not\n  rewritten later, so paths mentioned in older entries are historical.\n- **[RESTRUCTURE-PLAN.md](decisions/RESTRUCTURE-PLAN.md)** — the six-phase restructure,\n  its findings and current status.\n- **[LAUNCHER-ARCHIVE.md](decisions/LAUNCHER-ARCHIVE.md)** — the launcher sections\n  removed on 2026-09-21, with what each held and where those files still live.\n\n## Conventions\n\n- **[system/AUTHORITY.md](system/AUTHORITY.md)** — which source is authoritative\n  about what when artifacts disagree, and the Preferred / Allowed / Avoid /\n  Prohibited / Exception / Rationale grammar used to document usage.\n\nCode is authoritative about what *behaves* this way today, but not\nautomatically about what was *intended* — see AUTHORITY.md before resolving a\ndisagreement by editing the nearest file.\n"},{"path":"README.md","title":"MHT Design OS","section":"Root","bytes":10308,"markdown":"# MHT Design OS\n\nThe durable design workspace for **ThisIsMenopause** and the Legacy condition\nsites: the design system, the product knowledge, the prototypes built from\nboth, the decisions behind them, and the checks that keep them honest.\n\nThe design system is one subsystem inside it, under `system/`. Prototypes,\nproduct context, decisions and verification are the rest — which is why this is\na Design OS rather than a design system repo.\n\nPlain HTML/CSS/JS. No framework, no build step — open a file in a browser.\n\nStart at the **launcher** (`index.html`). A collapsible sidebar moves between\nfour sections:\n\n| Section | What it is |\n|---|---|\n| **Prototypes** | Working prototypes, built on the system |\n| **Color** | Every color token, resolved live from the loaded theme and mode |\n| **Type** | The type scale as specimens |\n| **Icons** | Both icon sets, searchable, click to copy the SVG |\n\nColor, type and icons render **inline** — nothing opens a new tab. Prototypes\nnavigate in the same tab; the hidden hotspot in a prototype's top-left corner\nbrings you back.\n\nThe launcher is deliberately **brand-agnostic**: its chrome is built from\nneutrals only, and the brand hue appears where it is the subject (a swatch, a\nspecimen) rather than as furniture. A tool serving two sites should not look\nlike one of them.\n\nIt supports **light and dark**, following the OS by default with a switch at\nthe bottom of the sidebar. Dark mode is opt-in by attribute so the prototypes,\nwhich are light-only product surfaces, are never dragged into it.\n\nSections for Components, Brand, Product and Decisions were removed on\n2026-09-21 to keep the front door to the basics. Nothing was deleted — see\n[decisions/LAUNCHER-ARCHIVE.md](decisions/LAUNCHER-ARCHIVE.md) for what moved\nand where it lives. All of it is still readable in the docs viewer at\n`system/docs/index.html`.\n\n## Quick start\n\nOpen the launcher and pick a flow:\n\n```bash\nopen index.html          # macOS — or just double-click it\n```\n\nEverything works straight from `file://`. To serve it over HTTP instead\n(any static server is fine):\n\n```bash\nnpx serve .              # then visit the printed URL\n# or: python3 -m http.server 8000\n```\n\nThen open the four flows from the launcher, or directly:\n`prototypes/navigation/visitor/`, `.../logged-out-member/`,\n`.../logged-in-member/`, `.../subscriber/`.\n\nThe launcher has a second section, **Entry Points** — four specific traffic\nsources, each starting from a mocked-up external page (a Google search result\npage, a Facebook ad, a Gmail inbox) that clicks through into a persona flow on a\nspecific starting screen instead of its default home: Paid Social (a specific\narticle), Organic + Paid Search (article variant and home variant), and a\nmember opening a content email. The external mocks live in `prototypes/entry-points/`;\nthe click-through uses a `?start=<screen-id>` query param read by `main.js` at\nboot (falls back to the normal home if the param is missing or doesn't match a\nreal screen in that persona).\n\n## Structure\n\n```\nindex.html                     Launcher — collapsible sidebar over every section\nlauncher/                      The launcher shell:\n  manifest.js                    Sections: a card grid, or a built-in view\n  launcher.css  launcher.js      Shell styles and rendering\n\nsystem/                        The design system\n  tokens/\n    tokens.css                   Universal palette — single source of truth\n    tokens.md                    Token roles, implementation-agnostic\n    manifest.js                  GENERATED mirror of tokens.css\n    reference.html               Live token page — renders tokens.css itself\n    themes/menopause.css         Purple hue family (production)\n    themes/legacy.css            Blue hue family (placeholder values)\n    themes/README.md             The two sites; how to add site 81\n  foundation/design.md         Design language (type, tinting, chrome assets)\n  patterns/navigation.md       Global-nav chrome (top nav, panel, footer)\n  parity.json                  Figma node <-> CSS block <-> contract\n  components/\n    index.html                   Component gallery — live specimens + inventory\n    manifest.js                  GENERATED inventory data\n    INDEX.md                     The same inventory as markdown\n    README.md                    Tiers, how to add a contract, known gaps\n    _TEMPLATE.md                 Component contract schema\n    <block>.md                   Hand-written contracts (one file each)\n  AUTHORITY.md                 Authority model + preference grammar\n  icons/\n    sheet.html                   Specimen page for both sets\n    editorial/*.svg              112 graphical line icons (display sizes)\n    ui/*.svg                     28 solid glyphs for interface chrome\n    categories.js                Authored grouping + display names\n    registry-*.js                GENERATED inline registries\n  motion/ai-pulse-spec.html    Ask AI pulse motion study\n  brand/                       Brand strategy and voice\n  docs/\n    index.html                   Docs viewer — renders every .md in the repo\n    content.js                   GENERATED mirror of the markdown\n  assets/\n    index.html                   Graphics gallery, grouped by purpose\n    manifest.js                  GENERATED asset inventory\n                                 Real assets exported from Figma (flat folder):\n                               logotype/logomark, advisor headshots, listicle\n                               icons, splash decoration. Nav/UI icons are\n                               inlined in main.js, not files.\n\nprototypes/                    Working prototypes, built on the system\n  navigation/                  The four-persona global-nav prototype\n    main.css  main.js            Styles and behavior (shared by its pages)\n    ARCHITECTURE.md              Render model, screen types, persona model\n    visitor/ subscriber/ logged-in-member/ logged-out-member/\n  entry-points/                Mock external pages (Google, Facebook, Gmail)\n                               that click through into a persona flow\n  community/                   Standalone Community surface\n\nproduct/                       Product surfaces (the what):\n  landing.md                     Splash Landing (modules + deep-links + assets)\n  advisors.md                    Medical Advisory Committee page\n  library.md                     Library / \"Resources\"\n  community.md                   Community (list + detail screens)\n  topic-hub.md                   Per-concern Topic Hub\n  account.md                     Account (profile dropdown + its screens)\n  onboarding.md                  Onboarding (Sign Up Start, Registration Step)\n\n.claude/skills/               How to work on this repo (committed)\ndecisions/\n  DECISIONS.md                 Chronological decisions log + \"on the horizon\"\n  RESTRUCTURE-PLAN.md          The six-phase restructure and its status\nevals/\n  check-all.js                 Runs every check; one exit code\n  lint-tokens.js               Fails on hex outside the tokens; audits rgba()\n  lint-links.js                Fails on an internal reference that does not resolve\n  gen-component-index.js       Regenerates system/components/INDEX.md (--check)\n  gen-icon-registry.js         Regenerates the icon registries (--check)\n  gen-token-manifest.js        Regenerates the token manifest (--check)\n  gen-asset-manifest.js        Regenerates the asset inventory (--check)\n  gen-docs.js                  Regenerates the docs viewer content (--check)\n  lint-parity.js               Validates system/parity.json; reports coverage\n  lint-dead-code.js            Unrendered blocks and uncalled functions (advisory)\nMANUAL.md                      Index of the spec docs\n```\n\n`.claude/skills/` holds the procedures for working on this repo — drift checks,\nwriting a component contract, adding an icon, starting a prototype. They are\ncommitted; only `.claude/settings.local.json` (local permission state) is\nignored. See [.claude/skills/README.md](.claude/skills/README.md).\n\n## How it works\n\nEach flow lives in its own folder and is a thin shell that sets one attribute and\nloads the token layer, a site theme, then the two shared files:\n\n```html\n<link rel=\"stylesheet\" href=\"../../../system/tokens/tokens.css\" />\n<link rel=\"stylesheet\" href=\"../../../system/tokens/themes/menopause.css\" />\n<link rel=\"stylesheet\" href=\"../main.css\" />\n<body data-persona=\"logged-in-member\">\n  <script src=\"../main.js\"></script>\n</body>\n```\n\nColor never appears in `main.css` — it lives in `tokens.css`, and the hue comes\nfrom the theme file. Swapping that one link re-skins the page. Run\n`node evals/lint-tokens.js` to check nothing has drifted back.\n\n`main.js` reads `data-persona` and renders that persona's nav/panel/dropdown and\nscreens — no framework, no build step, no persona switcher. The app is a **real\nresponsive product** (no device frame): a full-bleed mobile experience below\n1024px that reflows into a **desktop layout** — a persistent horizontal header\nand content capped/centered with fluid gutters — at 1024px and up. Content is\nconstrained (not fully fluid): a ~1000px general cap, a ~1194px splash canvas,\nand a 728px reading column. Layout mode is viewport-driven (matchMedia), so\nresizing the browser switches between mobile and desktop. Navigation happens via\n`data-screen=\"<id>\"` elements inside the prototype; each flow stays\nself-contained.\n\nThe full architecture — render model, screen types, responsive layout, and the\nfour personas — is in **[prototypes/navigation/ARCHITECTURE.md](prototypes/navigation/ARCHITECTURE.md)**.\n\n## Docs\n\n- **[system/AUTHORITY.md](system/AUTHORITY.md)** — the authority model (which\n  source wins about what) and the preference grammar used across component docs.\n- **[RESTRUCTURE-PLAN.md](decisions/RESTRUCTURE-PLAN.md)** — the six-phase restructure,\n  its findings and current status.\n- **[MANUAL.md](MANUAL.md)** — index of the spec docs (start here).\n- **[decisions/DECISIONS.md](decisions/DECISIONS.md)** — the chronological decisions log.\n- **[system/](system/foundation/)** — system & behavior: system, design, navigation.\n- **[product/](product/)** — the product surfaces: landing, advisors, library,\n  community, account, onboarding.\n"},{"path":"decisions/DECISIONS.md","title":"TIM — decisions log","section":"Decisions","bytes":125290,"markdown":"# TIM — decisions log\n\nThe chronological record of design/build decisions for the whole prototype —\nboth the global navigation ([navigation.md](foundation/navigation.md)) and the Splash\nLanding ([landing.md](domains/landing.md)). Entries are kept **interleaved in time\norder**, not split by surface, so the history reads as one timeline. Older\nentries are a record of what happened at the time — they are **not** corrected\nwhen later work supersedes them; a newer entry supersedes an older one in place.\nMonth sub-headings (`### YYYY-MM`) exist only so a reader or an agent can\nretrieve a slice of the timeline; they do not group or reorder anything.\nFilenames mentioned inside an entry that describe a *past* event (e.g. an\n`app.js` → `main.js` rename, \"`NOTES.md` is retired\") are left as they were\nwritten.\n\n---\n\n## Decisions log\n\n\n### 2026-07\n\n- **2026-07-24** — Built as plain HTML/CSS/JS to match `tim-splash-landing` conventions (no framework, no build step, easy Rails ERB port later).\n- **2026-07-24** — Chose to represent Panel/Dropdown as *interactive overlays* triggered from any screen, rather than as separate static tabs, since that's how they actually function (overlay on top of a base screen), even though Figma models them as separate frames.\n- **2026-07-24** — Subscriber nav styled like Member's, flagged as an assumption (see Open question in [navigation.md](foundation/navigation.md)) rather than invented independently.\n- **2026-07-24** — Icons and logo are placeholders (generic inline SVG set / text wordmark), not Figma exports, because the MCP asset URLs expire after 7 days and would break a prototype meant to last. Real asset pull-in is tracked as a follow-up (see [navigation.md](foundation/navigation.md) → Assets).\n- **2026-07-24** — Profile photo uses a stock placeholder image; Figma's own `placeholder_profile` illustration should replace it once assets are pulled locally.\n- **2026-07-24** — Pulled real assets from Figma (`42yas7Q9FfwhL6xUocjEAl`) into `assets/` and swapped out the placeholders: the inline-SVG icon set, the text-wordmark logo/logomark, and the hotlinked Unsplash profile photo are all gone. `app.js` now emits `<img>` tags pointing at local files (`../assets/...`); `main.css` sizes them instead of styling text logos or tinting inline SVGs. Re-verified the documented node ids against the live file first — they all still resolve and match the documented Assets tables, so no drift correction was needed. Icons keep the fills Figma exported (nav ink, library/community magenta, back-chevron blue). Three deliberate exceptions, all logged in [navigation.md](foundation/navigation.md) → Assets: the notification badge stays a CSS dot, the panel close (X) stays inline SVG (no Figma node for it), and the logged-in member avatar reuses `placeholder_profile` (no distinct headshot asset in source — matches the 2026-07-24 stock-photo decision below). Logo delivered as 3× PNG since the Figma nav logo is a raster image-fill, not vector.\n- **2026-07-24** — Restructured from one combined `index.html` (with a persona switcher) into one folder per persona (`/visitor/`, `/logged-out-member/`, `/logged-in-member/`, `/subscriber/`), each with its own `index.html` locked to that persona. Reason: individual flows need to be shared as standalone starting points without exposing the others. `NOTES.md` is retired — its node-id table now lives in `components.md`, and its \"how it works\" section is folded into this doc. Shared CSS/JS moved into standalone files under `/shared/` (later flattened to the repo root — see next entry). The root `index.html` is now a static picker linking out to each flow (not itself interactive). CTA buttons (\"Join\", \"Log in now\", \"Finish up now\") now do a real page navigation to `/logged-in-member/` instead of an in-page persona swap.\n- **2026-07-24** — Flattened the `/shared/` folder into the repo root to keep things lean: `main.css`, `app.js`, and `assets/` now sit at the top level. Each flow page references `../main.css` / `../app.js`, and `app.js`'s asset base became `../assets`. No behavior change — purely a path/layout move.\n- **2026-07-24** — Renamed `app.js` → `main.js` so the two shared source files read as a matched pair (`main.css` / `main.js`); flow pages now load `../main.js`. Also added a `README.md` and pruned two CSS rules left dead by the asset swap (`.icon-btn svg`, `.profile-btn svg` — everything in the nav is now an `<img>`).\n- **2026-07-24** — Removed the rig scaffolding from each flow page: the screen-tab switcher (`#screenTabs`) and the caption under the phone (`#stageHint`) are gone, along with their now-dead CSS and the `renderScreenTabs()` / hint code in `main.js`. Screen-to-screen navigation is meant to happen via elements linked *inside* the prototype instead — the `[data-screen=\"<id>\"]` click handler is kept for exactly that (any nav/panel element can carry `data-screen` to jump to a screen). Each flow page is now just its persona `<h1>` label + the 393×852 phone (the label is dropped in the next entry). Also swept out older dead rig CSS this exposed (`.rig-header*`, `.persona-tabs`, `.tab-btn*`, `.rig-footer*`).\n- **2026-07-24** — Dropped the persona `<h1>` header from each flow page too, to keep the page from scrolling vertically — a flow page is now just the 393×852 phone in a flush, centered `.stage` (padding removed). Removed the `.flow-header` CSS with it. Note: at full 393×852 the device still needs a viewport ≳860px tall to sit entirely scroll-free; below that the page scrolls rather than clipping the phone (the ≤430px-wide mobile media query already switches the phone to `100vh`).\n- **2026-07-24** — Added scale-to-fit (`fitPhone()` in `main.js`) so the device never causes a vertical scroll regardless of window height. When the viewport is shorter/narrower than the phone, the whole 393×852 device is scaled down with a `transform: scale()` (capped at 1× — it never upscales past the intended size), and the transform's leftover full-size layout box is collapsed with a negative `margin-bottom` so it doesn't reserve scroll height. Runs on load and on `resize`; skipped at ≤430px width, where the media query full-bleeds the phone instead. Net effect: the device renders at true 393×852 when there's room and shrinks proportionally to fit otherwise — no vertical scroll either way. This supersedes the \"needs ≳860px tall\" caveat in the entry above.\n- **2026-07-24** — Centered the device both vertically and horizontally instead of flush-to-top: `.stage` is now a full-viewport (`min-height: 100vh`) flex box that centers its child, and `fitPhone()` scales from `center center` with the collapsed layout box's negative margins split evenly on all four sides, so the scaled device sits dead-center at any window size (with no scroll either way).\n- **2026-07-24** — Wired the Panel (Library + Community) and Dropdown (account) items to actually navigate instead of being inert. Each navigating item in `TOPIC_ITEMS` / `TOPIC_ITEMS_MEMBER` / `COMMUNITY_ITEMS` / `DROPDOWN_MENU` carries a `screenId`, rendered as `data-screen`, and the existing `[data-screen]` click handler (already used for in-prototype links) sets the screen and closes the panel/dropdown — no second handler. Added placeholder destination screens, shared into each persona's `screens` array via `LIB` / `LIBRARY_SCREENS_FULL` / `LIBRARY_SCREENS_MEMBER` / `COMMUNITY_SCREENS` / `ACCOUNT_SCREENS`: Library `lib-*`, Community `com-*`, account `acct-*` (My Health / Messages / Notifications / Settings), all `uplevel`-type with a \"Home\" back bar. These in-page switches stay inside the persona, matching the boundary rule. Panel items are *list* views, so they get their own new screens rather than reusing the existing *detail* screens (Group Detail, Question Show, etc.), which stay as-is. **Log out is the one dropdown item that is not a screen** — it's an auth transition, so it uses `data-action=\"log-out\"` → `window.location.href` into the Logged Out Member flow (`../logged-out-member/`), joining Join / Log in now / Finish up as the folder-crossing CTAs. The Logged Out Member panel's items navigate into content screens even though that persona is gated (its panel already renders the member Library/Community lists, so the items had to point somewhere) — flagged to the user and left as-is. No screen-tab bar was re-added (it was removed earlier on 2026-07-24) — the user confirmed they don't want one, so the new screens are reachable via the panel/dropdown only.\n- **2026-07-24** — \"Log out\" changed from an in-persona placeholder screen to a real auth transition: it now carries `data-action=\"log-out\"` and navigates to the Logged Out Member flow (`../logged-out-member/`), joining Join / Log in now / Finish up as the folder-crossing CTAs. Removed the `acct-logout` screen.\n- **2026-07-24** — Nav/panel visual pass to match Figma:\n  - **Icons are now monochrome black source, tinted via CSS.** All 20 icon SVGs were normalized to black and are rendered as CSS-`mask`ed `<span class=\"icon\">` elements whose `background-color` is `currentColor`, so each context's `color` tints them: nav = ink, Library/Community = magenta (`--color-magenta`), back-chevron = blue, dropdown = ink. (Replaces the previous per-icon Figma fills + `<img>` approach.)\n  - **Panel:** light-pink circle (`--color-magenta-soft`) behind each Library/Community icon; wider (edge-bleeding) rollover; section labels back to 14px caps with normal tracking (they were double-spaced — a flex `gap` plus a label `margin`); \"Powered by\" now wraps to two lines; removed the close **X** entirely (Figma has none — dismiss via scrim); tightened item rhythm to ~24px feel.\n  - **Panel close animation:** `closePanel()` adds `.is-closing` → slower ease-out `slideOut`/`fadeOut` (0.32s), removed on `animationend`. Open eased to 0.24s ease-out.\n  - **Panel logo:** now a distinct asset at `assets/logo/panel-logo.svg` (user is providing it); until the file exists it falls back to the nav wordmark via `<img onerror>`.\n  - **Top nav:** logotype/logomark is now a button that returns to the persona's home (`go-home`); stacked visitor logotype reduced to 40px and pulled close to the menu icon (`nav__left` gap 2px); more air between the right-side icons when Join is present (`nav__right` gap 10px).\n  - Still TODO (next pass): add the global **footer** (Figma `6371:29` mobile / `6371:139` desktop) to all pages.\n- **2026-07-24** — Icon tinting reworked from CSS `mask` to **inline SVG**. The masked-`<img>`/`url()` approach tinted fine over http but the icons vanished when the prototype was opened directly (`file://` blocks external mask/SVG references). Fix: the 20 icons are now inlined in `main.js` as `ICON_SVGS` (each `fill=\"currentColor\"`), rendered as `<span class=\"icon\">…svg…</span>` and tinted by the context's `color`. Works over `file://` and http. Removed the now-redundant `assets/icons/*.svg` files (provenance node-ids kept in [navigation.md](foundation/navigation.md)).\n- **2026-07-24** — Added the **global footer** to every screen and made screens **vertically scrollable**. `.screen` is now a flex column: fixed nav/uplevel on top, a scrollable `.screen__scroll` holding the screen content + `renderFooter()`; panel/dropdown overlays stay pinned over the whole screen (don't scroll). Footer content mirrors Figma `6371:29` (branding + headline, two link columns, legal/disclaimer/copyright, CCPA \"Your Privacy Choices\" icon at `assets/footer/privacy-choices.png`). Footer links are non-navigating placeholders. The panel wordmark and footer wordmark both use the user-provided horizontal `assets/logo/logotype.svg`.\n- **2026-07-24** — Briefly added an optional `image` field (full-bleed sample screenshot as a screen body, `.screen__shot`) to test dropping in a Splash Landing capture, then **removed it entirely** — the capture bundled its own nav/footer (doubling the prototype's), and the decision is to build real content **modules** instead of pasting screenshots. Gone: the `image` branch in `render()`, the `.screen__shot` CSS, and the untracked splash png. The Visitor splash home now also uses `modules: true`, so every persona's home renders module content rather than a bare placeholder label.\n- **2026-07-24** — Root launcher cards stacked into a single column (was a 2-col grid). Added a hidden \"back to all flows\" hotspot: a small top-left circle (`.home-hotspot`, back-chevron icon) injected by `main.js` on each flow page, invisible until hover, linking to `../index.html`; hidden at ≤430px (where the phone is full-bleed). The launcher doesn't load `main.js`, so it has no hotspot.\n- **2026-07-24** — Level-up bar reworked. The panel/dropdown destination pages (`lib-*`, `com-*`, `acct-*`) are now top-level (`type: \"page\"`) with **no** level-up bar — there's nowhere up to go, and the nav logo returns home. The *detail* screens keep the bar, but it now steps **one level up** to the parent list (new `upTo` field → `data-screen`; e.g. Group Detail → `com-groups`, Question Show → `com-questions`, Member Profile → `com-meet`, Article Show → `lib-hrt`), instead of always `go-home`. It's framed as \"one level up,\" not \"back.\" (`program` has no list parent in the source, so it falls back to home.)\n- **2026-07-24** — Top nav auto-hides on scroll. The nav (+ level-up bar) moved into the scroll area as a sticky `.screen__chrome`; `attachAutoHide()` toggles `.is-hidden` (transform: translateY(-100%)) on scroll-down past ~80px and reveals it on scroll-up. `.screen__body--fill` keeps `min-height: 100%` on label/gated screens so the placeholder stays roughly centered and the footer sits below the fold.\n- **2026-07-24** — Kept the device fixed at 393×852 (responsive to larger iPhone sizes declined for now). Added a minimal set of placeholder content **modules** (`renderModules()`): a hero band + a \"Browse by topic\" card list, whose CTA/cards navigate via `data-screen` to real screens. Set `modules: true` on the home screens (Logged In Member, Subscriber) so they show realistic scrollable content instead of the flat centered label. Module/image screens flow at natural height (no `--fill`); label/gated screens keep the centered-label + footer-below-fold behavior.\n- **2026-07-24** — Added a \"Featured article\" module (in `renderModules()`, shown only where the persona has an `article` screen) that links into the Article Show detail page — so the level-up bar is actually reachable/demonstrable. Retargeted Article Show's level-up to the HRT topic (`upTo: \"lib-hrt\"`, label \"HRT & Other Treatments\") to match the featured card, giving a clean flow: home → featured HRT article → Article Show → level up → HRT topic page (top-level, no bar).\n- **2026-07-24** — Screen/panel polish pass: (1) `.screen__body` gets `min-height: 100%` so the content fills the viewport — the placeholder label centers in view and the footer starts below the fold (scroll to reach it). (2) Tightened the panel access-card spacing (`padding` 24→20, `gap` 16→10); combined with the earlier changes the Visitor panel (6 topics + card) now fits without scrolling. (3) Panel rollover changed from gray to a soft pink (`rgba(164,65,188,0.08)`, lighter than the icon circle) with a `0.18s` transition, and the icon circle deepens to `0.24` on hover — a small on-brand animation. (4) Footer: blue nav links have no underline; the legal/privacy links (`.footer__link--dark`) are underlined.\n- **2026-07-24** — Flattened `assets/` into a single folder (dropped the `logo/`, `footer/`, `profile/` subfolders — only 5 files across them, so the grouping wasn't earning its keep). Files now sit directly under `assets/` (`logotype.png`, `logotype.svg`, `logomark.png`, `placeholder_profile.svg`, `privacy-choices.png`); updated the `LOGO_FULL` / `LOGO_MARK` / `PROFILE_PLACEHOLDER` / `PANEL_LOGO` constants and the two inline footer paths in `main.js`, plus the folder references in `components.md` / `README.md`. Also removed a stray committed `.DS_Store`. Confirmed with the user that per-screen HTML files are **not** wanted — the JS-render model (one `main.js` renders every screen from `data-persona`/`data-screen`) is the intentional lean choice; real HTML pages would 6× the file count and duplicate all chrome.\n- **2026-07-24** — Level-up bar now **stays sticky** while the nav auto-hides. Split the old single sticky `.screen__chrome` into two independent sticky siblings: `.screen__nav` (auto-hides via `translateY(-100%)` on scroll-down, returns on scroll-up) and `.screen__uplevel` (stays pinned). `attachAutoHide()` sets the bar's `top` to the measured nav height while the nav shows, and animates it to `0` when the nav hides so the bar docks to the top edge. (First tried collapsing the nav inside one sticky chrome via negative margin — it zeroed the chrome height and clipped the bar out too; the two-sticky model is the fix.)\n- **2026-07-24** — Profile dropdown pass, following the Figma dropdown (7042:776) for the top spacing (user block → three 28px line rows, unhurried) plus a few explicit tweaks: **Notifications** moved **above the top divider** (deviation from Figma) so the unread count reads first, with a periodic **bell wiggle** (`bell-wiggle`, off under `prefers-reduced-motion`); the bottom **divider now sits above Log out** (was above Settings); the **Log out icon is rotated 90°** so the arrow points right (matches Figma). Menu order is now Notifications · ─ · My Health · Messages · Settings · ─ · Log out. Items carry an optional `iconMod` → `dropdown__item--{mod}` class for the per-item icon styling.\n- **2026-07-24** — Panel rollover tweaks: the icon circle **no longer deepens** on hover (removed the `0.24` hover state), the row rollover is now a **pill** (`--radius-pill`, was 12px) so the corners read as clearly rounded, and the rollover is a **single flat color** — the new opaque token `--color-magenta-soft-solid` (`#f8f0fa`, the over-white equivalent of the circle's `--color-magenta-soft` alpha). Both were then **lightened** together (circle alpha `0.14 → 0.08`, solid `#f2e4f6 → #f8f0fa`) so the whole hover treatment reads softer. On hover the icon circle goes **transparent** so it vanishes into the pill: one uniform color instead of a darker circle showing through (the earlier \"same tint\" attempt stacked two `0.14` alphas — circle over rollover — which read as two shades). Supersedes the \"circle deepens on hover\" / \"same tint\" notes above.\n- **2026-07-24** — Swapped in a user-provided `placeholder_profile.svg` (now a 128×128 self-contained circular avatar) and reworked the nav avatar around it: dropped `.profile-btn`'s gray background + border (the avatar is its own circle now), moved the circular crop onto an inner `.profile-avatar` wrapper, and re-parented the notification **badge** as a sibling so it sits **outside, on top of** the avatar's top-right edge (11px dot, 2px white ring) instead of being clipped by the avatar's `overflow: hidden`. This also fixed the avatar rendering non-square (it was being squished as a flex child); the block wrapper gives it a definite 36×36 box.\n- **2026-07-24** — Per-persona home/landing clarified:\n  - **Visitor** — the Splash Landing (hero + browse-by-topic + featured-article modules) is the landing; unchanged.\n  - **Logged In Member** — home is **Home as a hub (still to be designed)**, NOT the Visitor landing. Removed `modules: true` from its home; it now renders the placeholder label until the hub is designed. (The level-up-bar demo still lives on the Visitor/Subscriber landings, which have the featured-article module.)\n  - **Subscriber** — top nav and landing now **mirror the Visitor** exactly (`navVariant: \"visitor\"`, and its home screens mirror Visitor's Splash/Topic/Article + `LIBRARY_SCREENS_FULL`), **but** its slide-out panel keeps a subscriber-specific access card (`panelType: \"subscriber\"` → the \"Finish up now\" card). So: Visitor-style top nav + landing, subscriber-style panel upsell.\n- **2026-07-24** — Root launcher: title **left-aligned** (was centered); card order changed to **Visitor → Subscriber → Logged In → Logged Out**; refreshed the Subscriber and Logged In card copy to match the above. The hidden \"back to all flows\" hotspot now uses a **launchpad / 2×2 grid** icon (new `grid` entry in `ICON_SVGS`) instead of the back-chevron.\n- **2026-07-24** — Hid **scrollbars everywhere** (global `* { scrollbar-width: none }` + `*::-webkit-scrollbar { display: none }`) — scrolling still works, just no visible bar. Also removed all committed/stray `.DS_Store` files (already covered by `.gitignore`).\n- **2026-07-24** — Footer spacing: added **ample space above the footer** (`48px` margin-top on `.footer`) so page content doesn't sit flush against it, and bumped the bar's top padding to `28px`. (Briefly added a thin divider line at the top of the footer, then removed it at the user's request — the spacing alone provides the separation.)\n- **2026-07-27** — **Splash Landing content modules** built out from the Figma `Mobile_Splash_Landing` frame (file `EWsXKakhyFLhkse035AoHX`, node `4101:3`). `renderModules()` grew from the old minimal hero + \"Browse by topic\" placeholder into the full landing, in Figma order: **Checker** (hero with avatar stack + community count + symptom-checker card), **Listicles**, **Articles** (2-up grid), **Experts**, **Factoid**, **Community** (quote carousel + closing CTA). Renders on the personas whose home is the Visitor-style splash (**Visitor** and **Subscriber**); **Logged In / Logged Out** are unchanged (hub placeholder / gated home). Kept **\"Browse by topic\"** and the **featured-article** deep links as-is (prototype's own additions, not in the frame) so the nav flows still have real in-persona targets. New CSS tokens added to `:root`: `--color-purple-light` (`#d483cf`), `--color-navy-deep` (`#6a3693`), `--color-bg-purple-soft` (`#f6eff8`), `--color-bg-cool` (`#fafcff`), `--color-card-border`, `--color-pill-border`. **No fabricated data:** the community count renders as `[TBD] women in the community` (real figure needed from Eric/community team — not from Menopause_Survey_4), and community quotes are attributed to \"Community member\". The 82% / 72% stats and the \"Survey of 1,000 U.S. women ages 35–59\" source line come straight from the frame.\n- **2026-07-27** — **Splash refinement pass + sign-up / registration flows.** Aligned the splash to the Figma frame and the MHT Style Guide (`zV2gbuNONeoyeRVcFUFWeZ`, node `3941:17`): section padding **48px** top/bottom · **12px** sides; DM Serif headline sizes (hero/stat 40px, section/factoid/CTA 32px); size-based letter-spacing (**≥20px → -0.75px, ≤18px → -0.25px**); font sizes matched per-element. Removed \"Browse by topic\" (not in frame). **Listicles**: 5 cards from the desktop frame (`4113:51`), left-aligned, wider (210px), 80px icon circles using `assets/listicles_*.svg`, non-wrapping buttons, rollover uses `--color-primary-soft` (not pink). **Articles**: horizontal carousel, 324×170 thumbnails, simple diagonal-hatch placeholder pattern (no gradients), no eyebrows. **Experts**: solid `#EDF3F9` bg (`--color-bg-blue-soft`), avatar circles use the placeholder pattern. **Community**: the two real desktop-frame quotes (`4101:349`), no \"Community member\" label. **Hero**: avatar stack uses the placeholder pattern ([TBD] count kept). **Footer**: tagline copy → \"Expert advice. Real women. Real talk.\" at 20px/-0.75px Regular (frame `4101:162`).\n  - **Device**: retargeted the mock to **iPhone 16 Pro** (`--device-width/height` 402×874); bezel now sits outside the screen (`box-sizing: content-box`) so usable width is a true 402px, and `fitPhone()` measures the real rendered box.\n  - **Linking / new screens** (placeholder pages): Symptom Checker, Listicle Detail, Advisors, **Collection → All Collections** (level-up chain: Article Show → Collection → All Collections), Community Overview, Sign Up Start, Registration Step (Subscriber-only), and **My Profile** (the dropdown's \"View Profile\" now opens the member's OWN profile — a page with no level-up bar — distinct from the \"Someone's Member Profile\" reached via Meet Others). Splash CTAs: \"Check all my symptoms\" / \"Check symptoms first\" → Symptom Checker; listicle cards → Listicle Detail; one Article card → an Article Show that levels up to Collection; \"View all advisors\" → Advisors; \"Join the conversation\" → Community Overview; panel \"Get a preview first\" → Community Overview.\n  - **Cross-flows removed**: Join / Log in / Log out / Finish up no longer navigate to another persona's folder. **Join** family (nav, splash, panel) → **Sign Up Start**; Subscriber panel **Finish up now** → **Registration Step**.\n  - **Chromeless flow pages**: Sign Up Start and Registration Step render with no top nav, level-up bar, or footer — just an **✕** in the top-left that closes back to the page they opened from (tracked via `state.prevScreenId` / `close-flow`).\n- **2026-07-27** — **Docs reorganized into `foundations/`.** The two root spec docs were split by *surface* rather than by behavior-vs-components: `navigation.md` (nav behavior + components + assets), `landing.md` (Splash Landing), and this shared `decisions.md`; the old `components.md` was dissolved into the first two and `README.md` stayed at the repo root. `main.js`'s three provenance comments now point at `foundations/navigation.md`.\n- **2026-07-27** — **Auth / Sign-up carved into its own surface** ([onboarding.md](domains/onboarding.md)), out of `landing.md` where it had been nested. In the process, corrected stale docs against the code: `navigation.md` still claimed **Log out** did a real cross-folder navigation into `../logged-out-member/`, but the 2026-07-27 refinement made **Log in now** and **Log out** intentional **no-ops** (`data-action` with no handler — `main.js:732–734`). The docs now say so. **Join** / **Finish up now** open the chromeless `signup-start` / `registration-step` pages in-persona (`data-screen`), which is what the code does.\n- **2026-07-27** — **Community and Account carved into their own surfaces** ([community.md](domains/community.md), [account.md](domains/account.md)). Established the boundary rule: `navigation.md` owns the reusable **chrome mechanisms** (top nav, slide-out panel *container*, level-up bar, footer); each **content destination** — its menu items, screens, and icons — belongs to its surface doc. Community got the `com-*` list pages, the `group`/`question`/`activity`/`profile` detail screens, Community Overview, and the `community-*.svg` panel icons. Account absorbed the whole profile dropdown spec (§4) plus the `acct-*` screens, `my-profile`, and the `menu-*.svg` dropdown icons; `navigation.md` §4 is now just a pointer (the dropdown serves only Account, so it moved wholesale, unlike the panel which hosts multiple surfaces and stays chrome). Library remains nested in `navigation.md` until it's fleshed out.\n- **2026-07-27** — **Library carved into its own surface** ([library.md](domains/library.md)) too — the `lib-*` topic pages, Topic Center / Article Show browse screens, and the `topic-*.svg` panel icons. `navigation.md` §3 keeps the panel *container* and points both its Library and Community sections at their surface docs. Also **renamed the docs folder `foundations/` → `foundation/`** (singular) and **`auth.md` → `onboarding.md`** (the surface is the *start* of sign-up/registration; \"Auth\" read as too technical). Corrected another stale claim against the code while here: the `lib-*` / `com-*` / `acct-*` panel/dropdown destinations are `type: \"page\"` with **no** level-up bar (nav logo returns home) — the docs had described them as `uplevel` with a \"Home\" back bar, which the 2026-07-24 level-up rework had already superseded.\n- **2026-07-27** — Added two cross-cutting foundation docs and slimmed `navigation.md` to just the chrome. **[system.md](foundation/system.md)** — architecture & persona model: the `data-persona` render model, screen types (`tabs`/`page`/`uplevel`/`gated-home` + `chromeless`), state machine, device frame / `fitPhone()`, file layout, and the **Personas + Screens-per-persona** inventory moved out of `navigation.md`. **[design.md](foundation/design.md)** — the design language: the `--color-*` token catalogue, DM Serif type scale + the size-based letter-spacing rule (pulled from `landing.md`), spacing/radius/motion, the **icon-tinting system**, and the **chrome asset provenance** (logo, nav/uplevel icons, profile) moved out of `navigation.md` → Assets. `navigation.md` now keeps only the chrome components (top nav, panel container, level-up bar, footer) + interactions, pointing at system/design/surface docs for everything else. Surface docs' \"tinting mechanism\" refs repointed to `design.md`.\n- **2026-07-27** — Promoted two docs to the repo root and trimmed the README. The foundation index `foundation/README.md` → root **`MANUAL.md`**; the decisions log `foundation/decisions.md` → root **`DECISIONS.md`** (all-caps, sitting alongside `README.md`). The root `README.md`'s \"How it works\" was trimmed to a short quick-start that points at [system.md](foundation/system.md) rather than duplicating the render model / device frame / persona table. All cross-links repointed: the eight `foundation/*.md` docs now link up to `../DECISIONS.md`, while `MANUAL.md` and `DECISIONS.md` link down into `foundation/`.\n- **2026-07-28** — Dropdown: moved the **top divider above Notifications** (was above My Health), so it now separates the whole user block from the menu; Notifications still leads the menu. Order is now `User block · ─ · Notifications · My Health · Messages · Settings · ─ · Log out`. ([account.md](domains/account.md))\n- **2026-07-28** — New **in-page pill** pattern: an optional `pill: { label, screen }` on a screen renders a rounded `.page-pill` (label only, no chevron, `data-screen` navigation) at the top of the body — scrolls with the content instead of pinning as a bar. Applied to **Article Show (in Collection)** (`type: \"page\"` + `pill` → Collection) in place of its level-up bar; its parent **Collection** is now a plain `page` (no bar, no pill). Generic mechanism in `render()`, reusable by other pages. (Iterated: the pill first went on Collection with a chevron, then moved down to Article Show and lost the chevron.) ([navigation.md](foundation/navigation.md) §2)\n- **2026-07-29** — Synced Figma Global Navigation edits (footer + side panel), user-approved. **Panel:** section label **\"Library\" → \"Resources\"** (display only — the surface, `library.md`, and `lib-*` ids are unchanged); **\"All Topics\" → \"All Resources\"**; Community **\"Stories\" → \"Posts\"** and **\"All Activities\" → \"All Community\"** (icons/ids unchanged — `com-stories` / `com-activities`). Matching destination-screen titles and the two level-up back-labels (Topic Center → All Resources, Activity Show → All Community) renamed to match. **Footer:** legal item **\"Health Data Policy\" → \"Health Data\"**; copyright dropped **\"All Rights Reserved.\"** → now `© 2026 MyHealthTeam, A Swoop Company.`\n- **2026-07-29** — Synced the redesigned dropdown (Figma 7042:776) + a level-up label tweak. **Dropdown:** Notifications moved back **above the divider** and restyled as a distinct orange **chip** — faint orange fill (`--color-badge-soft` = `rgba(255,116,29,0.05)`), orange bell + label (`--color-badge`), `16px` radius, `12px` padding. The **notification badge color changed red → orange** (`--color-badge` `#e0483e` → `#ff741d`), so the nav avatar dot and the chip share it. Divider now sits below the chip (above My Health). **Level-up bar:** Article Show's back-label **\"Topic Center\" → \"Topic\"** (the destination page keeps its \"Topic Center\" title).\n- **2026-07-29** — Removed the **Topic Center level-up bar** (the one to \"All Resources\"): `topic` is now a top-level `type: \"page\"`. At that level the side panel + in-page navigation orient the user, so a level-up bar isn't needed. (Article Show still levels up to Topic.)\n- **2026-07-29** — Updated the **Hero and Factoid gradients** to match the revised Figma splash (`EWsXKakhyFLhkse035AoHX`): both are now **navy → magenta** (`--color-navy` `#2b2b68` → `--color-magenta` `#a440bc`) — hero at `184deg` (6%/94% stops), factoid at `180deg`. Retired the now-unused `--color-navy-deep` token (it was only the old factoid end stop).\n- **2026-07-29** — Added the splash **decorative graphical elements**, exported from Figma as **SVG** into `assets/` (grabbed via the export tool, not hand-added): hero concentric rings (`hero-rings-tl.svg`, `hero-rings-br.svg`) bleeding off the top-left / bottom-right corners behind the content (`.mod-hero__ring`, `z-index:-1` inside an `isolate`d `.mod-hero`), and a blob/dot element on each Factoid stat card top-right (`factoid-blob-1.svg`, `factoid-blob-2.svg`), clipped by the card's `overflow:hidden`. Used the isolated-stacking-context + negative-z pattern so the graphics sit above the section background but below the text (first attempt without `isolation:isolate` on the hero hid the rings behind the gradient).\n- **2026-07-29** — Splash polish: dropped the Figma **community count** (`12,345 women in the community`, replacing the `[TBD]` placeholder); tightened **Factoid stat-card padding** 32px → **24px**; and added the decorative magenta blob/dot graphic to the **closing CTA card** in both opposite corners (`closing-blob.svg`, one asset exported from Figma and reused), same isolated-stacking-context + `overflow:hidden` pattern as the factoid cards. Matched the Figma offsets (node `4101:141`): top-right `scaleY(-1)` at `top:-72/right:-156`, bottom-left `scaleX(-1)` at `bottom:-96/left:-154` — the two read as diagonal mirror-images (first pass used approximate offsets + a `rotate(180deg)`, which didn't match).\n- **2026-07-31** — Built the **Medical Advisory Committee** page (`advisors` screen, previously a labelled placeholder), from the separate Figma file **Medical Advisors** (`Zkiv6o4d7eyQOLAvwjVYTy`, mobile `2:43`). New `renderAdvisors()` + `ADVISORS` data (intro, five advisor cards — photo / name / role / affiliation / bio / non-navigating \"Read full bio →\" — and a \"Watch now\" video-placeholder section); `render()` branches on `screen.id === \"advisors\"` and it flows at natural height (excluded from `--fill`). **Real advisor headshots** exported from Figma as JPEG into `assets/advisor-{1..5}.jpg` (index = card order; mapping verified against the committee render so no photo is mismatched to a real person). (\"Read full bio\" and the Watch card are placeholders — no bio-detail page or real video in the prototype.)\n- **2026-07-31** — Corrections to the Advisors work: the **\"Watch Now\"** heading is a DM Serif headline in navy `#2b2b68` (title-case, not an all-caps magenta eyebrow); the top page **headline** is navy `#2b2b68` (not ink); the Watch **card** is the neutral diagonal-hatch **placeholder** used elsewhere (real thumbnail asset pending — removed the interim `advisor-watch.jpg`). Briefly used the advisor photos for the **splash hero** community-avatar stack, then **reverted** — the hero avatars stay hatch placeholders; the advisor photos belong only to the Advisors page.\n- **2026-07-31** — **Spun the Advisors page into its own [advisors.md](domains/advisors.md)** (one md per surface). It was nested in `landing.md`; that file now just links to it from the deep-link table, and `MANUAL.md` indexes it. The page is still a splash deep-link — its entry point stays in `landing.md`, its spec lives in `advisors.md`.\n- **2026-07-29** — Two splash fixes: (1) the hero avatar-stack ring was `--color-navy` (read as a dark border on the gradient) → changed to the Figma ring color `#6b3794`. (2) The horizontal **carousels** (Listicles, Articles, Community quotes) now go **full-bleed** — `margin: 0 -12px` + `padding: 0 12px` so they clip at the device edges instead of the section's 12px padding; first/last card stays inset while cards scroll in/out at the full width.\n- **2026-07-31** — **Split the docs into `foundation/` + `domains/`.** `foundation/` now holds only the cross-cutting *system & behavior* docs (`system.md`, `design.md`, `navigation.md` — the nav is the chrome/mechanism, not a surface); the six *product surfaces* (`landing`, `advisors`, `library`, `community`, `account`, `onboarding`) moved to a new `domains/` folder. Cross-links repointed: domain docs link up to `../foundation/…`, foundation docs down to `../domains/…`; `../DECISIONS.md` and same-folder sibling links were unaffected (both folders sit one level under root). `MANUAL.md` / `README.md` regrouped into the two sections; `main.js` unchanged (its provenance comments still point at `foundation/navigation.md`).\n\n### 2026-08\n\n- **2026-08-03** — **Side-panel redesign: one shared panel for every persona** (Figma Global Navigation `42yas7Q9FfwhL6xUocjEAl`, panel `7299:1987`). Replaced the per-persona Resources/Community menu lists **and** the Visitor/Subscriber \"Join for free\" / \"Finish up now\" access cards with a single structure: a **Home / Resources / Community** tab card, a list of eight **topic hubs**, and an **Explore (ToC)** pill. Rationale (user): move away from customising the panel per user type; make top-level Home/Resources/Community + **topic hubs the primary entry point to content and conversations**. Decisions: hub **labels stay the Figma placeholders** (\"Topic Hub Longer Name\" …) — real taxonomy TBD; **every hub opens the shared `topic` screen** (renamed **Topic Center → \"Topic Hub\"** — the Topic Center *is* the hub; Collections stays a separate construct → Explore points at `all-collections`); tabs resolve per-persona (Home → `persona.screens[0]`, Resources → `lib-all`, Community → `community-overview`). Because the panel is shared, `PANEL_TARGET_SCREENS` (`topic`, `community-overview`, `all-collections`, `lib-all`) are injected into every persona's `screens` so all targets resolve. Removed the now-dead `panelType` persona field, the `TOPIC_ITEMS` / `TOPIC_ITEMS_MEMBER` / `COMMUNITY_ITEMS` arrays, `renderPanelItems()`, and 11 now-unused topic/community `ICON_SVGS` (hub icons are pre-tinted `assets/hub-{1..8}.svg`, tab icons `assets/tab-{home,resources,community}.svg`). **Side effect + fix:** removing the panel's \"Finish up now\" card left `registration-step` (Subscriber) with no entry point. Fixed same day by making the **splash closing CTA card persona-aware** — for the Subscriber it becomes a \"You're almost in.\" / **Finish up now** card → `registration-step`; Visitors keep \"Join for free\" → `signup-start`. For consistency the **nav pill** is persona-aware too: **Finish** → `registration-step` for the Subscriber, **Join** → `signup-start` for the Visitor (both share the `visitor` nav variant, so the split is by persona key). These two are now the entry points to Registration Step.\n- **2026-08-03** — Two small tweaks after the panel redesign: (1) **Renamed the \"All Collections\" page → \"All Articles\"** (display label/title only; the internal id stays `all-collections`, still the Explore (ToC) target). (2) **Footer: replaced the \"Accessibility\" placeholder with \"Medical Advisors\"**, linking to the built Advisors page (`data-screen=\"advisors\"`). Since the footer is global chrome, `advisors` joined the shared-injection set — and that set (formerly `PANEL_TARGET_SCREENS`) was renamed **`SHARED_TARGET_SCREENS`** and now also carries `advisors`, so the Advisors page resolves in all four personas (verified on Logged In Member).\n- **2026-08-03** — **Replaced the full-width level-up bar with a sticky level-up *pill*** (Figma Global Navigation `42yas7Q9FfwhL6xUocjEAl`, pill `7294:1952`, frame `7283:153`). The pill sits in-page top-left but keeps the bar's exact pinning behavior — the sticky `.screen__uplevel` container is now transparent (so the pill floats) and `attachAutoHide` still pins it below the nav / docks it to the top edge when the nav hides. Styling: blue-tinted pill (`rgba(15,87,168,0.1)` bg+border, radius 48, 6×12 pad, 34px tall), a 16px **section icon** (pre-tinted blue `assets/up-*.svg`) + parent label (Lato Semibold 14px `#0f57a8`). No chevron. Per-screen `upIcon` added; labels/targets follow the linked frame — **Article Show now levels up to the Topic Hub** (`topic`, was the HRT topic page `lib-hrt`) and **Activity Show to Posts** (`com-stories`, was All Community). Removed the now-unused `back` `ICON_SVGS` entry. The separate non-sticky in-body `.page-pill` (Article Show → Collection) is unchanged — the deliberate scroll-with-content sibling.\n- **2026-08-03** — **Removed the \"Collection\" label pill (`.page-pill`), replaced with an in-page \"series\" callout box** on the Article Show (in Collection) screen. The pill was outdated; the new `.series-box` is a placeholder callout (\"Part of a series\" / \"This article is part of a series.\" / **\"Explore the full series →\"** → `collection`). Screen config changed from `pill: { label, screen }` to `series: { screen }`, rendered by `renderSeriesBox()`; the `screen.pill` render path and `.page-pill` CSS were deleted. Also **dropped this screen's page-title label** (\"Article Show\") — the callout box is now its only in-body chrome. `--color-pill-border` stays (still used elsewhere).\n- **2026-08-03** — **Built out `community-overview` as the `/community` hub landing.** Replaced the bare placeholder label with a **module-based render** (data array `COMMUNITY_MODULES` + `renderCommunityHub()`, mirroring the Splash Landing pattern), a lightweight **orientation menu** — not a content feed. Five outlined-box modules in fixed order: **Activity Feed** → `com-activities`, **Q&A** → `com-questions`, **Groups** → `com-groups`, **People** → `com-meet`, **Community Values & Ambassador Program** → new placeholder `com-values`. Each box has a title, one-line description, optional count **badge** (always the literal `[TBD]`, never fabricated — per the 2026-07-27 rule), and a \"View →\" link. Wireframe-level only (no live previews). Since the shared panel's single **Community** tab now points here, the hub is the **primary** community-nav surface; its five targets were added to `SHARED_TARGET_SCREENS` so they resolve in every persona (incl. Visitor/Subscriber). **`com-stories` (Posts) retired** — Stories folded into Activity: removed the screen, relabelled `com-activities` \"All Community\" → **\"Activity\"**, and repointed the Activity Show level-up pill to `com-activities` (\"Activity\", icon `up-posts.svg` → `up-activity.svg`). **Panel note:** collapsing the panel's old 5-item Community section to a single Community row was already done in the 2026-08-03 flat-panel redesign — no further panel change needed here. Docs: `community.md` (hub module structure + panel→Community table now one row), `navigation.md` §2 (Activity pill), `system.md`, `design.md`.\n- **2026-08-03** — **Built the first Topic Hub — a new surface.** A Topic Hub aggregates content across feature types (Q&A / conversations / groups+people / resources) for one popular patient concern (up to 8 per site), richer than the `/community` orientation menu (its modules carry content-preview affordances). Templated & data-driven: `TOPIC_HUBS` array + `TOPIC_HUB_PAGES` (maps each entry to a `type:\"page\"` screen with a `topicHub` ref) + `renderTopicHub()`; `render()` branches on `screen.topicHub`. First example **\"Newly Diagnosed\"** (`topic-hub-newly-diagnosed`) — the near-universal concern. Header = name + one-line description + a `[TBD]` stat pill (no fabricated number). Five modules, fixed order: **Top Q&A** → `com-questions` (with an italic note: no topic-filtering yet, so it's the general Q&A page), **Active Conversations** → `com-activities`, **Expert Resources** → `lib-all` (chose the brief's `lib-all` fallback over a single `article` — \"Expert Resources\" is a curated set, plural, and `lib-all` resolves everywhere), **Groups & People** → `com-groups`, **Action-Oriented Tools** → **disabled/dead** (no tools surface exists yet; dashed \"Coming soon\" box). Preview modules show skeleton placeholder rows — no fabricated posts/questions/photos (wireframe fidelity). Hub screens added to `SHARED_TARGET_SCREENS` so they resolve in every persona. **Disambiguation:** this is NOT the old `topic` screen (still mislabelled \"Topic Hub\", actually evolving into Collections) — flagged in code + docs; reconciling that label + the panel's \"Topic Hub\" rows is out-of-scope panel/Collections work. **Reachability:** one temporary entry point — a \"Popular topic hubs\" section at the bottom of the Community Overview hub (full panel wiring out of scope). **Docs:** new `domains/topic-hub.md` (placed in `domains/` with the other surfaces, not the literally-requested `foundation/` — flagged in that doc), added to `MANUAL.md`; `community.md` notes the temporary entry point.\n\n- **2026-08-03** — Topic Hub tweaks: (1) **Made the hub generic** — one reusable `topic-hub` (\"Topic Hub\") instead of the specific \"Newly Diagnosed\", so it serves as the template for every panel row (real per-concern hubs/names come later). (2) **Entry point moved to the side panel** — the panel's topic-hub rows now open `topic-hub` (`data-screen` \"topic\" → \"topic-hub\"); **removed** the temporary \"Popular topic hubs\" section from the Community Overview hub. (3) **Cards are whole-tappable** — dropped the blue \"View →\" / \"Explore →\" arrow links from both the Community hub and Topic Hub module cards; the card itself navigates. (4) **Tap/press feedback** — added `:active` press states (light-blue tint + border + slight scale, `-webkit-tap-highlight-color: transparent`) since this is a mobile prototype (tap/press, not desktop hover); the disabled Tools module is excluded. Docs: `topic-hub.md`, `community.md`, `MANUAL.md`.\n- **2026-08-03** — **Converted `:hover` → `:active` across the in-phone product UI** (mobile prototype = tap/press, not desktop rollover). All 13 remaining interactive `:hover` rules (hero/checker/listicle CTAs, `.mod-card` / `.mod-article`, primary/secondary buttons, `.icon-btn`, `.join-btn`, `.welcome-card__cta`, panel tab/hub/explore) now fire on press. The two card-lift rules (`.mod-card`, `.mod-article`) changed from a `translateY(-1px)` lift to a `scale(0.99)` press-down for a consistent pressed feel. **Left as `:hover`:** `.home-hotspot` (the hidden reveal-on-hover launcher escape — a dev affordance, not product UI) and `.launcher__card` (the desktop static picker, viewed outside the phone).\n- **2026-08-04** — **Doc/comment drift fix, no behavior change.** `foundation/navigation.md` §3 still said \"every hub opens the shared `topic` screen ('Topic Hub' — the Topic Center *is* the hub)\" — stale as of the 2026-08-03 Topic Hub tweaks, which repointed panel hub rows to the new generic `topic-hub` screen (confirmed against `main.js` — the panel button already renders `data-screen=\"topic-hub\"`). Corrected §3 to describe the current wiring and to note that `topic` (the old Topic Center) is still injected into `SHARED_TARGET_SCREENS` only because Article Show's level-up pill targets it, not because panel rows do. Also added an explicit ⚠️ naming-collision callout at the §2 pill table (Article Show's pill is labelled \"Topic Hub\" but targets the *old* `topic` screen, not the new surface) — the collision itself was already known and tracked in \"on the horizon\" below, this just makes it visible at the point in the doc where a reader would actually trip over it, rather than only in `topic-hub.md`. Two stale code comments in `main.js` (near `HUBS` and `TOPIC_HUB_SCREEN`) that still said \"the Topic Center *is* the hub\" — contradicting the correct disambiguation comment already sitting a few lines below them — were corrected to match. No screen, id, label, or rendered behavior changed.\n- **2026-08-04** — **Reconciled the Topic Hub / Topic Center name collision.** The old `topic` screen had briefly been mislabelled **\"Topic Hub\"** (from the panel redesign, when \"the Topic Center is the hub\" was the thinking) — colliding with the genuinely new `topic-hub` surface. Reverted `topic`'s `label`/`title` back to **\"Topic Center\"** (its honest identity, and what `library.md` already called it), renamed the const `TOPIC_HUB_SCREEN` → `TOPIC_CENTER_SCREEN`, and updated both Article Show pills' `backLabel` \"Topic Hub\" → \"Topic Center\" (still `upTo: \"topic\"`, icon `up-hub.svg` reused — no dedicated Topic Center icon). Net: `topic` = **Topic Center** (Collections-bound, where Article Show levels up); `topic-hub` = **Topic Hub** (the new surface, opened by the panel hub rows). No id changes, no rendered-navigation changes — just labels/naming. Verified in-browser (old screen now titled \"Topic Center\", Article pill reads \"Topic Center\", new hub still \"Topic Hub\", panel rows still → `topic-hub`). Docs: `navigation.md` §2/§3 (collision ⚠️ notes removed/replaced), `library.md`, `topic-hub.md`. **Remaining:** actually folding Topic Center into Collections (a design decision), tracked below.\n- **2026-08-04** — **Folded Topic Center into Collections; kept Topic Hub.** Completed the reconciliation above. **Retired the old `topic` \"Topic Center\" screen** (removed `TOPIC_CENTER_SCREEN` + its injection) — it was \"a collection of sponsored content,\" so it's now simply a **Collection** (the existing `collection` screen). **Article Show now levels up to a Topic Hub** — both `article` pills changed `backLabel` \"Topic Center\" → **\"Topic Hub\"**, `upTo` `topic` → **`topic-hub`** (icon `up-hub.svg` kept). The panel's up-to-8 hub rows are the pre-defined Topic Hubs (unchanged, → `topic-hub`). **Rebranded the article-in-collection callout from \"series\" → \"collection\"** (an article belongs to a *collection*, not a *series*): field `series` → `collection`, `renderSeriesBox()` → `renderCollectionCallout()`, CSS `.series-box*` → `.collection-callout*`, copy \"Part of a series / Explore the full series\" → \"Part of a collection / Explore the full collection\". Net model: an Article Show links **either** up to a **Topic Hub** (pill) **or** to a **Collection** (callout); no standalone Topic Center remains. Verified in-browser (Article Show pill \"Topic Hub\" → `topic-hub`; collection callout \"Part of a collection\" → `collection`; no refs to the retired `topic`; no console errors). Docs: `navigation.md` §2/§3, `library.md`, `system.md`, `landing.md`, `topic-hub.md`.\n- **2026-08-04** — **Launcher: added an Entry Points section alongside User Types.** The four persona flows (User Types — who someone is) previously covered the whole launcher; added a second section for Entry Points — where someone lands, not who they are. Mechanism: `main.js` reads a `?start=<screen-id>` query param at boot and uses it as the initial `state.screenId` instead of `persona.screens[0].id`, falling back to the normal default if the param is absent or doesn't match a real screen id in that persona. No new screens, folders, or personas needed for the click-through target itself — every Entry Point reuses an existing persona + existing screen.\n- **2026-08-04** — **Entry Points revised: trimmed to four, each fronted by a mocked external page.** Replaced the initial six-card guess (Search, Paid Social, New Member, Email, Organic, PWA — several of which just collapsed onto the same screen with no real distinction) with the four that matter, per real traffic data: **Paid Social** (a specific article), **Organic + Paid Search — article variant** and **— home variant** (306K combined organic + paid, two different results from the same search), and **a member opening a content email**. Each now starts from a standalone external-context mock instead of linking straight into the phone frame: new `entry-points/` folder — `google-search.html` (one mocked SERP with two real result rows, `id=\"article\"` and `id=\"home\"`, plus two grayed-out unrelated results for realism; `:target` styling highlights whichever the launcher card links to via `#article`/`#home`), `facebook-ad.html` (single sponsored post), `gmail-inbox.html` (inbox list, one unread message from \"This is Menopause\" among generic filler rows). These are self-contained (their own inline styles, no `main.css`/`main.js`) since they represent products outside TIM entirely — generic layouts evoking each context, not literal trademarked UI. Click-throughs: Facebook ad and the search article result → Visitor `?start=article`; the search home result → Visitor (default home); the Gmail message → Logged In Member `?start=article` (a content email reads as an article deep-link, not a \"someone replied\" notification, so this replaced the earlier `question` target). Docs: `README.md` (Quick start + Structure).\n- **2026-08-04** — **Launcher copy pass: cards describe what a flow is, not its status.** Removed the top-of-page intro paragraph (\"Pick a flow to preview it on its own...\") and the Entry Points section's explainer note — both were about the launcher itself, not the flows. Reworded card copy that had drifted into describing gaps/process instead of the current experience: Subscriber (\"currently mirrors... pending a subscriber-specific design\" → states what it actually shows today); Logged In Member (dropped the \"(still to be designed)\" aside on Home). Removed now-dead CSS (`.launcher__title p`, `.launcher__section-note*`) left behind by the copy removal.\n- **2026-08-04** — **Entry Points card titles simplified to \"Title – Text.\"** Dropped \"(306K combined)\" and the word \"variant\" from the two search cards, and the word \"specific\" from Paid Social. Titles are now **Paid Social – Article**, **Organic + Paid Search – Article**, **Organic + Paid Search – Home**, **Content Email – Article** — channel name, en dash, destination. Card copy underneath unchanged.\n- **2026-08-04** — **Entry-point mocks now render inside the same phone frame as the rest of the prototype**, instead of as full desktop-width pages. Each of the three `entry-points/*.html` pages links `../main.css` (for the shared `--device-width`/`--device-height` tokens and the `.stage`/`.phone` chrome — same 402×874 device, full-bleed under 430px) and wraps its mock content in `<div class=\"stage\"><div class=\"phone\" id=\"phone\">…</div></div>`, with the mock's own look (Google/Facebook/Gmail-style fonts and colors) scoped to a `.mock` wrapper instead of `body`, so it doesn't fight main.css's own body rules. New shared `entry-points/fit-phone.js` replicates `main.js`'s `fitPhone()` scale-to-fit behavior standalone — deliberately not loading the real `main.js`, since these pages have no `<body data-persona>` and it would error looking one up. Net effect: opening an Entry Point now feels like unlocking a phone to a search result, an ad, or an inbox, then tapping into the product — not switching from a desktop browser to a phone mid-flow.\n\n- **2026-08-04** — **Repo renamed `tim-user-types` → `tim-navigation-prototype`** (local dir, GitHub repo, and `origin` remote; GitHub keeps a redirect from the old name). The name now reflects what this is — a navigation prototype fronted by the launcher — rather than the original \"user types\" framing. README reframed around the **launcher** (the entry point) instead of leading with the Splash Landing; per-surface Figma frames are now cited in `domains/` rather than in the README. Also folded in: the nav **logomark** switched to `assets/logomark.svg` (vector) and the old `logomark.png` was removed; the Subscriber splash closing card is now a single headline, \"Don't miss out! You're almost in.\"\n- **2026-08-05** — **Built the skin-care Article Show and wired it up.** Turned the placeholder `article` screen into a real, scrollable article (`renderArticle()` + `.art-*` styles), matching the Figma **Articles** file's `Mobile_Article` frame: title-first header (byline + reaction/save), Key Takeaways card, `Poll` placeholder box, body sections, a **collapsed** \"Best Ingredients\" with a white fade + **Read full article** link, then the **Join the conversation** input, the **Menopause, answered** AI presets + **Ask AI**, a **Keep Reading** carousel, and a persona-aware closing **access card** (reuses the splash `.mod-cta-card` — Visitors \"Join for free\" → `signup-start`; Subscribers \"Finish up now\" → `registration-step`). Nav, the level-up pill, and footer come from shared chrome. **Splash Articles carousel:** the skin-care article is the **first** card (→ `article`), the **second** is the Collection (→ `article-collection`), and the rest are labelled placeholders via a new `article-placeholder` screen (so the built article is reachable only from the first card + entry points). **Entry points** (Facebook ad, Google result, Gmail email) rewritten to the skin-care article; all still deep-link via `?start=article`. Verified in-browser across Visitor + Subscriber; no console errors. Docs: `library.md`, `landing.md` are now behind on the article specifics (tracked below).\n- **2026-08-05** — **Uplevel pill: floating opaque `#EDF3F9`, real `derm` icon, scroll-only glow.** The pill's sticky container is transparent (`pointer-events:none`) so the pill floats in-page; its own **opaque** `--color-bg-blue-soft` fill (`#EDF3F9`, `1px #d6e2f1` border) is what stops article text bleeding through (replacing the old `rgba(15,87,168,0.1)` translucent fill and the briefly-tried white full-width strip). The Article Show pill now reads **\"Skin-related Hub\"** with a dedicated **`derm`** glyph (from the Figma pill) instead of the brain (`up-hub`). A soft blue drop-shadow is applied only once the pill floats over scrolled content — `attachAutoHide` toggles `.screen__uplevel.is-lifted` past `scrollTop > 8`; no glow while idle at the top on white.\n- **2026-08-05** — **Migrated `up-*` / `hub-*` icons to the inline `currentColor` system; removed the baked-circle SVGs.** All six level-up (`up-*`) and eight topic-hub (`hub-*`) glyphs now live inline in `ICON_SVGS` (`fill=\"currentColor\"`, glyph only). `renderUplevel` inlines the glyph when it's in `ICON_SVGS` (blue, via the pill's `color`), falling back to an `<img>` only for any icon not inlined; `renderPanel` inlines the hub glyph into a `.panel__hub-icon` span where the pale `#F6EFF8` circle and magenta tint are drawn by **CSS** rather than baked into the file. The 14 now-unused `assets/up-*.svg` / `assets/hub-*.svg` files were deleted. Rationale: don't bake circles into SVGs, default to `currentColor`, tint via CSS — consistent with the existing nav/dropdown inline-icon system. Docs: `foundation/design.md` (icon-tinting classification + chrome-asset table), `foundation/navigation.md` §2 (pill styling + uplevel icon table).\n- **2026-08-10** — **Desktop content-width pass: two widths, applied per page type.** Enforced the design rule of exactly two desktop widths — **1000px** for overviews (splash, community/topic hubs — unchanged) and **728px** (`--content-column`) for reading/content pages. **Article** and **Medical Advisors** were left-aligned to the 1000px box; both are now the **728px column centered in the viewport** (Advisors also drops the desktop two-up card grid back to a single-column divider list — it reads as a content page, not an overview). **Sign Up Start** (chromeless flow) had no desktop treatment (full-bleed, ✕ in the viewport corner); it's now a **728px centered column with the ✕ inside its top-left**. **Dropped the desktop breadcrumb bar** for now (`renderDesktopHeader` sets `crumb = \"\"`; detail screens show no up-nav on desktop). **Closing \"Join\" CTA card** refined to a clean two-column split — copy left (fills), buttons a compact **300px** right column (was two equal halves). Verified in-browser at desktop width (Article/Advisors/Sign Up centered at 728; no breadcrumb; card measured 1000px wide with 556px text + 300px buttons side-by-side); no console errors. CSS-only + the one-line `crumb` change in `main.js`.\n- **2026-08-10** — **Advisors: responsive card layout + intro/watch fixes** to match the Figma Medical Advisors frames (`Zkiv6o4d7eyQOLAvwjVYTy` — desktop `1:6`, mobile `2:43`). (1) **Cards are stacked on mobile, horizontal on desktop.** Base `.advisor-card` is a column (96px photo on top, then a new `.advisor-card__text` block) per the mobile frame; the desktop `@media (min-width:1024px)` block flips it to `flex-direction: row` (photo left, text right). Photos are plain circles for now (the Figma's subtle decorative ring not reproduced). (2) **Intro weights:** the closing italic line is **semibold italic** (`.mod-advisors__body--italic` → `font-weight: 600`); the lede stays **regular** (a brief semibold experiment was reverted — the Figma lede is regular). (3) **Watch card is now `aspect-ratio: 16/9`** (`width: 100%`) instead of a fixed 218px height, so the video frame holds a true 16:9 at both mobile width and the 728px desktop column (was ~3.3:1 / too short on desktop). Verified desktop + mobile; no console errors.\n- **2026-08-10** — **Side panel aligned across all personas; per-persona card added** (Figma Global Navigation `42yas7Q9FfwhL6xUocjEAl` — logged-in `7417:3712`, logged-out \"Access\" `7417:3673`). Consolidated the two panel renderers (`renderAnonPanel` / `renderMemberPanel`) into one `renderPanel`: **brand → persona card → TOPICS → footer** for everyone. Only the top card differs, now data-driven from `PANEL_CARDS`: Visitor \"Don't miss out!\" (Join for free), Subscriber (Finish up now), **Logged-Out** \"Welcome back, Jannie123! / You're not logged in.\" (Log in now = no-op `log-in` action, Join for free → `signup-start`), **Logged-In** \"Jannie123, how are you today? / It's okay to open up.\" (**Share now → `com-activities`/Activity**, **Ask a question → `com-questions`/Q&A**). This **dropped the members' Home/Resources/Community tab card + Explore pill** (the 2026-08-03 shared-panel layout). **TOPICS list is now identical for all personas** — 3 library topics + All Articles (`PANEL_TOPICS`) — replacing the members' 8 topic-hub rows in the panel. Made `signup-start` a shared injected screen (`SIGNUP_START_SCREEN`) so \"Join for free\" resolves for the logged-out member. Card reuses the existing `.panel__promo` styling (Figma's decorative corner blobs not reproduced). `HUBS` and the `.panel__tab*` / `.panel__top` / `.panel__explore` CSS are now unused by the panel (left in place; the desktop header still has its own inline nav). Verified all four personas in-browser (cards + 4-item TOPICS; Share→Activity, Ask→Q&A, Join for free→Sign Up Start all resolve); no console errors.\n- **2026-08-11** — **Panel corner graphics** (supersedes the \"decorative corner blobs not reproduced\" note above): every persona's panel card now carries the two decorative corner blobs (`.panel__promo-graphic--tr` top-right, `--bl` bottom-left; `closing-blob.svg`, mirrored per corner, clipped by `.panel__promo` `overflow: hidden`). Also landed alongside: **per-topic Topic Hubs** (`topic-hub-hrt/mood/sleep` via a `makeTopicHub` factory, replacing the single generic hub behind the panel rows) and the All-Articles row renamed **\"Explore All Articles\"** (`all-collections`). (Committed in `15e3784`.)\n- **2026-08-11** — **Logged-Out home: welcome card removed, placeholder label restored.** The Logged Out Member's home (`home-gated`) was a `gated-home`-type screen rendering a \"Welcome back… you're not logged in\" welcome card. That whole surface is retired — the screen is now a plain `tabs` placeholder labelled **\"Home as a hub (gated)\"**, matching the Logged-In Member's placeholder home. Removed `renderGatedHome()`, the `gated-home` render/type branch, and all `.welcome-card*` CSS. The \"Welcome back, Jannie123!\" copy and the **Log in now** CTA now live **only** in the panel access card (see the 2026-08-10 panel entry).\n- **2026-08-11** — **Collection article now uses the full article template.** The **Article Show (in Collection)** screen (`article-collection`, splash \"one Article card\" deep-link) previously rendered only a bare collection callout. Refactored the standalone article into a shared `renderArticleShell(content)`; `renderArticle()` (skin-care copy) and the new `renderCollectionArticle(collection)` both call it. The collection variant passes generic placeholder copy (\"Article Title Goes Here\", placeholder byline/takeaway/lede/2 sections) plus `topCard: renderCollectionCallout(collection)`, so the **\"Part of a collection\" card sits directly above Key Takeaways** in an otherwise-normal article. Verified in-browser: collection article shows the callout above Key Takeaways; the standalone skin-care article is unchanged (own title, no callout); no console errors.\n- **2026-08-11** — **Article template: poll placeholder replaced with a body paragraph.** The shared `renderArticleShell` carried a `.art-poll` placeholder box between the lede and the body sections. Removed it (and its CSS); the shell now renders an optional `midPara` body paragraph in that spot instead. Both callers supply copy — real copy for the skin-care article, placeholder copy for the collection article. Verified in-browser on both articles (no poll; extra body paragraph present; collection callout still above Key Takeaways); no console errors.\n- **2026-08-11** — **Side panel: \"Explore All Articles\" → \"All Articles\" + a list icon.** Dropped \"Explore\" from the panel's last TOPICS row (`PANEL_TOPICS`) and gave it a placeholder **`list`** glyph (three rows + bullet dots, added to `ICON_SVGS` in the 44×44 hub-icon style) — it previously rendered iconless. Also renamed the destination page (`all-collections` / `ALL_ARTICLES_SCREEN` label + title) to \"All Articles\" so the nav item and the page it opens stay consistent. Verified in-browser (panel row reads \"All Articles\" with the list icon); no console errors.\n- **2026-08-11** — **Splash Articles carousel: Collection card gets a real image + a \"collection\" marker.** The 2nd article card (the Collection variant → `article-collection`) now carries a generic image (`article-2.png`, joining the skin-care card's `article-1.png`) instead of the diagonal-hatch placeholder. It's flagged `collection: true`, which (a) runs its title on **two lines** (a longer generic title + a `min-height: 2.6em` reservation) and (b) adds a magenta **stack / \"layers\" icon** to the **right of the title** so the card reads as **part of a Collection**. Implemented by splitting `.mod-article-card__title` into a flex row (`__title-text` + `__stack`). Verified via computed geometry (icon right of a 2-line title, magenta 20×20, top-aligned); no console errors. (Screenshot pane was flaky on deep scroll, so verification was DOM/geometry-based.)\n- **2026-08-13** — **Community page reworked into a content preview — Visitor + Subscriber only.** The `community-overview` screen now renders **two different surfaces by persona**: Visitor and Subscriber get a new **content-preview page** (`renderCommunityPreview`, Figma Community `5:2` desktop / `6:68` mobile); Logged-Out and Logged-In Members keep the existing wireframe orientation menu (`renderCommunityHub`). The preview is a beige full-bleed intro banner (DM Serif \"Community\" title, subtitle \"Get a glimpse what's happening inside — then join to take part.\", avatar stack + \"12,345 women in the community\", dotted corner graphic) over **four feature preview cards** (`COMMUNITY_PREVIEW` → `.comm-card`: 80px magenta glyph icon, title, headline stat, one-line summary, chevron) and a **light join upsell card** (`.comm-upsell` — white/bordered, NOT the navy splash CTA). The **Posts** card is `memberOnly`: magenta \"· Member only\" tag + a **lock** (not an arrow) → gated behind joining. Cards link to the existing `com-*` pages (Posts→`com-activities`, Q&A→`com-questions`, Groups→`com-groups`, People→`com-meet`). Upsell is persona-aware, mirroring the splash home: Visitor = \"You don't have to figure this out alone.\" / **Join for free** → `signup-start`; Subscriber = \"Don't miss out! You're almost in.\" / **Finish up now** → `registration-step` (secondary \"Check symptoms first\" → `symptom-checker` for both). **Stat counts are the literal \"[TBD]\" placeholder** (\"[TBD] updates shared\", etc.) — the mockup's sample numbers (123/12/3/32) are dropped, keeping the no-fabricated-counts rule (2026-07-27) in force here too. (The one-line summary copy under each card stays as descriptive preview text, not a count.) **Glyphs are inlined**, not `<img>`: the six card icons + chevrons live in `COMMUNITY_SVGS` in `main.js` (rendered inline so they travel with the JS and paint over `file://`) — unlike `ICON_SVGS` they keep baked-in fills (magenta glyph / blue arrow), not `currentColor`. The larger **decorative graphics stay as `<img>`** (`community-banner-graphic.svg` ~23KB, `community-upsell-{tr,bl}.svg` ~87KB each — too heavy to inline): the banner graphic lives *inside* `.comm-preview__banner-inner` so it right-aligns to the 728px column (not the full-bleed margin), and the upsell corner graphics are offset to bleed off the card corners. Desktop upsell is an **even two-column grid** (copy left, buttons right, `1fr 1fr`) so the buttons are wide and \"Check symptoms first\" never wraps. Source `.svg` files are still in `assets/` (icons now only as the inline source-of-record); avatars reuse `community-{1,2,3}.png`. Verified in-browser (mobile + desktop, both Visitor and Subscriber; members still get the hub); no console errors.\n- **2026-08-13** — **Community: Logged-Out + Logged-In Member pages built — an activity feed (`renderCommunityFeed`).** `community-overview` now splits **three** ways (was two): Visitor/Subscriber → `renderCommunityPreview`; **Logged-Out Member → `renderCommunityFeed(true)`**; **Logged-In Member → `renderCommunityFeed(false)`** (Figma logged-out `9:134`/`9:200`, logged-in `34:609`/`34:536`). The feed reuses the preview's beige banner (title + avatar stack + count, no subtitle) over four sections from `COMMUNITY_FEED`: **Trending Posts** (3 post cards + a \"Post\" pencil button, vertical list → `com-activities`), **Open Questions** (3 cards, **carousel** → `com-questions`), **Groups You're In** (3 cards w/ 80px icon + membership ✓ + description, list → `com-groups`), **People Near You** (3 cards, **carousel** → `com-meet`); each ends in a \"View all … →\" link. The two carousels reuse the shared carousel plumbing — `.comm-feed__scroll` was added to `attachCarousels()` and the `[data-carousel]` arrow handler's selectors; arrows (`.comm-feed__arrows`) are desktop-only (mobile swipes), matching the splash. **Logged-Out is gated:** `.comm-feed--gated` blurs the card content 4px + makes it non-interactive, and a **login-reminder card** (\"Welcome back, Jannie123! / You're not logged in. / **Log in now**\") sits on top — the `data-action=\"log-in\"` no-op auth transition, mirroring the panel access card; its corner graphics reuse `community-upsell-{tr,bl}.svg`. Logged-In sees the same feed live, no reminder. The member nav chrome is the persona's own (member-photo avatar/dropdown), NOT the Figma frame's stray \"Join\" button (a copy artifact). **Counts are `[TBD]`** (\"[TBD] members / team members\", answer count \"[TBD]\") per the 2026-07-27 rule; post/question/group **text** stays as illustrative placeholder copy (usernames literally \"Username\") to convey the feed. New inlined glyphs in `COMMUNITY_SVGS`: `community-post-pencil` (blue), `community-comment` (gray), `community-check` (blue ✓); avatars reuse `community-{1,2,3}.png`. `renderCommunityHub`/`COMMUNITY_MODULES` are now unreached (kept as a defensive fallback only). Verified in-browser (logged-out gated/blurred + reminder; logged-in live; all four sections; carousels; mobile + desktop); no console errors.\n- **2026-08-13** — **Profile dropdown anchored to the profile button (no per-breakpoint CSS).** The account dropdown was pinned `top: 60px; right: 12px` from the viewport edge — correct on mobile (button near the edge) but far off on desktop, where the profile button sits at the centered 1000px content box's right edge (`.dnav__bar` padding = `--content-pad`). Rather than add a desktop media-query override, `positionDropdown()` (called at the end of `render()`) reads the `.profile-btn` rect and sets the dropdown's `top`/`right` inline so it always sits directly under the avatar in **both** layouts. The dropdown lives in a viewport-fixed overlay, so the button's viewport rect maps straight onto its absolute `top`/`right`; it runs synchronously before paint (no flash). The base CSS `top/right` stay as a fallback. (This matches the existing JS-positioning pattern in `attachAutoHide`.)\n- **2026-08-14** — **Global footer: more breathing room + a thin divider line.** Content sometimes sat too close to the footer (worst on desktop, where the space above the footer had been dropped to `0`). Bumped the space above the footer to **72px** (base) / **80px** (desktop) and added a **thin full-width divider line** above it (`border-top: 1px solid var(--color-border)`), matching the Figma footer (`6371:29`). Applies globally (the footer is appended to every screen). Verified both breakpoints in-browser (divider renders; 72/80px gap); no console errors.\n\n- **2026-08-14** — **Locked \"Posts\" card opens a member sign-up gate (not a navigation).** On the Visitor/Subscriber community preview, the `memberOnly` **Posts** card previously linked to `com-activities`. It now opens a **member-gate modal** (`renderMemberGate`, gated behind `data-action=\"member-gate\"` + `state.gateOpen`) instead — mirroring the live site's social-login gate: a DM-Serif title (\"This is a member-feature! / Sign up for free to view posts.\"), three outlined \"Continue with …\" pills (Facebook / Google / Email — brand glyphs `gate-facebook/google/email` inlined in `COMMUNITY_SVGS`), and an \"Already a member? Log In\" link. All CTAs resolve to `signup-start` (this prototype has no separate social-auth or login screens). The modal is a viewport-fixed scrim + centered card; dismissed via the top-left ✕ or a scrim tap (both `data-action=\"close-gate\"`, guarded by `e.target.dataset.action === \"close-gate\"`; the ✕'s svg is `pointer-events:none` so the button is always the target). `state.gateFeature` (+ a `GATE_FEATURES` copy map, and `data-gate` on the card) lets the same modal gate other features later. `state.gateOpen` resets on `hashchange` so navigating away closes it. Verified in-browser (desktop + mobile: opens from Posts, ✕/scrim close, options → sign-up); no console errors.\n- **2026-08-14** — **Member-gate modal polish.** Iterated on the gate from the entry above: (1) **\"Log In\" is now a no-op** (`data-action=\"log-in\"`, not `→ signup-start`) — there's no login screen in this prototype. (2) **Copy restructured into a hierarchy** — DM-Serif headline **\"This is for members only!\"** + a smaller **Lato-gray** subline **\"Join for free to view {feature}.\"** (`.gate__headline` / `.gate__sub`, gray = `--color-text-secondary`, mirroring the landing subheads), replacing the single all-serif title. (3) **Email envelope glyph tinted the action blue** (`#0F57A8`, was navy). (4) **Document scroll locked while the modal is open** — `render()` toggles `html.is-modal-open { overflow: hidden }` (the document is the app's scroller), so the page behind the overlay no longer scrolls; released on close. Verified in-browser (headline/subline hierarchy, blue envelope, Log In does nothing, real wheel scroll over the overlay leaves the page put, scroll restored on close); no console errors.\n\n- **2026-08-14** — **Placeholder pages: label sits high, footer stays below the fold.** The labelled placeholder screens (`.screen__body--fill`) used `min-height: 100%`, which never resolved against the flex parent's indefinite height — so the body only took the leftover space and the tall (381px) global footer filled the rest of the viewport, fully visible under the label. Fixed: `.screen__body--fill { min-height: 100vh }` (a definite height) pushes the footer entirely below the fold, and `.screen__body--fill:not(.screen__body--flow) .screen__placeholder { margin-block: 22vh auto }` anchors the label in the upper third instead of the dead-center of the tall body. Scoped `:not(--flow)` so the chromeless flow pages (Sign Up Start / Registration Step — no footer) keep their centerd label. Verified desktop + mobile (label ~31–36% from top, footer below the fold and still reachable by scrolling); no console errors.\n\n- **2026-08-14** — **Community upsell card: headline to 2 lines (desktop).** The closing \"You don't have to figure this out alone.\" card's even `1fr 1fr` grid gave the 36px headline a ~303px column, wrapping it to 3 lines. Widened the copy column (`grid-template-columns: 1fr 270px` → ~336px left) so it wraps to **2 lines**; the buttons keep a fixed 270px and still fit \"Check symptoms first\" (verified via measurement — 2 lines, no button overflow). Desktop-only (`@media min-width:1024px`); the mobile card is a single column, unaffected.\n\n- **2026-08-14** — **Visitor Community page replaced with a \"Community Features\" tour.** The Anonymous **Visitor** `community-overview` now renders `renderCommunityFeatures()` (Figma `184:588` desktop / `184:441` mobile) instead of the stats/upsell preview — so the branch splits **four** ways: Visitor → features tour; Subscriber → `renderCommunityPreview` (unchanged); Logged-Out/Logged-In → the feed. The new page is a \"Community Features\" heading over four `.feat-card`s from `COMMUNITY_FEATURES` (Posts / Meet Others / Q&A / Groups), each a magenta category eyebrow + DM-Serif title + description + a magenta line-art illustration bleeding off the top-right over a faint blue panel; cards link to the `com-*` pages. New assets (`<img>`): `community-feat-{posts,meet,qa,groups}.svg` + shared `community-feat-panel.svg`. No stats/lock/member-gate/upsell on the Visitor page now (those remain on the Subscriber preview, which still triggers the member-gate from its locked Posts card). Responsive: illustration top-right + full-width description on mobile; illustration centerd on the right of a 728px card on desktop. Verified in-browser (visitor desktop + mobile match Figma; subscriber/​members unchanged); no console errors.\n\n- **2026-08-14** — **DM-Serif headlines now use the landing's navy globally + Community Features refinements.** Several tweaks in one pass:\n  - **Global headline color:** every DM-Serif headline on a *light* background now uses `--color-navy` (#2b2b68, the purplish-blue the landing's section titles already use) instead of near-black `--color-text`/`#000`. Changed: `.comm-preview__title`, `.comm-upsell__title`, `.feat__heading`, `.feat-card__title`, `.comm-feed__title`, `.comm-reminder__title`, `.gate__headline`. Headlines on *dark* backgrounds (hero, CTA card, factoid, panel promo — all white-on-navy/gradient) are deliberately left white.\n  - **Subscriber joins the Visitor** on the Community Features tour (`renderCommunityFeatures`) — the branch now sends both there; `renderCommunityPreview` + the member-gate are no longer reached by any persona (kept as reference).\n  - **Feature pair added** (`COMMUNITY_PAIR`, Figma `212:110`): two richer cards below the four features — **Ambassadors & CEMs** (people preview → `com-meet`) and **Community Guidelines** (checklist → `com-values`) — a `.feat-pair` **row on desktop, stacked on mobile**. New `<img>` assets: `community-feat-{leadership,guidelines}.svg`, `community-feat-check.svg` (magenta), `community-feat-panel2.svg` (taller gradient panel).\n  - **Feature-card polish:** the blue panel is now **centerd vertically** on both breakpoints (was too high on mobile); the Q&A speech-bubble art had its **white fill removed** (`fill=\"white\"`→`none` in `community-feat-qa.svg`) so the panel shows through.\n  Verified in-browser (visitor + subscriber, desktop + mobile; pair is a row on desktop / stacked on mobile; headlines navy; bubble transparent; panel centerd); no console errors.\n- **2026-08-14** — **Community Features follow-ups.** (1) The six line-art illustrations are now **inline SVG** (`COMMUNITY_FEAT_SVGS`, keyed by `art` name), rendered in a `<span>` wrapper instead of `<img>` (the `.svg` files stay in `assets/` as the source). The blue panels stay `<img>`. (2) The pair's **Community Guidelines eyebrow shortened to \"Guidelines\"** — the full label wrapped to two lines in the narrow desktop column. (3) The faint **blue panels are now bottom-aligned** (bled slightly off the card's bottom-right) instead of vertically centerd / top-anchored. Verified desktop + mobile; no console errors.\n- **2026-08-14** — **Community Features: all remaining SVGs inlined; panels nudged right.** Followed up on the above by inlining the **panels and the checklist check** too, so *every* graphic on the page is inline SVG (`COMMUNITY_FEAT_SVGS`) except the person-avatar PNGs. The panels carry a `userSpaceOnUse` linear gradient with a fixed id, so a new `panelSvg(name, uid)` helper suffixes the gradient id (definition + `url(#…)` reference) uniquely per card — otherwise inlining the same panel on 4 feature cards / 2 pair cards would create duplicate ids (verified 6 unique gradient ids, no console errors). Also **nudged the panels a little further right** (feature `right: -24→-44px`, pair `-56→-76px`) while keeping the bottom alignment consistent across all cards. Inline panel svg is width-driven (`width:100%; height:auto`, height from viewBox). Verified desktop + mobile.\n\n---\n\n\n### 2026-09\n\n- **2026-09-18** — **Phase 0 of the repo restructure: one token layer.** Consolidated five competing token declarations (`main.css`, `design-system/README.md`, `design-system/MHT-Design-Tokens.html`, `line-icons/icon-sheet.html`, `assistant/ai-pulse-spec.html`, `community-standalone/styles.css`) into `system/tokens/tokens.css` plus one theme file per site hue. Kept the existing `--color-*` role names so no rule had to be rewritten; value-named tokens (`--color-magenta`, `--color-purple-light`, `--color-bg-purple-soft`) survive as deprecated aliases pointing at hue-neutral roles (`--color-accent`, `--color-accent-light`, `--color-bg-accent-soft`), since \"magenta\" is wrong the moment a site is blue. Pages now link `tokens.css` -> `themes/<site>.css` -> `main.css`; the theme is declared in the HTML rather than imported inside CSS so a page can be re-skinned on its own, which is the 80-site requirement.\n- **2026-09-18** — Brand model settled: **one universal palette, one hue family per site**, not two brands. Six `--brand-*` tokens (accent, accent-soft, accent-soft-solid, accent-strong, accent-light, accent-pale, scrim, bg-accent-soft) swap per site; action blue, ink, surfaces, borders, type and layout are universal. Adding a site is one file in `system/tokens/themes/` and nothing else — if it ever requires touching anything outside that folder, something has leaked out of the theme layer. `legacy.css` is placeholder: only `--brand-accent` (`#3f7ec1`, from `icon-sheet.html`) is sourced; the rest are derived using the same relationships Menopause uses.\n- **2026-09-18** — Consolidated 11 near-duplicate color declarations while tokenising. Two were one-hex-digit drifts that had gone unnoticed: `#a441bc` vs `--color-magenta` `#a440bc`, and `#dfe5f1` vs `--color-primary-soft` `#dee5f1`. The rest folded near-blacks (`#000`, `#151515`, `#1a1a1a`) into `--color-text` `#0d1b29` and a mid-gray (`#6b6b6b`) into `--color-text-secondary` `#626b74`. These are real, if slight, rendered changes — accepted deliberately as the cost of one ink scale.\n- **2026-09-18** — `--color-magenta-pale` (`#ecc7f5`) was promoted out of `community-standalone/` into the theme layer as `--brand-accent-pale`. It existed there with a documented rationale: the full accent sits at ~2.4:1 on a photo scrim and fails AA, so eyebrow text lifts to a pale tint on dark only. That is a system-level accessibility need, not fork drift. The Legacy equivalent is derived and **not** contrast-checked yet.\n- **2026-09-18** — `community-standalone/feat-cards.css` is deliberately a portable drop-in, so its `--tf-*` scope was kept rather than removed; the values became `var(--color-x, <literal>)` so the file still works pasted into a site with no design system, and consumes MHT tokens where they exist.\n- **2026-09-18** — Added `evals/lint-tokens.js`: fails on any hex literal outside the token layer. Exceptions are explicit and each carries a reason — `-webkit-mask` (luminance, not color), the portable-drop-in fallbacks, and `entry-points/`, which mocks Google/Facebook/Gmail chrome and must keep *their* brand colors. `design-system/`, `line-icons/` and `assistant/` are skipped pending the Phase 2 move, and that exclusion is scoped to be removed then.\n- **2026-09-18** — Known gap from Phase 0: brand-colored raster and SVG assets do not theme. Swapping to the Legacy hue recolors the UI but leaves the logo purple. Brand marks need `currentColor` (or per-site files) in Phase 3.\n\n- **2026-09-18** — **Phase 1: the launcher became a home shell.** `index.html` is now a collapsible left sidebar over seven sections (Prototypes, Foundations, Components, Icons & Graphics, Brand, Product, Decisions), rendering entirely from `home/manifest.js`. Adding a prototype or doc is one manifest entry, never a markup edit. Items carry a `status` of live/draft/planned, and `planned` renders as a dashed, non-clickable card — so the roadmap is visible without dead links. Everything opens in a new tab, preserving the shareable-URL property the old launcher had.\n- **2026-09-18** — The manifest is a `.js` assigning `window.HOME_MANIFEST`, not a `.json`. Chrome blocks `fetch()` of local files over `file://`, and this repo's invariant is that it works with no server. A `.json` would have broken that silently. Verified by loading the home from `file://` in a real browser: 7 sections, 13 cards, theme swatch resolved.\n- **2026-09-18** — The home shell carries a live Menopause/Legacy theme switcher. It is deliberately built from the same tokens as the product, so a broken token shows up on the front door first. The swatch colors are **not** hardcoded in JS — they are read back out of the loaded theme files with `getComputedStyle` at boot, so a hue change in `system/tokens/themes/` is picked up automatically and cannot drift. The theme list itself is manifest data.\n- **2026-09-18** — `evals/lint-tokens.js` gained a warning tier and now scans `.js` as well as `.css`/`.html`. Extending it immediately caught 42 brand colors baked into the inline logo SVGs in `main.js` — the concrete reason the logo stays purple when the Legacy hue is applied. Recoloring those needs per-path judgment (brand fill vs decoration vs a third-party logo), so they are reported as **warnings** that do not fail the run rather than hidden behind an exception. Third-party brand colors (Google, Facebook) are exempt outright: they are not ours to normalize. A lint that hides known debt stops being verification.\n- **2026-09-18** — Branch hygiene before the restructure: `feature/resources` was at the identical commit as `main` with nothing to merge, so it was deleted rather than merged. An audit found all ten remaining remote branches are likewise fully merged into `main` with zero unmerged commits. The previously untracked `design-system/`, `line-icons/` and `assistant/` folders were committed as-is first, so every later move is a reviewable diff against a known baseline instead of an unrecoverable edit to untracked files.\n\n- **2026-09-18** — Removed the theme switcher from the home. It was demonstrating a capability rather than serving the page: the home is a Menopause-themed page like any other, and there is no Legacy site template to switch to yet. The two sites are now **documented** instead — a Sites group under Foundations, and `system/tokens/themes/README.md` rewritten as the canonical per-site reference. The theme layer itself is unchanged and still works; product pages link one theme each. A \"Legacy site template\" entry sits in the manifest as `planned`, so the gap is visible rather than implied.\n\n- **2026-09-18** — **Phase 2: the folder move.** The repo is now `index.html` + `home/` (the front door), `system/` (the design system: tokens, foundation, patterns, icons, motion, brand, assets), `prototypes/` (navigation, entry-points, community), `product/` (surface knowledge, was `domains/`), `decisions/` and `evals/`. Every move used `git mv`, so history follows each file. Verified by checksum: 217 tracked files before and after, with an identical set of content hashes — the move changed locations only.\n- **2026-09-18** — The old `foundation/` folder mixed three unrelated things, so it was split rather than renamed: `design.md` is design language and went to `system/foundation/`; `navigation.md` is a pattern and went to `system/patterns/`; `system.md` described the `data-persona` render model, which is prototype architecture, not system foundation, and became `prototypes/navigation/ARCHITECTURE.md`. `domains/` kept its meaning and was renamed `product/`.\n- **2026-09-18** — `design-system/` was dissolved: its README became `system/tokens/tokens.md`, `MHT-Design-Tokens.html` became `system/tokens/reference.html`, and the brand documents moved to `system/brand/`. Icons and graphics are now in one place under `system/` (`icons/`, `assets/`), as are the motion studies (`motion/`).\n- **2026-09-18** — Asset paths survive on one seam. `main.js` resolves every file through a single `ASSET_BASE` constant, so the move was a one-line change from `\"../assets\"` to `\"../../../system/assets\"` — the pages now sit three levels below the root. Confirmed by loading the prototype and checking every `<img>`: 18 images, none broken, including the advisor headshots, over both http and `file://`.\n- **2026-09-18** — Historical paths inside this log were deliberately **not** rewritten during the move, per the append-only policy in the header: entries record what was true when written. Every other document was rewritten and checked — 119 markdown links across the repo resolve, and all 36 home-manifest links resolve. The link checker skips this file for that reason.\n- **2026-09-18** — Added `### YYYY-MM` sub-headings to this log. It had grown to 83KB across 148 lines at ~560 characters per line, which loads whole or not at all. Splitting by surface was rejected — the interleaved timeline is what lets a nav decision and a landing decision from the same week explain each other. Month headings give retrieval a handle without reordering or regrouping anything.\n\n- **2026-09-18** — **Fixed: the back-to-launcher hotspot was broken on every flow page.** `main.js` built the link in a JS template literal with `href=\"../index.html\"`, which after the Phase 2 move resolved to `prototypes/navigation/index.html` — a path that does not exist. Corrected to `../../../index.html` and pulled out into a `LAUNCHER_HREF` constant beside `ASSET_BASE`, so the two path seams sit together. The Phase 2 checks missed it because they grepped `href=\"` in HTML files and `ASSET_BASE` in JS; a URL inside a JS string was in neither set.\n- **2026-09-18** — Added `evals/lint-links.js` in response to that miss: it resolves every internal reference in HTML (`href`/`src`), CSS (`url()`) and JS string literals, and fails when one does not exist. Two resolution rules are explicit because paths do not always resolve against their own file — `launcher/manifest.js` hrefs are consumed by `index.html` at the root, and `main.js` paths are resolved by the page that loads it. Documentation examples that name deliberately non-existent files (`me.jpg`, `photos/stories.jpg`) are ignored by name with a reason. 57 references checked, all resolving.\n- **2026-09-18** — Renamed the front door from \"Home\" to the **launcher** throughout: `home/` → `launcher/`, `home.css`/`home.js` → `launcher.css`/`launcher.js`, `window.HOME_MANIFEST` → `window.LAUNCHER_MANIFEST`, the `home.*` localStorage keys → `launcher.*`, and `.home-hotspot` → `.launcher-hotspot`. The product has its own **Member Home** surface (the `home` screen id, the `go-home` action, the \"Home\" nav tab), and one word for two different things confuses people and agents alike. Those product usages were deliberately left untouched. \"Launcher\" was chosen over \"dashboard\" because the page is a directory of things to open, not a status surface.\n- **2026-09-18** — Caught while renaming: the three `entry-points/` pages carry their own hand-written copy of the hotspot markup rather than getting it from `main.js`. Their `href` was already correct, but renaming the CSS class in `main.css` would have silently unstyled them. Updated in step. The duplication itself is still there and is a candidate for the component work in Phase 3.\n\n- **2026-09-18** — Folded the Design OS handoff's **authority model** and **preference grammar** into `system/AUTHORITY.md`. The authority model replaces the \"code is the source of truth\" line that used to close `MANUAL.md`: that rule was convenient but wrong, because it meant any bug in `main.css` could silently become the specification. Code is now authoritative about what *behaves* this way today; contracts about intended semantics; decisions about why. The rest of the handoff (`docs/design-os/`) was deliberately **not** copied — roughly 70% of it describes what this repo already does, and its own README says to map onto what exists rather than add folders.\n- **2026-09-18** — **Phase 3: the component index is generated, not authored.** `evals/gen-component-index.js` derives `system/components/INDEX.md` from `main.css` — 79 blocks with their parts, variants, whether JS renders them, and whether a contract exists. `--check` fails when it is stale. The only human judgment is the tier classification (primitive / chrome / module / surface), which lives in the generator so it shows up in a diff. Extracting 79 blocks at once would have been a large risky change with no immediate payoff; making the monolith addressable was the cheaper first move.\n- **2026-09-18** — **Brand marks now theme.** The inlined logo SVGs in `main.js` carried baked `fill=\"#A440BC\"` and `fill=\"#2B2B68\"`, which is why the wordmark stayed purple when the Legacy hue was applied — the gap flagged in Phase 0. Those paths now carry no fill and are classed `.logo-accent` / `.logo-ink`, driven from `--color-accent` and `--color-navy` in `main.css`. The knockout `fill=\"white\"` was left literal on purpose: it is a hole punched through the mark, not a brand color. Verified in a browser under both themes.\n- **2026-09-18** — **The Ask AI pulse was purple on a blue site too, and the lint could not see it.** Its keyframes baked the accent as `rgba(164, 64, 188, 0.65)`, and `lint-tokens.js` only matched hex literals. Converted to `rgb(from var(--color-accent) r g b / 0.65)` (relative color syntax, verified supported in-browser; needs Chrome 119+ / Safari 16.4+), and extended the lint to audit `rgb()`/`rgba()`. Per the newly adopted graduation policy the rgba check starts **audit-only**: 36 remaining hits are mostly universal ink and white overlays, which are lower risk than brand hues.\n- **2026-09-18** — **The prototype had no keyboard focus styling at all** — zero `:focus` rules across 64 rendered `<button>` elements, while the launcher written in Phase 1 had proper focus styles throughout. Added a single `:focus-visible` rule (2px `--color-primary`, 2px offset) plus a pill-radius variant for the round controls. `:focus-visible` rather than `:focus`, so it never appears on a mouse click. This is the clearest argument for contracts: the blocker was found by the act of writing down what the Ask AI button's states were *supposed* to be, not by looking at the code.\n- **2026-09-18** — Removed the dead legacy launcher CSS from `main.css` (13 rules, `.launcher__*`), orphaned when Phase 1 moved the launcher to its own stylesheet. A first attempt at this deleted the entire Article component block as well, because the section-boundary regex matched the wrong comment; caught by diffing the removed selectors, restored, and redone with explicit line assertions. Regex deletion across a stylesheet has now caused this twice — exact-string or line-asserted edits only.\n\n- **2026-09-18** — Renamed the repo again, `mht-design-system` → **`mht-design-os`**, after the Design OS handoff made the case: the design system is *one subsystem*, under `system/`, while `prototypes/`, `product/`, `decisions/` and `evals/` are not design system at all — four of the seven top-level areas sit outside it. Naming **inside** `system/` still says \"design system\" on purpose (`tokens.css`, `reference.html`), because that part genuinely is one; only the repo-level identity changed — the launcher title and rail, the README and the manual. Renaming everything would have erased the distinction the new name exists to make. The README's h1 was also still `TIM — Navigation Prototype`, stale since before the restructure, and now describes the workspace rather than the one prototype.\n\n- **2026-09-18** — **The two icon sets are not rival versions of the same thing.** Of 112 files and 28 inlined icons, exactly **two names overlap** (`menu`, `messages`). One set is editorial — graphical, detailed, symptoms and anatomy and care, drawn on a 128 grid at stroke 3.4. The other is UI chrome — solid glyphs at 16-44px for nav, tabs and actions. Two further facts settled it: the line icons are unusable as chrome (stroke 3.4 on a 128 canvas renders 0.53px at 20px, a hairline), and an `<img src=\"icon.svg\">` cannot inherit CSS `color`, so tinting requires the SVG inline. Kept as **two named sets**, `system/icons/editorial/` and `system/icons/ui/`, deliberately not merged.\n- **2026-09-18** — Extracted the 28 UI glyphs out of `main.js` into files, so SVG files are now the single source of truth for both sets. `evals/gen-icon-registry.js` generates the inline registries the prototype and the sheet consume — the same generated-not-authored pattern as the component index. Split per set on purpose: the prototype loads only the 28 UI glyphs (53KB) and never the 112 editorial icons (160KB it would not use). `icon()` normalizes a camelCase request (`tabHome`) to the kebab filename key, so no call site had to change.\n- **2026-09-18** — All 112 editorial icons converted from `stroke=\"#000000\"` to `currentColor`, so they inherit color and re-skin with the site. The sheet's footer had documented a real reason for the literal black — Figma and Illustrator drop inherited paint on import — but that turned out to be already handled: `serialise()` bakes the current literal hex onto every shape before copying. So the design-tool path was never relying on the source files, and the conversion costs nothing. The footer note was rewritten to describe the actual arrangement rather than the old one.\n- **2026-09-18** — The icon sheet held a **third copy** of all 112 icons, hardcoded inline. It now renders every tile from the generated registries, so it is a specimen of the files rather than a parallel copy — 112 inline SVGs down to 2. Its category grouping and display names were extracted first into `system/icons/categories.js` as authored data; `gen-icon-registry.js --check` verifies every file appears in exactly one category, so a new icon cannot go uncategorised. The sheet also moved from **Archivo** to **Lato**, the brand body face, and its swatch was corrected from `#A441BC` to `#A440BC` — the same one-digit accent drift found in Phase 0, still living in a third place.\n\n- **2026-09-18** — **Phase 3 complete. Both color lints graduated from advisory to blocking, and the warning count went to zero.** 42 baked SVG colors and 36 `rgba()` literals are gone: brand paths are classed and driven from the token layer, every `rgba()` that was an exact token value became `rgb(from var(--token) r g b / a)`, and the handful that must stay literal are named in the lint with reasons — third-party marks (Facebook, Google), knockouts punched through filled shapes, and pure black/white at alpha, which are compositing primitives with no hue to track. A new baked color now fails the run instead of adding to a warning pile nobody reads.\n- **2026-09-18** — The one-digit accent drift turned up **four more times** — in two community preview icons and both AI gradient functions — after being fixed in `main.css` in Phase 0 and in the icon sheet in the icon work. Three separate rounds of fixing the same drift, in three different files, is the argument for the lint being blocking rather than advisory: each round only found the copies that happened to be in scope.\n- **2026-09-18** — The community feature illustrations were half-converted: 13 paths on `currentColor` and 18 with the accent baked in, inside the same 16-icon set. So the set only partly re-skinned, which is worse than not at all — a coherent wrong color is easier to spot than a half-right one. All now `currentColor` with the accent set once on the wrapper.\n- **2026-09-18** — Wrote four more component contracts (`icon-btn`, `uplevel`, and the module button pair, which share one). Each surfaced something the code did not say: the module buttons are **dark-surface only**, which their class names do not convey, and the secondary's border measures **2.90:1** against the darkest gradient anchor — under the 3:1 non-text minimum. `.uplevel`'s dock animation ignored `prefers-reduced-motion`; `main.css` had four reduced-motion blocks but every one was narrowly scoped to the assistant or the notification badge. Fixed.\n- **2026-09-18** — Left unresolved on purpose: `.uplevel span` sets `font-weight: 400` with the comment `/* Lato Semibold */`. Semibold is 600, weight 600 is loaded, and the only other \"SemiBold\" comment in the stylesheet correctly pairs with 600. Per `system/AUTHORITY.md` the code is authoritative about what renders today and the comment claims an intent; which one is right cannot be determined from the repo. Recorded in the contract and surfaced rather than guessed — changing it would alter rendered output on a hunch.\n- **2026-09-18** — The community photo-card scrim moved from a fixed plum to `rgb(from var(--color-scrim) …)` so the card themes. Contrast was measured **before** changing it, not after: white type over the heavy end is 16.4:1 on the old literal, 11.8:1 on the Menopause scrim and 11.6:1 on Legacy — all far above the 4.5:1 minimum, so tying it to the hue does not weaken the legibility the gradient exists to provide.\n- **2026-09-18** — The `.launcher-hotspot` markup is now one shared script (`prototypes/entry-points/back-to-launcher.js`) instead of three hand-copies. The duplication had already caused a real miss: renaming the class during the Home→launcher rename silently unstyled all three mocks, because the copies live in HTML that `main.js` never touches.\n\n- **2026-09-18** — **Phase 4: `system/parity.json` + `evals/lint-parity.js`.** Entries were read from Figma rather than mined from the node ids scattered through the docs, which changed three of them. `.uplevel` maps to `7294:1952`, a frame literally named \"Uplevel\" — 34px tall in both, with a 16×16 Icon child and a Label child matching the contract's anatomy exactly. `.footer` maps to `6371:78`, not the `6371:29` the docs cite: `6371:29` is the outer `Mobile_Footer` wrapper, while `6371:78` is the footer itself and its `Bar` / `End` children map exactly onto `.footer__bar` / `.footer__end`.\n- **2026-09-18** — Found while verifying: the two panel persona cards, `7417:3673` and `7417:3712`, are **both named \"Access\"** in Figma. Only the button label tells them apart — \"Log in now\" for logged-out, \"Share now\" for logged-in. The docs' mapping is correct, but anyone reading the Figma file alone cannot tell which persona a frame belongs to. Recorded on both parity entries; renaming one in Figma would remove the risk.\n- **2026-09-18** — **Code Connect considered and not adopted.** It would give richer mapping, but it needs a Node toolchain plus `figma connect publish` in CI, which ends the no-build-step property that lets every page open straight from `file://`. `parity.json` gets detectable drift without that and is the natural input if Code Connect is adopted later. Recorded so the option is a decision rather than an oversight.\n- **2026-09-18** — Parity coverage is **3 of 81 blocks and deliberately partial**. An entry is added only when the node has actually been read from Figma; a guessed mapping is worse than none because it looks authoritative. `.ai-btn`, `.icon-btn` and the two module buttons have contracts but no entry: the accessible page of the Global Navigation file exposes the nav controls only inside frame `6371:107`, which is `hidden=true` — a reference copy, not the canonical nav. The lint reports these four every run rather than letting them be quietly filled in.\n- **2026-09-18** — A possible discrepancy recorded rather than resolved: that hidden nav copy draws the AI control at 36×36 with a 22×22 stars vector, while `main.css` builds `.ai-btn` at 44px with `.ai-btn__stars` at 22×22 — the glyph matches, the control does not. 44px is the accessibility minimum for a touch target and the contract gives that as its rationale, so this may be a deliberate uplift rather than drift. Both readings are plausible from the repo alone, so it needs the canonical frame.\n\n- **2026-09-18** — **Phase 5: four skills, not six.** `drift-check`, `new-component-contract`, `add-icon` and `new-prototype` are in `.claude/skills/`. `design review` and `accessibility review` were deliberately skipped: they are mostly judgment rather than procedure, and what a skill could usefully say already lives closer to the work — accessibility expectations are in each component contract, and `system/AUTHORITY.md` covers how to weigh sources that disagree. A skill that only said \"check contrast and keyboard access\" would be a maintenance cost pretending to be guidance. The bar applied was a real, repeating, repo-specific sequence with commands in it, where getting the order wrong causes a concrete failure.\n- **2026-09-18** — `.gitignore` changed from ignoring all of `.claude/` to `.claude/*` plus `!.claude/skills/`. Local permission state stays ignored; the skills are tracked, because a procedure that only exists on one machine is not a procedure. This was a blocker found at the start of Phase 5 — skills would have been written into a gitignored directory and silently never shared.\n- **2026-09-18** — `evals/lint-links.js` now walks into `.claude/skills/` (while still skipping the rest of `.claude/`). The skills reference repo paths heavily and are exactly the kind of link that rots after a move. Verified the check actually works by deliberately pointing a skill at a non-existent file and confirming it failed — a checker that silently passes everything is worse than none. Link count went 213 → 226.\n\n- **2026-09-18** — Three decisions taken from outside the repo, closing questions that had been recorded rather than guessed. `.uplevel`'s label weight is **600** — the comment carried the intent and the value had drifted from it. `.ai-btn` at **44px is correct** and deliberate, the accessibility minimum for a touch target; the 36×36 in the Figma reference copy is not current, and the frames will be refreshed rather than the code pulled back. The two ambiguous Figma frames were **renamed** to \"Access — Logged out Member\" and \"Access — Logged in Member\".\n- **2026-09-18** — Correcting an earlier overstatement: the two \"Access\" frames were described as a drift risk for anyone reading the Figma file. Walking the ancestor chain before renaming showed the enclosing SECTIONs (\"Logged out Member\" / \"Logged in Member\") always distinguished them in the layers tree. The frame names were ambiguous only in isolation — in a search result or a bare node-id list. The rename is still worth having; the risk was smaller than recorded.\n- **2026-09-18** — **The `.uplevel` contract described a component that does not render.** `renderUplevel()` is defined in `main.js` and never called; `.screen__uplevel` is only referenced by a `querySelector` that always returns null. The desktop breadcrumb was dropped on 2026-08-04 with a note to re-add it \"when the up-nav treatment is decided\", and the pill went with it. The contract now leads with a dormant-status banner. The font-weight fix is correct but has no visible effect today.\n- **2026-09-18** — **Phase 6: `evals/lint-dead-code.js` and `evals/check-all.js`.** The dead-code check exists precisely because nothing caught the dormant uplevel: the component index's \"JS\" column only asks whether a string appears in `main.js`, which cannot tell a definition from a call. It reports unrendered CSS blocks (`.mod-card`, `.mod-cards`) and uncalled functions (`renderUplevel`, `renderCommunityPreview`) as **advisory**, because parked code is a legitimate state — the point is that it should be a known state, not a surprise found while writing documentation. `renderCommunityPreview()` looks like a genuine regression: a comment says \"Subscriber keeps renderCommunityPreview\" while nothing calls it.\n- **2026-09-18** — `evals/check-all.js` runs all six checks under one exit code, blocking vs advisory declared per check. Its first version silently dropped every advisory finding by capturing only stdout while the NOTEs are written to stderr — caught by reading the output rather than trusting it. Both runners were then verified by deliberately breaking something and confirming they fail.\n\n- **2026-09-18** — **Cleanup: `renderCommunityPreview()` was not a regression after all.** The render branch already gave Visitor and Subscriber the same Community Features treatment, so the behavior was already what was wanted — only the comment claiming \"Subscriber keeps renderCommunityPreview\" was stale. Deleted the function and its `COMMUNITY_PREVIEW` data (96 lines), which orphaned `.comm-card` and `.comm-upsell`; those went too, with the already-dead `.mod-card` / `.mod-cards`. 99 lines of CSS, 81 blocks down to 77.\n- **2026-09-18** — Finished the Phase 0 token migration: the deprecated value-named aliases (`--color-magenta`, `--color-magenta-soft`, `--color-magenta-soft-solid`, `--color-magenta-pale`, `--color-purple-light`, `--color-bg-purple-soft`) are deleted, with all **40** call sites migrated to the `--color-accent*` roles. They had been marked \"removed once the component layer lands (Phase 3)\" since Phase 0.\n- **2026-09-18** — `.uplevel` is **kept and parked, not deleted** — wanted for pages that fall under the main sections. `lint-dead-code.js` gained an explicit `PARKED_FUNCTIONS` list so it stops reporting it, with the reason attached. Parking something requires a contract that says so, otherwise the list becomes a way to silence the check rather than answer it.\n- **2026-09-18** — **The token reference page carried an entire parallel vocabulary** — `--action-900`, `--neutral-700`, `--reaction-900: #A441BC` (the one-digit accent drift, yet again, in a fourth place) — with values that had fallen behind the real layer. Rebuilt: it now renders the actual tokens from a generated manifest and resolves **live** values with `getComputedStyle`, so switching the theme shows what the browser actually computes. 54 tokens across 11 groups, no second copy that can drift, and `gen-token-manifest.js --check` keeps it honest.\n- **2026-09-18** — `MHT-Style-Guide-v1.pdf` removed as outdated; the Figma file is the live source. It stays in git history, so the 6.9MB is not recovered from the repo — only from the working tree. `lint-links.js` caught the dangling `MANUAL.md` reference immediately.\n- **2026-09-18** — Leaner: component contracts flattened from `<block>/docs.md` to `<block>.md` — five directories that each held exactly one file. Maximum path depth is now **3**, and every remaining depth-3 directory earns it (`system/icons/{ui,editorial}`, `system/tokens/themes`, the four persona folders, the four skills). The four persona folders are the one remaining candidate: flattening `prototypes/navigation/visitor/index.html` to `visitor.html` would save a level but change a shareable URL, so it was not done unilaterally.\n- **2026-09-18** — `.icon-btn`'s composition into `.ai-btn` is now explicit in the stylesheet at both blocks, not just implied by `class=\"icon-btn ai-btn\"` in the markup. Editing the base reaches the pill, and the comments say so.\n\n- **2026-09-18** — **Launcher pages for components and graphics.** Both sections had been linking raw files — `INDEX.md` rendering as markdown source, and the assets folder as a browser directory listing, which does not work from `file://` and says nothing about what an asset is for. `system/components/index.html` renders live specimens from the **real** `prototypes/navigation/main.css`, so the page is the components rather than a picture of them; the two module buttons sit on a dark gradient stage because they are dark-surface only, a constraint a white stage would have hidden. `system/assets/index.html` shows 53 assets in 9 groups over transparency/light/dark backdrops. Both read generated manifests, so neither can drift from what it documents.\n- **2026-09-18** — `evals/gen-asset-manifest.js` reports any asset matching no grouping rule rather than filing it under \"Other\". It caught `ai-stars.svg` on the first run, which now has a rule. A catch-all bucket is how an inventory stops being an inventory.\n- **2026-09-18** — **History rewritten to purge `MHT Style Guide v1.pdf`.** It was committed on 2026-09-18 to preserve then-untracked work, removed from the working tree later the same day as outdated, and then purged from history entirely because it should never have been committed. `git filter-repo` removed both of its historical paths (`design-system/MHT Style Guide v1.pdf` before the Phase 2 move, `system/brand/MHT-Style-Guide-v1.pdf` after). **`.git` went from 23MB to 2.1MB.** The working tree is provably unchanged: `main`'s tree hash is identical before and after (`cf82ef29`), and all 152 commits are preserved. Verified by a fresh clone — the blob is absent and all seven checks pass.\n- **2026-09-18** — Two things worth knowing about that rewrite. Every commit SHA changed, so the merge commits referenced by PRs #17-#23 no longer exist; the PRs remain readable but their commit links are dead. And a backup tag made beforehand was **itself rewritten** by `filter-repo`, so it was not a backup at all — the remote was the only surviving copy of the old history until the force-push. If this is done again, the backup has to live outside the repo.\n\n- **2026-09-18** — **The four remaining launcher sections needed one page, not four.** Foundations, Brand, Product and Decisions all linked raw `.md` files, which a browser shows as plain text — one problem wearing four hats. `system/docs/index.html` renders all 31 markdown documents with a sidebar, and 20 launcher links now open a rendered document instead of source. Building four bespoke pages would have been four things to maintain for one underlying gap.\n- **2026-09-18** — Two details in the docs viewer worth keeping. Inline code is extracted behind a sentinel **before** any other inline pass, so markup inside backticks (`--color-*`, `<div>`, `**bold**`) is never re-parsed as markup. And markdown links are relative to each document's own folder rather than to the viewer, so they are rewritten on render — another inlined `.md` opens in the viewer, anything else resolves against the repo root. `MANUAL.md`'s 21 links come out as 18 internal and 3 external, all resolving.\n- **2026-09-18** — Content is generated into `content.js` rather than fetched, for the same reason as the icon registry and the launcher manifest: Chrome blocks `fetch()` of local files and this repo must work with no server. One file rather than 31 — a single 314KB payload on an internal tool beats 31 scripts plus dynamic script injection.\n- **2026-09-18** — Three lint adjustments, each recorded in the script rather than silently applied. `lint-tokens` skips `system/docs` because the generated mirror legitimately quotes colors — including historical drift values inside append-only `DECISIONS` entries, which are records and must not be \"corrected\". `lint-links` skips `content.js` because its paths are prose and its real links are relative to each original document. And `lint-links` now ignores `$1`/`$2` regex placeholders, which it had been reading as references out of the renderer's own replacement strings — a false positive it found in its own new neighbour.\n\n- **2026-09-21** — **The launcher was cut from seven sections to four** — Prototypes, Color, Type, Icons. The test applied: does a section answer \"what can I open right now?\" If it answers \"where do I read about X?\", it is a document and belongs in the docs viewer. Foundations was superseded (color and type are now first-class inline views, which is what people came to Foundations for); Components, Brand, Product and Decisions are reading material. Nothing was deleted — `decisions/LAUNCHER-ARCHIVE.md` records every removed entry, its description and where the file still lives, because the curation is work that a file listing cannot reconstruct.\n- **2026-09-21** — **Color, type and icons now render inline in the launcher.** Nothing opens a new tab, including prototypes, which navigate in the same tab — their hidden top-left hotspot is the way back, so a new tab was never needed. The icon browser reads the same generated registries the standalone sheet does, and the 160KB editorial registry is injected on first visit to the Icons section rather than loaded up front.\n- **2026-09-21** — **The launcher chrome is brand-agnostic.** It was built from the Menopause accent — colored rollovers, an accent bar on the selected item, accent eyebrows — which made a tool serving two sites look like one of them. The chrome is now neutrals only; the brand hue appears where it is the subject, in a swatch or a specimen. The rollover is a neutral wash, the selected state is weight and surface with no accent bar, and the collapse control is a sidebar glyph rather than a chevron, which said \"back\" rather than \"panel\".\n- **2026-09-21** — **Dark mode added to the token layer**, opt-in by `<html data-mode=\"dark\">` with deliberately **no** `prefers-color-scheme` block. Two reasons: duplicating every value into a media query means two copies that drift, and more importantly `tokens.css` is loaded by the prototypes, which are light-only product surfaces — an OS-level query would turn them dark without anyone asking. Pages that should follow the OS set the attribute themselves; prototypes never do.\n- **2026-09-21** — Dark mode measured rather than eyeballed. On `#16181c` the full brand accent is **3.42:1** (fails) and `--color-primary` is **2.50:1** (fails badly). The accent steps up to `--brand-accent-light`, which already existed in every theme described as \"emphasis on dark backgrounds\" — exactly this job — reaching 6.73:1. Primary is replaced outright with `#6aa6e8` at 6.97:1.\n- **2026-09-21** — The standalone icon sheet had **no `<!DOCTYPE>`, `<html>`, `<head>` or charset** — it began at `<title>` and relied on the browser guessing the encoding. An em-dash in a new comment exposed it as mojibake. Given a proper document shell. Its light/dark attribute was also renamed `data-theme` → `data-mode`, since `data-theme` means the site hue family everywhere else in the repo. The page is kept for the one thing the launcher does not do: COPY bakes a literal hex onto every shape, because design tools drop inherited paint on import.\n\n- **2026-09-21** — US spelling throughout: 158 replacements across 36 files (`colour`→`color`, plus `behaviour`, `centre`, `grey`, `normalise`, `organise`, `favour`). Checked first that none appeared in a code identifier — all were prose and comments. `decisions/LAUNCHER-ARCHIVE.md` was missed on the first pass because it was still untracked and the script drew its file list from `git ls-files`.\n- **2026-09-21** — Two bugs found while verifying the mode switch, both worth recording because the first looked like a CSS problem and was not. **Re-rendering synchronously inside the click handler read stale values**: `getComputedStyle` still returned the pre-change mode because the style recalculation had not run yet. Deferred to the next frame. Then **`data-mode` collided with itself** — `<html>` carries it, so `querySelector('[data-mode=\"dark\"]')` matched the document element rather than the button. The buttons now use `data-set-mode`. The collision only became visible once the attribute was actually being set on `<html>`, which is why it survived the first round of testing.\n\n- **2026-09-21** — **The 28 UI glyphs were normalised onto a 24x24 canvas with a 20-unit live area.** They had **eleven different viewBoxes**, inherited from whatever size each was exported at, and measured in a browser the artwork filled anywhere from **47% to 100%** of its own canvas. Rendered at one size in the launcher grid, the drawn glyph ranged **19px to 40px inside an identical 40px box** — better than a 2x spread. After normalising, the spread is **zero**.\n- **2026-09-21** — Why it had never shown: the prototype sized every icon at its own call site to match its own canvas — 16px for the `up-*` set, 44px for the `hub-*` markers, 22px for the nav glyphs. That works, and it is why nobody noticed, but it means the set had **no shared sizing contract** — the thing a design system is supposed to provide. The launcher grid exposed it the moment it asked them all to behave the same.\n- **2026-09-21** — Bounding boxes were measured with `getBBox()` in a real browser and recorded in `evals/ui-icon-bboxes.json`, because parsing curve geometry in Node would be guesswork. `evals/normalize-ui-icons.js` applies the transform: scale so the longest side is 20 units, centre on 24, wrap in a `<g transform>`. `my-health` is the only glyph with a stroke, so its `stroke-width` is pre-divided by the scale factor to land on a consistent 1.5 weight.\n- **2026-09-21** — Normalising the files changed what every consumer rendered, so all nine call sites in `main.css` were compensated by 1.2x to keep the shipped appearance identical — a declared box of N now renders N x 20/24 of ink. Verified per site: hub icons land at 21.7px against 22px before. The boxes look like odd numbers (19, 26, 29) because they are compensating for the live area; rationalising them to round values is a deliberate visual change rather than a refactor, and was not done here.\n- **2026-09-21** — Caught while verifying: `.ai-btn__stars` does **not** style a registry icon. It styles the gradient duo-star built inline by `aiDuoStarsGradientSVG()`, which is a full-bleed 22 canvas and was never part of the normalisation — so the 1.2x made it 18% larger. Reverted to 22 with a comment saying why. `.art-answered__stars` and `.gate__option-icon` are the same kind of thing and were left alone.\n\n## On the horizon\n\n- ~~Pull real icon/logo/placeholder-profile assets from Figma into `/assets/`~~ — done 2026-07-24 (see decisions log + [navigation.md](foundation/navigation.md) → Assets).\n- Design the **Logged In Member** \"Home as a hub\" (currently a placeholder) and any **Subscriber**-specific top nav / landing (Subscriber mirrors the Visitor experience and the shared panel, differing only in the persona-aware splash closing card → Registration Step).\n- ~~Fold Topic Center into Collections~~ — done 2026-08-04 (old `topic` retired → it's now a `collection`; Article Show levels up to a Topic Hub or links to a Collection).\n- **Settle the Topic Hub taxonomy.** The panel now shows **3 per-topic hubs** (HRT, Mood, Sleep — via `makeTopicHub`) + **Explore All Articles**, all sharing generic placeholder hub content. Remaining: confirm the real topic set, and give each hub its own real copy/modules. (The `collection` screen and the collection article are also still wireframe placeholders with generic copy — real sponsored-collection content is future work.)\n- Decide whether CTA buttons should trigger a real auth flow stub vs. the current persona-swap convenience.\n- Eventual port to Rails ERB partials once nav/panel/dropdown are locked, per the tim-component-showcase workflow.\n"},{"path":"decisions/LAUNCHER-ARCHIVE.md","title":"Launcher archive","section":"Decisions","bytes":8600,"markdown":"# Launcher archive\n\nOn 2026-09-21 the launcher was cut back to four sections — Prototypes, Color,\nType, Icons — so it surfaces the basics rather than everything. This records\nwhat was removed and where it still lives, because the curation (the grouping,\nthe descriptions, the deliberate `planned` placeholders) is work in its own\nright and is not recoverable from a file listing.\n\n**Nothing was deleted.** Every file below is still in the repo and still\nreachable through the docs viewer at `system/docs/index.html`, which indexes\nall 31 markdown documents. The standalone pages — the component gallery, the\ngraphics gallery, the token reference — are also still there and still pass\ntheir checks.\n\nTo restore a section, copy its block back into `launcher/manifest.js`. Sections\nwith `kind` omitted default to a card grid.\n\n---\n\n## Foundations\n\n_The universal layer every site and prototype shares — color, type, space and motion — plus the hue family that distinguishes each site._\n\n### Conventions\n\n> How to read the rest of the system: who is authoritative about what, and the grammar used to document usage.\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Authority & preference | `system/docs/index.html?doc=system/AUTHORITY.md` | Which source wins when artifacts disagree, and the Preferred / Allowed / Avoid / Prohibited grammar. |\n\n### Sites\n\n> One universal palette, one hue family per site. Components are identical everywhere — only the hue differs. Adding a site is one file in system/tokens/themes/ and nothing else.\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Site & theme guide | `system/docs/index.html?doc=system/tokens/themes/README.md` | What each site is, what belongs in a theme, and how to add site 81. |\n| ThisIsMenopause | `system/tokens/themes/menopause.css` | Purple hue family. Production values, Figma-confirmed. The theme every prototype here is built against. |\n| Legacy condition sites | `system/tokens/themes/legacy.css` | Blue hue family for the 80+ existing condition sites, migrating onto Menopause styling. Placeholder values — only the accent is sourced. |\n| Legacy site template | _not built_ | A prototype shell running the Legacy hue end to end. Not built yet. |\n\n### Tokens\n\n> One source of truth. Every page links tokens.css, then a theme, then its own styles.\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Token reference | `system/tokens/reference.html` | Every token, rendered from tokens.css itself with live computed values and a theme switcher. |\n| tokens.css | `system/tokens/tokens.css` | The universal palette. Source of truth; names no hue. |\n| Design language | `system/docs/index.html?doc=system/foundation/design.md` | Color tokens, type scale, tinting, chrome asset provenance. |\n\n### Motion\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Ask AI pulse | `system/motion/ai-pulse-spec.html` | Motion study for the assistant pill — timing, easing, scope. |\n\n### Patterns\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Global navigation | `system/docs/index.html?doc=system/patterns/navigation.md` | Top nav, slide-out panel, level-up pill, footer. |\n\n---\n\n## Components\n\n_The monolith made addressable. A generated index of every CSS block, plus hand-written contracts for the ones that earn one._\n\n### Index\n\n> Generated from main.css by evals/gen-component-index.js, so it cannot drift from the code.\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Component gallery | `system/components/index.html` | Live specimens rendered from the real main.css, plus every block by tier. Theme switcher included. |\n| Component index (markdown) | `system/components/INDEX.md` | The same inventory as a file, for reading in an editor or a diff. |\n| How components work here | `system/docs/index.html?doc=system/components/README.md` | What the tiers mean, how to add a contract, and the known gaps. |\n| Contract template | `system/docs/index.html?doc=system/components/_TEMPLATE.md` | The schema for a component contract, using the preference grammar. |\n\n### Contracts\n\n> Written by hand. Authoritative about intent, not about CSS values.\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Ask AI button | `system/docs/index.html?doc=system/components/ai-btn.md` | The assistant entry point — states, pulse motion, reduced-motion and accessibility. |\n| More contracts | _not built_ | Buttons, the level-up pill and icon-btn are the next candidates. |\n\n### Figma parity\n\n> Coverage is partial on purpose — an entry is added only when the node has been read from Figma. Guessed mappings look authoritative and are worse than none.\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| parity.json | `system/parity.json` | Figma node to CSS block to contract, with what each mapping was verified against. |\n| Code Connect | _not built_ | Considered and not adopted — it needs a Node toolchain and CI, which ends the no-build-step property. |\n\n---\n\n## Brand\n\n_Who the brand is and how it sounds. Strategy, voice, messaging and copy guardrails._\n\n### Source of truth\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Brand source of truth | `system/docs/index.html?doc=system/brand/brand-source-of-truth.md` | Positioning, values, voice pillars, tone spectrum, messaging architecture. |\n| Site design language | `system/docs/index.html?doc=system/brand/site-design-language.md` | thisismenopause.com — color roles, components, layout rules. |\n| MHT Style Guide (Figma) | _not built_ | The live style guide. The v1 PDF was removed as outdated — link to be added. |\n\n---\n\n## Product\n\n_What each surface is and how it behaves — the product knowledge behind the prototypes._\n\n### Surfaces\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Landing | `system/docs/index.html?doc=product/landing.md` | Splash Landing — six content modules and its deep-links. |\n| Library / Resources | `system/docs/index.html?doc=product/library.md` | Topic pages, Topic Center, Article Show. |\n| Community | `system/docs/index.html?doc=product/community.md` | Posts, Q&A, Groups, Meet-Others and their detail screens. |\n| Topic Hub | `system/docs/index.html?doc=product/topic-hub.md` | Per-concern hub aggregating Q&A, conversations, groups, resources. |\n| Advisors | `system/docs/index.html?doc=product/advisors.md` | Medical Advisory Committee page. |\n| Account | `system/docs/index.html?doc=product/account.md` | Profile dropdown and its destination screens. |\n| Onboarding | `system/docs/index.html?doc=product/onboarding.md` | Sign Up Start and Registration Step. |\n\n### Architecture\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| System & persona model | `system/docs/index.html?doc=prototypes/navigation/ARCHITECTURE.md` | The data-persona render model, screen types, responsive layout. |\n\n---\n\n## Decisions\n\n_Why things are the way they are. Append-only: older entries record what was true at the time and are never rewritten._\n\n### Logs\n\n| Item | Where it lives | What it was |\n|---|---|---|\n| Docs viewer | `system/docs/index.html` | Every markdown document in the repo, rendered and cross-linked, with a sidebar. |\n| Decisions log | `system/docs/index.html?doc=decisions/DECISIONS.md` | Every design and build decision, interleaved in time order. |\n| Restructure plan | `system/docs/index.html?doc=decisions/RESTRUCTURE-PLAN.md` | The six-phase plan this home is part of, with findings and status. |\n| Manual | `system/docs/index.html?doc=MANUAL.md` | Index of the spec docs. |\n\n---\n\n## Why these four went\n\nThe launcher had grown to seven sections and 51 entries. Most of it was\nreference material that is read occasionally and does not belong on a front\ndoor — the front door should answer \"what can I open right now?\".\n\n- **Foundations** — superseded. Color and type are now first-class sections\n  rendered inline, which is what people actually came to Foundations for. The\n  remaining entries (authority model, theme contract, design language,\n  navigation pattern) are documents, and documents belong in the docs viewer.\n- **Components** — the gallery and contracts still exist at\n  `system/components/`. It is a maturing area with five contracts out of 77\n  blocks; it earns a launcher section when there is more to show than to\n  explain.\n- **Brand**, **Product**, **Decisions** — all reading material. The docs\n  viewer renders them properly, with cross-links.\n\nThe test applied: does this section answer \"what can I open right now?\" If it\nanswers \"where do I read about X?\", it is a document, and the docs viewer is\nthe right home.\n"},{"path":"decisions/RESTRUCTURE-PLAN.md","title":"TIM Repo Restructure — Plan","section":"Decisions","bytes":17703,"markdown":"# TIM Repo Restructure — Plan\n\nStatus: **All six phases complete.** Cleanup pass next.\n\nGoal: turn this repo into a design system that is *executable context* for\nagents — a single source of truth for tokens, an addressable component layer,\na launcher that hosts many prototypes, and mechanical verification that\ncatches drift. Structure follows **System → Context → Behavior → Verification**.\n\n---\n\n## Findings that shaped this plan\n\nEvidence gathered from the current repo, not assumptions.\n\n### 1. Five competing token declarations, three naming schemes\n\n| Source | Scheme | Font |\n|---|---|---|\n| `main.css` `:root` (47 vars) | `--color-primary`, `--color-text` | Lato |\n| `design-system/README.md` | hand-copied table of the above | Lato |\n| `design-system/MHT-Design-Tokens.html` | `--action-900`, `--neutral-700` | Lato |\n| `line-icons/icon-sheet.html` | `--menopause`, `--brand`, `--ink` | **Archivo** |\n| `assistant/ai-pulse-spec.html` | `--blue`, `--magenta`, `--ink` | Lato |\n| `community-standalone/styles.css` | fork of `main.css` | Lato |\n\nEvery page added recently reinvented the palette. This is the highest-value\nproblem in the repo and no folder move fixes it.\n\n### 2. Two token values have already silently drifted\n\n| Literal in `main.css` | Existing token | Delta |\n|---|---|---|\n| `#a441bc` | `--color-magenta` `#a440bc` | one hex digit |\n| `#dfe5f1` | `--color-primary-soft` `#dee5f1` | one hex digit |\n| `#0f57a8` | `--color-primary` | exact duplicate |\n| `#edf3f9` | `--color-bg-blue-soft` | exact duplicate |\n\n### 3. Hex literals outside `:root` — smaller than feared\n\n111 total occurrences, but **57 are `#fff`** and only ~20 values are distinct.\nFull inventory in Phase 0 below.\n\n### 4. There is no component layer\n\n`main.js` is 284KB with **76 inline SVGs**; `main.css` is 99KB. Nothing is\naddressable, so `components/` cannot be created by `mkdir` — it requires\nextraction, or at minimum an index that points into the monolith.\n\n### 5. Two unrelated icon systems\n\n`line-icons/icons/` holds **112 SVG files referenced by zero prototype files**.\nThe prototype inlines a separate, unrelated set in `main.js`. The icon sheet\nalso uses Archivo while the product uses Lato.\n\n### 6. Code Connect conflicts with \"no build step\"\n\nNo `package.json`, no `figma.config.json`, no `*.figma.ts`. Real Code Connect\nneeds a Node toolchain plus `figma connect publish` in CI, and a stable code\nidentity per component. The docs already cite Figma node ids in prose\n(`4101:3`, `zV2gbuNONeoyeRVcFUFWeZ`) — formalize that into `parity.json` data\nfirst; adopt Code Connect only once components exist and a toolchain is\naccepted.\n\n### 7. `foundation/` currently mixes three different concerns\n\n- `design.md` — design language → belongs in the **design system**\n- `navigation.md` — nav chrome spec → a **pattern**\n- `system.md` — the `data-persona` render model → **prototype architecture**\n\nSplitting these three is worth more than any renaming.\n\n### 8. What NOT to change\n\n`domains/` holds product surface knowledge (landing, community, account,\nonboarding). This is the \"product context\" layer and is harder to rebuild than\na generic `patterns/` folder. Keep it (rename to `product/` at most). Do not\nreshape the repo merely to mirror Coinbase's architecture.\n\n---\n\n## Brand model\n\nNot two brands — **one universal palette with a hue family per site**, scaling\nto the 80+ existing condition sites. Components are identical everywhere;\nonly the hue differs (Menopause purple, Legacy blue). Legacy is not a separate\nproduct but the existing sites migrating onto Menopause styling.\n\nThis rules out a `data-brand` switch with a block per site — at 80 sites that\nbecomes the bottleneck. Instead a single `--brand-*` set lives in one file per\nsite, and `tokens.css` never names a hue. Adding a site is one file.\n\n## Decisions taken\n\n1. **Token naming: keep the `main.css` scheme** (`--color-primary`,\n   `--color-text`, `--color-magenta`). Zero churn in the working prototype;\n   `design-system/README.md` already documents it. `MHT-Design-Tokens.html`\n   gets rewritten to match rather than the reverse.\n2. **The existing prototype moves** into `prototypes/navigation/`. Accepts a\n   wide mechanical path rewrite in exchange for a clean system/prototype\n   boundary and room for prototype #2+.\n3. **All hex literals get migrated** in Phase 0 — every value becomes a token\n   or a documented one-off exception. A lint you cannot switch on is not\n   verification.\n4. **Brand scope is accent-only.** The six `--brand-*` hue tokens swap per\n   site. Action blue, ink, surfaces, borders, type and layout are universal.\n5. **Repo renames to `mht-design-os`**, brand-neutral so it does not favor\n   Menopause over the Legacy sites. It landed as `mht-design-system` first and\n   was renamed again after the Design OS handoff made the case: the design\n   system is one subsystem under `system/`, while `prototypes/`, `product/`,\n   `decisions/` and `evals/` are not design system at all. The local directory\n   rename goes **last** (it invalidates any running session's working\n   directory); the GitHub rename is yours to do.\n\n   Naming inside `system/` deliberately still says \"design system\" — that part\n   genuinely is one.\n6. **DECISIONS.md is not split by surface.** Its interleaved timeline is a\n   deliberate design that explains cross-surface decisions; splitting destroys\n   that. It gets `### YYYY-MM` sub-headings for retrieval instead, and moves\n   to `decisions/` in Phase 2 with a README stating the append-only policy.\n\n---\n\n## Target structure\n\n```\nindex.html                  Home shell (collapsible left sidebar)\nlauncher/\n  manifest.js               adding a prototype = one entry, not markup surgery\n  launcher.css  launcher.js\n\nsystem/                     the design system: live pages + docs\n  tokens/\n    tokens.css              SINGLE source of truth — imported by everything\n    tokens.md               intent, roles, naming rules\n  foundation/\n    color.md  type.md  space.md  motion.md\n    brand.md  tone-of-voice.md\n  patterns/\n    navigation.md           (from foundation/navigation.md)\n  components/\n    <name>/demo.html        live specimen + variants\n    <name>/docs.md          intent, usage, a11y, do/don't\n  icons/\n    icons/*.svg  sheet.html  icons.json\n  assets/                   logos, graphics\n  motion/                   ai-pulse-spec.html + future studies\n  parity.json               Figma node id <-> component <-> class <-> doc\n\nprototypes/\n  navigation/               current 4-persona app + main.css/js\n    ARCHITECTURE.md         (from foundation/system.md)\n  entry-points/\n  community/                (from community-standalone/)\n\nproduct/                    (was domains/) product surface knowledge\ndecisions/                  DECISIONS.md\n.claude/skills/             design, design-to-code, design-review,\n                            a11y-review, new-component, drift-check\nevals/                      lint scripts\n```\n\n---\n\n## Phases\n\n### Phase 0 — Tokens (no folder moves) — **DONE**\n\nDelivered:\n\n- `system/tokens/tokens.css` — the universal palette. 55 tokens; no hue named.\n- `system/tokens/themes/menopause.css` — purple family, production values.\n- `system/tokens/themes/legacy.css` — blue family, placeholder values.\n- `system/tokens/themes/README.md` — the theme contract and both load models.\n- `evals/lint-tokens.js` — drift lint, passing.\n- All 8 product pages + both standalone pages repointed at the token layer.\n\nVerified: every one of the original 38 token names preserved; brace balance\n616→613 (exactly the three removed blocks); resolved-value diff shows **11\nchanged declarations, all intended consolidations**; theme switch confirmed in\na browser on live product UI.\n\nNot done in Phase 0, deliberately: `design-system/`, `line-icons/` and\n`assistant/` still hold their own palettes. Those are documentation pages that\nget rebuilt when they move under `system/` in Phase 2; the lint skips them and\nthat exclusion is scoped to be removed then.\n\nOriginal steps, for reference:\n\n1. Create `system/tokens/tokens.css` from the current `main.css` `:root` block\n   (47 vars, names unchanged), including the `@media (min-width:1024px)`\n   desktop overrides.\n2. Resolve the four drift/duplicate values:\n   `#a441bc`→`--color-magenta`, `#dfe5f1`→`--color-primary-soft`,\n   `#0f57a8`→`--color-primary`, `#edf3f9`→`--color-bg-blue-soft`.\n3. Add tokens for the recurring unnamed values:\n   `#fff` (57 uses), `#e9ebee` (9), `#f3f4f6` (4), `#000` (4), `#e5e5e5` (3),\n   and the 2-use pairs `#fbf7f3` `#f6f7f9` `#eef1f7` `#eef0f3` `#6b6b6b`\n   `#1a1a1a`. Name by role, never by value — the existing rule in\n   `foundation/design.md`.\n4. Document the true one-offs (`#e3e6ea` `#d6e2f1` `#6b3794` `#151515`) as\n   exceptions with a reason comment, or fold them into a token.\n5. Point `main.css`, `community-standalone/styles.css`,\n   `assistant/ai-pulse-spec.html`, `line-icons/icon-sheet.html` and\n   `design-system/MHT-Design-Tokens.html` at `tokens.css`; delete their local\n   `:root` blocks.\n6. Resolve the **Archivo vs Lato** conflict in the icon sheet.\n\nExit criterion: exactly one `:root` color/type declaration exists in the repo.\n\n### Phase 1 — Launcher shell — **DONE**\n\nDelivered:\n\n- `index.html` — collapsible sidebar over seven sections; state persisted.\n  Named the **launcher**, never \"Home\" — the product has its own Member Home.\n- `launcher/manifest.js` — all home content as data. Adding a prototype is one entry.\n- `launcher/launcher.css`, `launcher/launcher.js` — shell styles and rendering, no framework.\n- A Sites group documenting both hue families, with the missing Legacy site\n  template listed as `planned` rather than left implied.\n\n37 items catalogued across Prototypes, Foundations, Components, Icons &\nGraphics, Brand, Product and Decisions. `planned` items render as dashed,\nnon-clickable cards so the roadmap shows without dead links.\n\nVerified in a browser: section routing and `#hash` deep links, collapse,\nmobile reflow, and a `file://` load with no server (7 sections, 13 cards).\n\nA theme switcher was built and then removed — it demonstrated the token layer\nbut did not serve the page, and there is no Legacy site template to switch to\nyet. The two sites are documented instead.\n\nThe manifest is `.js` not `.json` because Chrome blocks `fetch()` over\n`file://` — a `.json` would have broken the no-server invariant silently.\n\n### Phase 2 — The move — **DONE**\n\nThe target structure above is now the actual structure, with two refinements\nfound in execution: `design-system/` was dissolved into `system/tokens/` and\n`system/brand/` rather than kept, and the old `foundation/` was split three\nways (design language to `system/foundation/`, the nav pattern to\n`system/patterns/`, the render model to `prototypes/navigation/ARCHITECTURE.md`).\n\nEvery move used `git mv`, so history follows each file.\n\nVerified:\n\n- **217 tracked files before and after, identical content-hash set** — the move\n  changed locations only.\n- 119 markdown links resolve; all 36 home-manifest links resolve.\n- Prototypes render over http and `file://` with 0 broken images, including\n  advisor headshots. Entry-point click-through into a persona flow works.\n- `.figma/make/dev.json` priority paths and exclusions rewritten.\n- `evals/lint-tokens.js` rescoped from directory names to exact relative paths\n  and still passes.\n\nNot done at the time: the local directory and GitHub repo rename (now `mht-design-os`).\nThat is deliberately last — renaming the working directory invalidates any\nrunning session, and the GitHub rename is the user's to perform.\n\n### Phase 3 — Components — **DONE**\n\nDone:\n\n- `system/AUTHORITY.md` — the authority model and preference grammar, folded in\n  from the Design OS handoff. Replaces the old \"code always wins\" line.\n- `system/components/INDEX.md` — all 79 CSS blocks by tier, **generated** from\n  the stylesheet by `evals/gen-component-index.js` so it cannot drift.\n  `--check` fails when it is stale.\n- `system/components/_TEMPLATE.md` + `README.md` — the contract schema and how\n  to use it.\n- `system/components/ai-btn/docs.md` — the first real contract.\n- **Brand marks now theme.** The inlined logos carried baked `fill=\"#A440BC\"`,\n  which is why the wordmark stayed purple under the Legacy hue. The paths are\n  now classed `.logo-accent` / `.logo-ink` and driven from the tokens.\n  Verified in a browser: the wordmark goes blue with the theme.\n- **Ask AI pulse now themes.** Its keyframes baked the accent as `rgba(...)`,\n  which the hex-only lint never saw. Converted to\n  `rgb(from var(--color-accent) …)`; `lint-tokens` now audits `rgba()` too, as\n  an advisory tier.\n- **Keyboard focus, repo-wide.** Writing the first contract surfaced that the\n  prototype had *no* focus styling at all — zero `:focus` rules across 64\n  rendered buttons. Added one `:focus-visible` rule plus a pill-radius variant.\n  This was a blocker, found by the act of documenting.\n- Removed the dead legacy launcher CSS (13 rules) left behind by Phase 1.\n\nAlso done:\n\n- **Five contracts**: `ai-btn`, `icon-btn`, `uplevel`, and the module button\n  pair. Each surfaced a real defect — see `system/components/README.md`.\n- **The two icon systems** were never rival versions: editorial (112,\n  display-sized line art) and UI glyphs (28, interface chrome) share 2 names\n  out of 140. Files are the source of truth for both, with generated\n  registries; the sheet renders from them instead of holding a third copy.\n- **Both lints graduated from advisory to blocking.** 42 baked SVG colors and\n  36 `rgba()` literals went to **zero**: brand colors are classed and\n  token-driven, exact token values became `rgb(from var(--token) …)`, and the\n  handful that must stay literal — third-party marks, knockouts, pure\n  black/white at alpha — are named with reasons.\n- **The `.launcher-hotspot` markup** is one shared script instead of three\n  hand-copies.\n\n### Phase 4 — Figma parity — **DONE**\n\n`system/parity.json` ties a Figma node to the CSS block that implements it, and\n`evals/lint-parity.js` validates the structure, checks every mapped block still\nexists, and reports coverage.\n\nEntries were **read from Figma, not mined from prose**. That immediately paid:\n\n- `.uplevel` → `7294:1952`, literally named \"Uplevel\", confirmed 34px in both\n  with a 16×16 Icon and a Label child matching the contract's anatomy.\n- `.footer` → `6371:78`, whose `Bar` and `End` children map exactly onto\n  `.footer__bar` / `.footer__end`. The docs had cited the outer `6371:29`;\n  `6371:78` is the more precise anchor.\n- The two panel persona cards are **both named \"Access\"** in Figma and differ\n  only by button label — a real drift risk for anyone reading the file without\n  the docs.\n\n**Code Connect was considered and not adopted.** It needs a Node toolchain plus\na publish step in CI, which ends the no-build-step property that lets every page\nopen from `file://`. `parity.json` gets detectable drift without that and is the\ninput if Code Connect is adopted later.\n\nCoverage is 3 of 81 blocks and deliberately partial — an entry is added only\nwhen the node has been read. The four contracted components without one are\nreported every run, with what each is waiting on recorded in `openQuestions`.\n\n### Phase 5 — Skills — **DONE**\n\nFour skills in `.claude/skills/`, each referencing the authoritative files\nrather than copying them: `drift-check`, `new-component-contract`, `add-icon`,\n`new-prototype`.\n\nTwo of the six candidates were **not** written. `design review` and\n`accessibility review` are mostly judgment rather than procedure, and what a\nskill could usefully say already lives closer to the work — accessibility\nexpectations are in each component contract, and AUTHORITY.md covers weighing\nsources. A skill saying \"check contrast and keyboard access\" would be a\nmaintenance cost pretending to be guidance.\n\nThe bar applied: a real, repeating, repo-specific sequence with commands in it,\nwhere getting the order wrong causes a concrete failure.\n\nTwo supporting changes:\n\n- `.gitignore` now tracks `.claude/skills/` while still ignoring\n  `settings.local.json`. A procedure that exists on one machine is not a\n  procedure.\n- `evals/lint-links.js` now checks inside the skills. They reference repo paths\n  heavily and are exactly the kind of link that rots after a move — verified by\n  deliberately breaking one and confirming the check fails.\n\n### Phase 6 — Drift lint — **DONE**\n\nSix checks, one runner:\n\n```bash\nnode evals/check-all.js\n```\n\n`evals/lint-dead-code.js` is the new one, and it exists because of a real miss:\n`system/components/uplevel/docs.md` was written as though the level-up pill\nshipped. It does not — `renderUplevel()` is defined and never called, and the\npill was parked on 2026-08-04 pending an up-nav decision. Nothing caught it,\nbecause the component index's \"JS\" column only asks whether the string appears\nin `main.js`, which cannot tell a definition from a call.\n\nIt reports two things, advisory rather than blocking, because parked code is a\nlegitimate state:\n\n- CSS blocks nothing renders — currently `.mod-card`, `.mod-cards`\n- functions defined and never called — `renderUplevel()`,\n  `renderCommunityPreview()`\n\n`renderCommunityPreview()` is the more interesting of the two: a comment at\n`main.js:784` says *\"Subscriber keeps renderCommunityPreview\"*, so this reads\nlike a regression rather than a deliberate parking.\n\nBoth runners were tested by deliberately breaking something and confirming they\nfail — a checker that silently passes everything is worse than none.\n"},{"path":"product/account.md","title":"TIM Account — spec","section":"Product","bytes":3796,"markdown":"# TIM Account — spec\n\nThe Account surface: the **profile dropdown** menu and the destination screens it\nopens (My Health / Messages / Notifications / Settings) plus the member's own\n**My Profile**. The dropdown is triggered from the top-nav profile avatar — that\ntrigger is nav chrome, spec'd in [navigation.md](../system/patterns/navigation.md) §1 (Top Nav) /\nInteractions. **Log out** lives here structurally but is an auth transition — see\n[onboarding.md](onboarding.md). Chronological history is in [decisions/DECISIONS.md](../decisions/DECISIONS.md). Code\nis the source of truth — correct this doc if it drifts.\n\nAll destination screens here are **labelled placeholders** — real UI is not built.\n\n---\n\n## Profile dropdown\n\nAnchored card, top-right under the profile avatar, 246px wide, white, `#dbdddf`\nborder, `0 0 8px rgba(13,27,41,0.1)` shadow, 24px padding. The user block uses\n28px line rows for unhurried spacing at the top (matches Figma).\n\n**Structure:** `User block → Notifications chip → divider → My Health · Messages · Settings → divider → Log out`\n\n- **User block:** name (\"Janet Smithsonian\"), handle (\"@jannie1234\"), \"View Profile\" link (`#0f57a8`) → navigates to the member's own **My Profile** (`my-profile`, via `data-action=\"go-profile\"`) — a page with **no** level-up pill, distinct from \"Someone's Member Profile\" reached via Meet Others (see [community.md](community.md)).\n- **Notifications (5)** → `acct-notifications` — sits **above the first divider** as a distinct orange **chip**: faint orange fill (`--color-badge-soft`), orange bell + label (`--color-badge`, `#ff741d`), `16px` radius, `12px` padding — matching the orange notification badge. Its bell **wiggles periodically** (`bell-wiggle`, disabled under `prefers-reduced-motion`).\n- Top divider sits **below** the Notifications chip, separating it from the rest of the menu.\n- Then: My Health → `acct-health`, Messages → `acct-messages`, Settings → `acct-settings`.\n- Bottom divider (sits **above** Log out), then: **Log out** → currently a **no-op** (`data-action=\"log-out\"` with no handler — auth transition not wired; see [onboarding.md](onboarding.md)). Its icon is rotated 90° so the arrow points **right** (matches Figma).\n\nThe `acct-*` rows carry a `data-screen` and navigate to a placeholder destination\nscreen within the persona (in-page state change; the dropdown closes on click).\n**Log out** is the exception (see above).\n\n**Persona availability:** only defined for **Logged In Member** in the source file\n(node 7042:711). Logged Out Member has a profile-style icon in the nav but no\ncorresponding dropdown content in Figma — not built. Subscriber uses the Visitor\nnav (Join button, no profile icon), so it has no dropdown either.\n\n---\n\n## Destination screens\n\n| Screen | id | Reached from |\n|---|---|---|\n| My Health | `acct-health` | dropdown |\n| Messages | `acct-messages` | dropdown |\n| Notifications | `acct-notifications` | dropdown |\n| Settings | `acct-settings` | dropdown |\n| My Profile | `my-profile` | dropdown \"View Profile\" (`go-profile`) |\n\nAll `acct-*` rows are top-level `page`s with **no** level-up pill (the nav logo\nreturns home), as is `my-profile`. **Log out** is not a screen — it's an auth\nno-op (see [onboarding.md](onboarding.md)).\n\n---\n\n## Assets — dropdown icons\n\nFrom the Logged-In Member dropdown `7042:711 → 7042:776`. Monochrome SVGs, tinted\nink via CSS (see [design.md](../system/foundation/design.md) → icon-tinting system):\n\n| File | Label | Source node |\n|---|---|---|\n| `menu-myhealth.svg` | My Health | 7042:785 |\n| `menu-messages.svg` | Messages | 7042:789 |\n| `menu-notifications.svg` | Notifications | 7042:793 |\n| `menu-settings.svg` | Settings | 7042:797 |\n| `menu-logout.svg` | Log out | 7042:806 |\n"},{"path":"product/advisors.md","title":"TIM Advisors — spec","section":"Product","bytes":3030,"markdown":"# TIM Advisors — spec\n\nThe **Medical Advisory Committee** page — the intro, the five advisor cards, and a\n\"Watch Now\" clip. Reached two ways: the landing's Experts \"View all advisors →\"\n(see [landing.md](landing.md)) and the **global footer's \"Medical Advisors\"** link\n(see [navigation.md](../system/patterns/navigation.md) §5) — the latter makes it reachable\nfrom every persona. This doc owns the page itself. Built from the separate\nFigma file **Medical Advisors** (`Zkiv6o4d7eyQOLAvwjVYTy`, mobile `2:43`).\nChronological history is in [decisions/DECISIONS.md](../decisions/DECISIONS.md). Code\n(`../main.js`, `../main.css`) is the source of truth — correct this doc if it drifts.\n\n---\n\n## Screen & rendering\n\n- `advisors` screen: top-level `type: \"page\"` (no level-up pill). In the splash\n  personas (Visitor, Subscriber) via `SPLASH_FLOW_SCREENS`, and injected into the\n  member personas too (`SHARED_TARGET_SCREENS`) so the footer link resolves for all.\n- `render()` branches on `screen.id === \"advisors\"` → `renderAdvisors()`; the page\n  flows at natural height (excluded from `screen__body--fill`).\n- Content data is the `ADVISORS` array in `main.js` (real names/roles/affiliations/\n  bios from the frame).\n\n## Sections\n\n- **Intro** — DM Serif title (28px, navy `#2b2b68`), a bold lede, a body paragraph,\n  and an italic closing line, then a hairline divider (`#e5e5e5`).\n- **Committee** — the five advisors, each a card: 96px round **photo**, name\n  (20px semibold ink), role (16px magenta), affiliation (14px gray), bio (16px),\n  and a non-navigating **\"Read full bio →\"** placeholder (no bio-detail page\n  exists). Cards separated by hairline dividers.\n- **Watch now** — a DM Serif \"Watch Now\" headline (24px, navy `#2b2b68`,\n  title-case), a **video placeholder** (218px, rounded, the neutral-gray\n  diagonal-hatch pattern used elsewhere — a real thumbnail asset is pending), and\n  the clip title in gray (16px).\n\n## The five advisors (card order)\n\n| # | Name | Role | Affiliation |\n|---|---|---|---|\n| 1 | Christy James Guepet, M.D., FACOG, FPMRS | OB-GYN | Southern Women’s Specialists |\n| 2 | Cindi Rauert Lanners, PT, DPT | Physical Therapist | University of Colorado School of Physical Therapy |\n| 3 | Angela McCool-Pearson, M.D. | OB-GYN | Southern Women’s Specialists |\n| 4 | Chevon Rariy, M.D. | Endocrinologist | Visana Health |\n| 5 | Lauren Tetenbaum, LCSW, JD, PMH-C, MSCP | Licensed Clinical Social Worker | Menopause Society Certified Practitioner |\n\n## Assets\n\n**Real advisor headshots** exported from Figma into `assets/advisor-{1..5}.jpg`\n(index = card order above), shown as 96px circles (`object-fit: cover`). The\nmapping was verified against the committee render so no photo is paired with the\nwrong person. The Watch card has no asset yet (placeholder pattern in the\nmeantime).\n\n## Pending / placeholders\n\n- **\"Read full bio →\"** — non-navigating (no per-advisor bio page).\n- **Watch card** — diagonal-hatch placeholder until a real video thumbnail asset\n  is added.\n"},{"path":"product/community.md","title":"TIM Community — spec","section":"Product","bytes":14426,"markdown":"# TIM Community — spec\n\nThe Community surface: the **Community Overview hub** and the feature pages it\nleads to (Activity / Q&A / Groups / People / Community Values). The hub is the\n**primary navigation surface** into community features — reached from the shared\npanel's single **Community** tab (all personas) and the Splash Landing's \"Join\nthe conversation\" CTA. The panel *container* itself is nav chrome, specified in\n[navigation.md](../system/patterns/navigation.md) §3. Chronological history is in\n[decisions/DECISIONS.md](../decisions/DECISIONS.md). Code is the source of truth — correct this doc if\nit drifts.\n\nAll screens here are **labelled placeholders** (the Figma source models the\nnav/panel states, not full page layouts) — real list/detail UI is not built.\n\n---\n\n## Panel → Community\n\nThe slide-out panel no longer carries a 5-item Community list. As of the\n2026-08-03 flat-panel redesign it has a single **Community** row (tab) that points\nat the hub:\n\n| Panel row | Screen id |\n|---|---|\n| Community | `community-overview` |\n\nThe hub is then the sole path into the community feature pages below.\n\n---\n\n## Detail screens\n\nDistinct *detail* screens, deliberately separate from the `com-*` list pages.\nEach carries a **level-up pill** stepping one level up to its parent list (the pill\nmechanism and the Figma node id for each screen are spec'd in\n[navigation.md](../system/patterns/navigation.md) §2):\n\n| Screen | id | Levels up to |\n|---|---|---|\n| Group Detail | `group` | Groups (`com-groups`) |\n| Question Show | `question` | Questions & Answers (`com-questions`) |\n| Activity Show | `activity` | Activity (`com-activities`) |\n| Someone's Member Profile | `profile` | Meet Others (`com-meet`) |\n\n(These are the *Logged In Member* detail screens. \"Someone's Member Profile\" is\nanother member's profile, reached via Meet Others — distinct from the viewer's\nown **My Profile**, which is an Account screen; see [account.md](account.md).)\n\n---\n\n## Community Overview (the hub)\n\n`community-overview` (label \"Community Overview\") — a top-level `page` that renders\n**one of four surfaces, split by persona** (all branch off `screen.id ===\n\"community-overview\"` in `render()`):\n\n| Persona | Surface | Render fn |\n|---|---|---|\n| **Visitor, Subscriber** | **Community Features** tour — Figma `184:588` / `184:441` | `renderCommunityFeatures()` |\n| **Logged-Out Member** | Activity **feed**, gated (blurred + login reminder) — Figma `9:134` / `9:200` | `renderCommunityFeed(true)` |\n| **Logged-In Member** | Activity **feed**, live — Figma `34:609` / `34:536` | `renderCommunityFeed(false)` |\n\n`renderCommunityHub()` (old wireframe menu) and `renderCommunityPreview()` (the\nstats/upsell preview + member-gate) are no longer reached by any persona — kept as\nfallbacks / reference only.\n\n### Visitor + Subscriber — the Community Features tour (`renderCommunityFeatures`)\n\nThe Anonymous Visitor and the Subscriber get a **\"Community Features\"** page (added\n2026-08-14, Figma `184:588` desktop / `184:441` mobile) — a feature tour, not the\nstats/upsell preview. Data-driven from `COMMUNITY_FEATURES` + `COMMUNITY_PAIR` in\n`main.js`: a `Community Features` heading, then four feature cards (`.feat-card`),\nthen a two-card **feature pair** (`.feat-pair`). Each feature card has a magenta\ncategory eyebrow, a DM-Serif title, a description, and a magenta line-art\nillustration bleeding off the top-right over a faint blue panel:\n\n| Card | Category | Title | → screen |\n|---|---|---|---|\n| Posts | POSTS | What people are talking about | `com-activities` |\n| Meet Others | MEET OTHERS | Discover women like you | `com-meet` |\n| Q&A | Q&A | Ask, answer, and be heard | `com-questions` |\n| Groups | GROUPS | Find the circle that fits your journey | `com-groups` |\n\n**Feature pair** (`COMMUNITY_PAIR`, Figma `212:110`) — two richer cards below the\nfour, **side-by-side on desktop** (`.feat-pair` row, equal width/height) and\n**stacked on mobile**:\n\n- **Ambassadors & CEMs** — \"The women who provide support\" + a preview of two people\n  (avatar + name + role) → **View people →** (`com-meet`).\n- **Community Guidelines** (eyebrow: **\"Guidelines\"** — the full \"Community\n  Guidelines\" wrapped in the narrow desktop column) — \"How we look after each other\"\n  + a four-item checklist (magenta check + rule) → **View guidelines →** (`com-values`).\n\nNo stats, lock, member-gate, or upsell. **Every graphic on this page is inline SVG**\n(`COMMUNITY_FEAT_SVGS` in `main.js`, keyed by name) — the line-art illustrations, the\nchecklist check, and the faint blue panels; only the person avatars stay `<img>` (PNGs).\nThe panels carry a `userSpaceOnUse` linear gradient, so `panelSvg(name, uid)` suffixes\nthe gradient id per card to avoid duplicate ids when the same panel is inlined on\nseveral cards. Panels are **bottom-aligned and bled off the card's bottom-right**\n(`community-feat-panel.svg` on the feature cards, the taller gradient\n`community-feat-panel2.svg` on the pair). Responsive: illustration top-right + full-width\ndescription on mobile; illustration centerd on the right of a 728px card on desktop\n(feature cards); the pair is a **row on desktop, stacked on mobile**. All DM-Serif\ntitles use the landing's `--color-navy` (see [DECISIONS](../decisions/DECISIONS.md) 2026-08-14).\n\nReached from:\n\n- The shared panel's single **Community** tab (all four personas — see [navigation.md](../system/patterns/navigation.md) §3)\n- Splash Community section **\"Join the conversation →\"** (see [landing.md](landing.md))\n\n### (Superseded) Preview page + member-gate (`renderCommunityPreview`)\n\n> **Superseded (2026-08-14):** the Subscriber now gets the Community Features tour\n> above, so this preview — and the member-gate modal it triggered from the locked\n> Posts card — is **no longer reached by any persona**. Kept as reference (and the\n> `.gate` modal styles/handlers remain in place should a gate be wired up again).\n\nA \"get a glimpse what's happening inside\" marketing preview (added 2026-08-13,\nFigma Community `5:2` desktop / `6:68` mobile), not the orientation menu. Data-driven\n(`COMMUNITY_PREVIEW` + `renderCommunityPreview` in `main.js`). Structure:\n\n- **Beige intro banner** (`.comm-preview__banner`, full-bleed `#fbf7f3`): DM Serif\n  \"Community\" title, subtitle \"Get a glimpse what's happening inside — then join to\n  take part.\", avatar stack (`community-{1,2,3}.png`) + \"12,345 women in the\n  community\", and a dotted corner graphic.\n- **Four feature preview cards** (`.comm-card`) — 80px magenta glyph icon, title,\n  headline stat, one-line summary, chevron:\n\n  | Card | Stat | Chevron | Tap → |\n  |---|---|---|---|\n  | Posts | [TBD] updates shared **· Member only** | lock | **member-gate modal** |\n  | Questions & Answers | [TBD] questions answered | arrow | `com-questions` |\n  | Groups | [TBD] groups active | arrow | `com-groups` |\n  | People | [TBD] people joined | arrow | `com-meet` |\n\n  **Posts is `memberOnly`** — magenta \"· Member only\" tag + a **lock** (not an arrow).\n  Instead of navigating, it opens the **member sign-up gate** (`renderMemberGate`,\n  `data-action=\"member-gate\"`): a modal with a DM-Serif headline **\"This is for\n  members only!\"** and a smaller Lato-gray subline **\"Join for free to view posts.\"**\n  (`state.gateFeature` fills the feature word — `GATE_FEATURES` map), three \"Continue\n  with …\" options (Facebook / Google / Email — all → `signup-start`), and an \"Already\n  a member? **Log In**\" link that is a **no-op** (`data-action=\"log-in\"` — no login\n  screen exists in this prototype). Dismissed via the top-left ✕ or the scrim (both\n  `data-action=\"close-gate\"`). While open, the document scroll is **locked**\n  (`html.is-modal-open { overflow: hidden }`, toggled in `render()`), so the page\n  behind doesn't scroll. Brand glyphs (`gate-facebook/google/email`) are inlined in\n  `COMMUNITY_SVGS`; the email envelope is tinted the action blue (`#0F57A8`).\n- **Light join upsell** (`.comm-upsell` — white/bordered, *not* the navy splash CTA):\n  persona-aware, mirroring the splash home's closing card. Visitor → \"You don't have\n  to figure this out alone.\" / **Join for free** (`signup-start`); Subscriber → \"Don't\n  miss out! You're almost in.\" / **Finish up now** (`registration-step`). Both share\n  the secondary **Check symptoms first** (`symptom-checker`) and the disclaimer note.\n\n**Counts:** each stat's number is the literal **`[TBD]`** placeholder (\"[TBD] updates\nshared\", etc.) — never a fabricated number, same rule as the member hub and the splash\ncommunity count ([DECISIONS](../decisions/DECISIONS.md) 2026-07-27). The mockup's sample numbers\n(123/12/3/32) are dropped. The one-line summary under each card stays as descriptive\npreview copy (not a count).\n\nAssets — the card **glyphs are inlined** in `main.js` as `COMMUNITY_SVGS`\n(`community-{posts,questions,groups,people}` + `community-{lock,arrow}`; baked-in\nfills, not `currentColor`-tinted like `ICON_SVGS`). The larger **decorative graphics\nstay `<img>`** (too heavy to inline): `community-banner-graphic.svg` (inside\n`.comm-preview__banner-inner`, right-aligned to the 728px column) and\n`community-upsell-{tr,bl}.svg` (bleed off the upsell corners). Source `.svg` files\nremain in `assets/`.\n\n### Members — the activity feed (`renderCommunityFeed`)\n\nThe Logged-Out and Logged-In members get a real **activity feed** (added 2026-08-13,\nFigma logged-out `9:134`/`9:200`, logged-in `34:609`/`34:536`), data-driven from\n`COMMUNITY_FEED` in `main.js`. Same reused **beige banner** as the preview (`.comm-preview__banner`,\ntitle + avatar stack + count — no subtitle), then four sections in `.comm-feed__body`:\n\n| Section | Cards | Layout | Card → |\n|---|---|---|---|\n| **Trending Posts** | 3 post cards (52px avatar, uppercase eyebrow, body) + a \"Post\" button | vertical list | `com-activities` |\n| **Open Questions** | 3 question cards (text, \"Username asked…\", comment icon + count) | **carousel** | `com-questions` |\n| **Groups You're In** | 3 group cards (80px icon, name, count, membership ✓, description) | vertical list | `com-groups` |\n| **People Near You** | 3 person cards (80px photo, Username, count, arrow) | **carousel** | `com-meet` |\n\nEach section ends in a \"View all … →\" link. The two carousels reuse the shared\ncarousel mechanism — `.comm-feed__scroll` is wired into `attachCarousels()` and the\n`[data-carousel]` arrow handler; arrows (`.comm-feed__arrows`) show on desktop only,\nmobile is swipe. The **\"Post\" button** and cards point at the `com-*` list pages.\n\n**Logged-Out vs Logged-In:** the logged-out member sees the feed **gated** —\n`renderCommunityFeed(true)` adds the `.comm-feed--gated` class (blurs the card\ncontent 4px and makes it non-interactive) and prepends a **login-reminder card**\n(\"Welcome back, Jannie123! / You're not logged in. / **Log in now**\", the\n`data-action=\"log-in\"` no-op auth transition — mirrors the panel access card). The\nlogged-in member sees it live, no reminder. Both share all four sections' content.\n\n**Counts** are the literal **`[TBD]`** placeholder (\"[TBD] members\", \"[TBD] team\nmembers\", question answer count \"[TBD]\") — no fabricated numbers ([DECISIONS](../decisions/DECISIONS.md)\n2026-07-27). The post/question/group **text** is illustrative placeholder copy\n(usernames are literally \"Username\"), kept to show what the feed looks like.\n\nAssets: the \"Post\" pencil, question comment icon, and group membership check are\ninlined into `COMMUNITY_SVGS`; post/people avatars reuse `community-{1,2,3}.png`;\nthe reminder-card corner graphics reuse `community-upsell-{tr,bl}.svg`.\n\n### Members (fallback) — the orientation menu (`renderCommunityHub`)\n\n> **Superseded (2026-08-13):** members now get the activity feed above. This\n> wireframe hub is retained only as a defensive fallback for an unknown persona.\n\n**Render:** module-based, following the Splash Landing pattern — a data array\n(`COMMUNITY_MODULES` in `main.js`) + a render function (`renderCommunityHub()`),\nnot one-off inline HTML. `render()` branches on `screen.id === \"community-overview\"`;\nthe page is excluded from `screen__body--fill` so it flows from the top.\n\n**Modules** (5, fixed order) — each an **outlined box** (`.comm-mod`, wireframe-level,\nno visual polish, no live content preview) with a title, one-line description, an\noptional count **badge**, and a \"View →\" link:\n\n| # | Module | Description | Badge | → screen |\n|---|---|---|---|---|\n| 1 | Activity Feed | See what members are sharing today | `[TBD]` | `com-activities` |\n| 2 | Q&A | Ask or answer real member questions | `[TBD]` | `com-questions` |\n| 3 | Groups | Join circles built around your journey | `[TBD]` | `com-groups` |\n| 4 | People | Meet ambassadors, CEMs, and peers | `[TBD]` | `com-meet` |\n| 5 | Community Values & Ambassador Program | How we look after each other | — | `com-values` |\n\nAny count is the literal **`[TBD]`** placeholder — never a fabricated number (same\nrule as the splash community count, [DECISIONS](../decisions/DECISIONS.md) 2026-07-27).\n\nBecause the hub is shared across all personas, its five target screens are injected\ninto every persona (`SHARED_TARGET_SCREENS` in `main.js`) so the links resolve\neverywhere, not just in the member personas. `com-values` (Community Values &\nAmbassador Program) is a new placeholder `page` added for module 5.\n\n(The [Topic Hub](topic-hub.md) surface is **not** reached from here — its entry\npoint is the side panel's topic-hub rows. See [topic-hub.md](topic-hub.md).)\n\n---\n\n## Assets — Community panel icons\n\n> **Superseded (2026-08-03):** the side-panel redesign removed the Community menu\n> list from the panel (Community is now a single tab → Community Overview), so\n> these icons are **no longer rendered**. The files remain in `assets/` but are\n> currently unused. See [navigation.md](../system/patterns/navigation.md) §3.\n\nFrom the Logged-Out Member panel `6960:3` (Community). Monochrome SVGs, tinted\nmagenta via CSS like the other panel icons (see [design.md](../system/foundation/design.md) →\nicon-tinting system):\n\n| File | Label | Source node |\n|---|---|---|\n| `community-stories.svg` | Posts | 7042:583 |\n| `community-qa.svg` | Questions & Answers | 7042:588 |\n| `community-groups.svg` | Groups | 7042:593 |\n| `community-meet.svg` | Meet Others | 7042:598 |\n| `community-activities.svg` | All Community | 7042:603 |\n"},{"path":"product/landing.md","title":"TIM Splash Landing — spec","section":"Product","bytes":8769,"markdown":"# TIM Splash Landing — spec\n\nSource: the **Mobile_Splash_Landing** frame in Figma file `EWsXKakhyFLhkse035AoHX`\n(node `4101:3`), the desktop frame `4113:51` (listicles) / `4101:349` (community\nquotes), and type from the **MHT Style Guide** (`zV2gbuNONeoyeRVcFUFWeZ`, node\n`3941:17`).\nPrototype: plain HTML/CSS/JS in this repo (`../index.html`, `../main.css`, `../main.js`), no build step.\n\nThis doc is the self-contained spec for the **Splash Landing** — the\nVisitor/Subscriber home built from content modules, plus the flow/deep-link pages\nits CTAs open. The global navigation chrome (top nav, panel, dropdown, level-up\nbar, footer) is a separate surface — see [navigation.md](../system/patterns/navigation.md). The\nchronological history of every change lives in [decisions/DECISIONS.md](../decisions/DECISIONS.md). Code\nis the source of truth if any of these disagree — correct the doc, don't erase the\nstale entry.\n\n**Code source of truth:** the landing is rendered by `renderModules()` in\n`main.js` (starts ~`main.js:376`); the CTA deep-link screens live in the\n`SPLASH_FLOW_SCREENS` array (~`main.js:139`). `render()` calls `renderModules()`\nfor home screens flagged `modules: true`.\n\n---\n\n## Where it renders\n\n`renderModules()` runs only on the personas whose home is the Visitor-style\nsplash:\n\n- **Anonymous Visitor** — the Splash Landing is the home.\n- **Subscriber** — top nav and landing **mirror the Visitor** exactly\n  (`navVariant: \"visitor\"`; home screens mirror Visitor's Splash / Topic /\n  Article). Its slide-out panel is now the shared panel (the old\n  subscriber-specific \"Finish up now\" access card was removed 2026-08-03 — see\n  [navigation.md](../system/patterns/navigation.md) §3).\n\nIt does **not** render for:\n\n- **Logged In Member** — home is \"Home as a hub\" (still to be designed), a\n  placeholder label, not the splash.\n- **Logged Out Member** — a gated home, a placeholder label (\"Home as a hub (gated)\"), not the splash. (The \"Welcome back… you're not logged in\" welcome card was removed 2026-08-11; that copy now lives only in the panel access card — see [navigation.md](../system/patterns/navigation.md) §3.)\n\n---\n\n## The six sections (Figma order)\n\nBuilt out 2026-07-27 from the `Mobile_Splash_Landing` frame, in this order:\n\n1. **Checker** — hero: avatar stack + community count + symptom-checker card.\n2. **Listicles** — 5 cards (see asset table below).\n3. **Articles** — horizontal carousel, 324×170 thumbnails. The first two cards\n   carry real images (`article-1`, `article-2`); the rest fall back to the\n   diagonal-hatch placeholder pattern. No eyebrows. The **Collection** card (2nd,\n   → `article-collection`) is flagged `collection: true`, which runs its title on\n   two lines and adds a magenta **stack (\"layers\") icon** to the right of the\n   title so the card reads as part of a Collection.\n4. **Experts** — solid `#EDF3F9` background (`--color-bg-blue-soft`). The three\n   teaser cards are **derived from the committee data** — `ADVISORS.slice(0, 3)`\n   in `main.js` — so they stay in sync with the [Advisors page](advisors.md)\n   (currently Guepet / Lanners / McCool-Pearson). Cards show each advisor's\n   `shortName` (trimmed credentials) rather than the full committee name. Real headshots\n   (`assets/advisor-{1..3}.jpg`, 61px circles, `object-fit: cover`); \"View all\n   advisors →\" opens the full page. (The Medical Advisors Figma set is\n   intentionally ignored here — the committee data is the source of truth.)\n5. **Factoid** — the 82% / 72% stats with the \"Survey of 1,000 U.S. women ages\n   35–59\" source line, straight from the frame.\n6. **Community** — quote carousel (the two real desktop-frame quotes, `4101:349`)\n   + closing CTA.\n\n**Data note:** the hero community count renders as `12,345 women in the community`\n(the number from the Figma frame). The 82% / 72% stats and the survey source line\nalso come straight from the frame.\n\n---\n\n## Styling spec\n\nThe color tokens, DM Serif type scale, and the size-based letter-spacing rule\nthis landing uses are the shared design system — catalogued in\n[design.md](../system/foundation/design.md) (the landing's build is where several tokens, e.g.\n`--color-bg-*` and the purple accents, were first added). This section covers only\nwhat's **specific to the splash sections**:\n\n- **Section padding:** 48px top/bottom, 12px sides (per the frame).\n- **Hero + Factoid gradient:** both `--color-navy` (`#2b2b68`) → `--color-magenta` (`#a440bc`) — hero `184deg`, factoid `180deg`.\n- **Decorative graphics** (exported from Figma as SVG into `assets/`): the hero has concentric-ring elements bleeding off the top-left (`hero-rings-tl.svg`) and bottom-right (`hero-rings-br.svg`) corners, behind the content (`.mod-hero__ring`, `z-index:-1` inside an `isolate`d hero). Each Factoid stat card has a blob/dot element peeking from its top-right (`factoid-blob-1.svg`, `factoid-blob-2.svg`), clipped by the card's `overflow:hidden` (`.mod-stat-card__graphic`). The closing CTA card carries a magenta blob/dot element in **both** opposite corners (`closing-blob.svg`, one asset reused — top-right `scaleY(-1)` at `top:-72/right:-156`, bottom-left `scaleX(-1)` at `bottom:-96/left:-154`, so the two read as diagonal mirror-images matching Figma node `4101:141`; `.mod-cta-card__graphic`).\n- **Factoid card padding:** `24px` (the cards use tighter padding than the frame's 32px).\n- **Hero avatar stack:** 24px circles with a `2px solid #6b3794` ring (Figma ring color — a dark navy ring read as an unwanted border on the gradient), overlapped `-8px`. Uses generic stock faces (`assets/community-{1..3}.png`) representing community members — **not** the named committee headshots (those are real advisors and shouldn't stand in for anonymous users).\n- **Carousels are full-bleed:** the horizontal scrollers (Listicles, Articles, Community quotes) use `margin: 0 -12px` + `padding: 0 12px` so they clip at the **device edges**, not at the section's 12px padding — the first/last card stays inset 12px while cards scroll in/out at the full width.\n- **Listicles:** 5 cards from the desktop frame (`4113:51`), left-aligned, wider\n  (210px), 80px icon circles using `assets/listicles_*.svg`, non-wrapping\n  buttons; rollover uses `--color-primary-soft` (not the panel pink).\n- **Articles:** horizontal carousel, 324×170 thumbnails (first two cards use\n  real images `article-1`/`article-2`; others fall back to the diagonal-hatch\n  placeholder). The Collection card gets a magenta stack icon beside a two-line\n  title.\n- **Footer tagline:** \"Expert advice. Real women. Real talk.\" at 20px / -0.75px\n  Regular (frame `4101:162`).\n\n---\n\n## Flow / deep-link screens\n\nThe splash CTAs open destination pages within the same persona\n(`SPLASH_FLOW_SCREENS`, ~`main.js:139`). Most are labelled placeholders; **Advisors**\nis a fully built page with its own spec in [advisors.md](advisors.md):\n\n| CTA / trigger | Opens |\n|---|---|\n| \"Check all my symptoms\" / \"Check symptoms first\" | **Symptom Checker** |\n| Listicle cards | **Listicle Detail** |\n| One Article card (Collection — 2-line title + stack icon) | **Article Show** (in a collection) → full article template with a \"part of a collection\" callout box above Key Takeaways, linking to its **Collection** |\n| \"View all advisors\" | **Advisors** — see [advisors.md](advisors.md) |\n| \"Join the conversation\" (Community) / panel Community tab | **Community Overview** |\n| Closing card primary — Visitor: **Join for free** | **Sign Up Start** — see [onboarding.md](onboarding.md) |\n| Closing card primary — Subscriber: **Finish up now** | **Registration Step** — see [onboarding.md](onboarding.md) |\n\nThe **closing CTA card is persona-aware**: Visitors see \"You don't have to\nfigure this out alone.\" / **Join for free** → Sign Up Start; Subscribers see\n\"You're almost in.\" / **Finish up now** → Registration Step (the card is now the\nsole entry point to Registration Step). That sign-up surface — chromeless\nrendering plus the nav entry point — is specified in [onboarding.md](onboarding.md).\n\n---\n\n## Assets — listicles (`assets/`, flat folder)\n\nThe 5 listicle icons are full-color SVGs exported from the desktop listicles\nframe (`4113:51`), rendered in 80px circles:\n\n| File | Listicle |\n|---|---|\n| `listicles_flashes.svg` | Hot flashes |\n| `listicles_fog.svg` | Brain fog |\n| `listicles_libido.svg` | Libido |\n| `listicles_sleep.svg` | Sleep |\n| `listicles_weight.svg` | Weight |\n\nExpert-card headshots **are** real — they reuse the committee's\n`assets/advisor-{1..3}.jpg` (see the Experts note above). Article thumbnails are\n**not** — they use a simple diagonal-hatch placeholder pattern (no gradients),\ndeliberately, until real imagery is provided.\n"},{"path":"product/library.md","title":"TIM Library — spec","section":"Product","bytes":3920,"markdown":"# TIM Library — spec\n\nThe Library surface (labelled **\"Resources\"** in the panel; screen ids stay `lib-*`):\nthe topic pages (HRT, Mood, Sleep, Diet, Family, All Resources) plus Article Show.\nThese are reached **through** the slide-out panel's Resources\nsection — the panel *container* itself is nav chrome, specified in\n[navigation.md](../system/patterns/navigation.md) §3. Chronological history is in\n[decisions/DECISIONS.md](../decisions/DECISIONS.md). Code is the source of truth — correct this doc if it\ndrifts.\n\nAll screens here are **labelled placeholders** — real topic/article UI is not\nbuilt.\n\n---\n\n## Panel Library items → topic screens\n\nThe Resources section appears in **every** persona's panel. Section label \"RESOURCES\".\nEach item carries a `data-screen` and navigates in-persona (the panel closes on\nclick):\n\n| Item | Screen id | In personas |\n|---|---|---|\n| HRT & Other Treatments | `lib-hrt` | all |\n| Mood & Mental Health | `lib-mood` | all |\n| Sleep & Insomnia | `lib-sleep` | all |\n| Diet & Nutrition | `lib-diet` | Visitor, Subscriber (full set) |\n| Family & Relationships | `lib-family` | Visitor, Subscriber (full set) |\n| All Resources | `lib-all` | all |\n\n- **Full set** (Visitor, Subscriber): all six.\n- **Short set** (Logged Out Member, Logged In Member): HRT, Mood, Sleep, All\n  Resources.\n\nAll `lib-*` topic pages are top-level (`type: \"page\"`) with **no** level-up pill —\nthe nav logo returns home.\n\n---\n\n## Browse screens\n\nThe Visitor article-browsing chain (shared flow screens in `SPLASH_FLOW_SCREENS`).\nAn **Article Show** belongs either to a **Topic Hub** or to a **Collection**:\n\n| Screen | id | Links up to |\n|---|---|---|\n| Article Show | `article` | **Topic Hub** (`topic-hub`) — level-up pill labelled \"Topic Hub\" |\n| Article Show (in Collection) | `article-collection` | **Collection** (`collection`) — full article template + in-body collection callout above Key Takeaways |\n\n> **Topic Center was folded into Collections (2026-08-04).** The old `topic`\n> \"Topic Center\" screen is **retired**. It was \"a collection of sponsored content,\"\n> so it's now simply a **Collection** (`collection`). Article Show no longer levels\n> up to a standalone Topic Center — it links to a **Topic Hub**\n> ([topic-hub.md](topic-hub.md), a separate new surface) instead; the other article\n> links to a Collection.\n\nThe splash's \"one Article card\" opens the `article-collection` variant. That\nArticle Show uses the **same full article template** as the standalone Article Show\n(shared `renderArticleShell`, generic placeholder copy); an in-page\n**\"part of a collection\" callout box** sits directly **above Key Takeaways** and\nlinks to its **Collection** (see\n[navigation.md](../system/patterns/navigation.md) §2). Collection itself is a plain page.\nThat collection chain is a Splash Landing deep-link, documented in\n[landing.md](landing.md).\n\n---\n\n## Assets — Library (panel topic) icons\n\n> **Superseded (2026-08-03):** the side-panel redesign removed the per-topic\n> Library menu list (and its `icon()` calls), so these icons are **no longer\n> rendered**. The panel now shows generic topic-hub icons (`assets/hub-{1..8}.svg`,\n> pre-tinted). The files below remain in `assets/` but are currently unused; kept\n> for reference / a possible future Resources page. See\n> [navigation.md](../system/patterns/navigation.md) §3.\n\nFrom the Visitor panel `6950:226` (Library). Monochrome SVGs, tinted magenta via\nCSS like the other panel icons (see [design.md](../system/foundation/design.md) → icon-tinting\nsystem):\n\n| File | Label | Source node |\n|---|---|---|\n| `topic-hrt.svg` | HRT & Other Treatments | 6950:246 |\n| `topic-mood.svg` | Mood & Mental Health | 6950:251 |\n| `topic-sleep.svg` | Sleep & Insomnia | 6950:256 |\n| `topic-diet.svg` | Diet & Nutrition | 7010:168 |\n| `topic-family.svg` | Family & Relationships | 7010:166 |\n| `topic-all.svg` | All Resources | 6950:261 |\n"},{"path":"product/onboarding.md","title":"TIM Onboarding — spec","section":"Product","bytes":3936,"markdown":"# TIM Onboarding — spec\n\nThe onboarding surface: the start of account creation — **Sign Up Start** and\n**Registration Step** — and every CTA that opens them, plus the auth transitions\nthat are deliberately *not* wired yet. These CTAs originate on other surfaces (the\nsplash in [landing.md](landing.md), the nav Join button in\n[navigation.md](../system/patterns/navigation.md)), but the destination\npages and their behavior are specified here. Chronological history is in\n[decisions/DECISIONS.md](../decisions/DECISIONS.md). Code is the source of truth — correct this doc if it\ndrifts.\n\n**Code source of truth:** the two screens are declared in `main.js` —\n`signup-start` in `SPLASH_FLOW_SCREENS` (~`main.js:142`) and `registration-step`\nappended to the Subscriber persona only (~`main.js:203`). Chromeless rendering is\nthe `screen.chromeless` branch of `render()` (~`main.js:571`); the ✕ runs the\n`close-flow` action (~`main.js:721`), returning to `state.prevScreenId`\n(remembered on every navigation, ~`main.js:681`).\n\n---\n\n## Screens\n\nBoth are `type: \"page\"`, `chromeless: true` — they render with **no top nav, no\nlevel-up pill, no footer**; just an **✕** in the top-left (`.flow-close`,\n`data-action=\"close-flow\"`) that closes **back to the page they opened from**\n(`state.prevScreenId`).\n\n| Screen id | Label | Available to |\n|---|---|---|\n| `signup-start` | **Sign Up Start** | Visitor **and** Subscriber (part of the shared `SPLASH_FLOW_SCREENS`) |\n| `registration-step` | **Registration Step** | **Subscriber only** (appended to that persona's `screens`) — pending a fuller subscriber-specific design |\n\nBoth are labelled placeholder pages, consistent with the rest of the prototype —\nreal form UI is not built.\n\n---\n\n## Entry points (as wired)\n\nEvery entry point uses `data-screen` (a real in-persona navigation to the\nchromeless page), **not** a cross-folder jump:\n\n| Trigger | Persona(s) | Opens |\n|---|---|---|\n| Nav pill **Join** | **Visitor only** | `signup-start` |\n| Nav pill **Finish** | **Subscriber only** | `registration-step` |\n| Splash closing card **Join for free** | **Visitor only** | `signup-start` |\n| Splash closing card **Finish up now** | **Subscriber only** | `registration-step` |\n\nBoth the nav pill and the splash closing card are **persona-aware** (Subscriber\nand Visitor share the `visitor` nav variant, so the split is by persona key, not\nnav variant). For the Subscriber the nav pill reads **Finish** and the closing\ncard reads \"You're almost in.\" / **Finish up now** — both → `registration-step`.\nFor the Visitor they're **Join** / **Join for free** → `signup-start`.\n\n> **Note (2026-08-03 shared-panel redesign):** the panel's Join for free /\n> Finish up now access cards were removed. Their roles moved to the splash\n> closing card above — which is now the sole entry point to `registration-step`.\n\n---\n\n## Auth transitions — intentionally not wired\n\nTwo CTAs *look* like auth transitions but are **currently no-ops** — they carry a\n`data-action` that has **no matching case** in the click handler, so nothing\nhappens (`main.js:732–734`: \"Cross-flow auth transitions … are intentionally\nno-ops in this prototype — these CTAs no longer jump to another persona's page;\neach flow stays self-contained\").\n\n| CTA | Where | Action | Behavior today |\n|---|---|---|---|\n| **Log in now** | Logged Out Member panel access card | `data-action=\"log-in\"` | no-op |\n| **Log out** | Logged In Member profile dropdown | `data-action=\"log-out\"` | no-op |\n\nThis **supersedes** the earlier (2026-07-24) design in which Log out did a real\ncross-folder navigation into the `../logged-out-member/` flow — see\n[decisions/DECISIONS.md](../decisions/DECISIONS.md). A real implementation would replace these no-ops\nwith an actual auth flow rather than a client-side folder swap; that decision is\nstill open (\"on the horizon\" in [decisions/DECISIONS.md](../decisions/DECISIONS.md)).\n"},{"path":"product/topic-hub.md","title":"TIM Topic Hub — spec","section":"Product","bytes":5735,"markdown":"# TIM Topic Hub — spec\n\nA **Topic Hub** is a single destination that aggregates content **across feature\ntypes** — Q&A, conversations, groups/people, resources — for one specific, popular\npatient concern (e.g. \"Newly Diagnosed,\" \"Fatigue & Energy Management\"). Up to **8\nper site**. The point: a member can explore one concern in one place instead of\nbouncing between the Community feature pages and the Library. Chronological history\nis in [decisions/DECISIONS.md](../decisions/DECISIONS.md). Code (`../main.js`, `../main.css`) is the\nsource of truth — correct this doc if it drifts.\n\n> **Current state:** there is **one generic Topic Hub** (id `topic-hub`, titled\n> \"Topic Hub\") used as the reusable template that every panel topic-hub row opens.\n> The real per-concern hubs (their names + which ~8 concerns) come later; the\n> render + data model already support multiple entries in `TOPIC_HUBS`.\n\n> **Topic Hub vs. the old Topic Center.** Topic Hubs are a **brand-new surface** and\n> the concept we're keeping. The old `topic` \"Topic Center\" screen was **retired\n> 2026-08-04** — folded into **Collections** (it was \"a collection of sponsored\n> content,\" so it's now just a `collection`). So an **Article Show now levels up to a\n> Topic Hub** (pill labelled \"Topic Hub\" → `topic-hub`); a different article links to\n> a **Collection** (in-body callout → `collection`). The panel's up-to-8 hub rows all\n> open this surface.\n>\n> **Doc placement note:** the request named `foundation/topic-hub.md`, but every\n> product *surface* lives in `product/` (system/ is the design system only),\n> so this doc sits alongside its cited templates `community.md` / `library.md`.\n\nVs. the [Community Overview](community.md) hub: that's a lightweight *orientation\nmenu* (plain link-out boxes, no previews). A Topic Hub is richer — its modules are\nmeant to show **content previews**, closer in spirit to the Splash Landing modules.\n\n---\n\n## Screen & rendering\n\n- **Templated, data-driven.** Hubs are entries in the `TOPIC_HUBS` array in\n  `main.js`; `TOPIC_HUB_PAGES` maps each to a screen `{ id, label, type: \"page\",\n  title, topicHub }`. Adding a hub = adding a data entry, **not** a one-off screen\n  (same \"data array + render function\" convention as `renderModules()`).\n- `render()` branches on `screen.topicHub` → `renderTopicHub(hub)`; the page is\n  excluded from `screen__body--fill` so it flows from the top.\n- `type: \"page\"` — **no level-up pill** (a top-level destination, like the other\n  `com-*` / hub pages; the nav logo returns home).\n- Each hub's screen is injected into **every** persona (`SHARED_TARGET_SCREENS`)\n  so it (and its module targets) resolve everywhere.\n\n## Structure\n\n**Header** — topic **name** (DM Serif, navy), a one-line **description**, and a\n**stat** pill. The stat is always the literal **`[TBD]`** placeholder\n(`\"[TBD]% of community discussion this month\"`) — never a fabricated number (same\nrule as the splash community count, [DECISIONS](../decisions/DECISIONS.md) 2026-07-27).\n\n**Modules** — outlined boxes (`.topic-hub-mod`, same wireframe treatment as the\ncommunity hub). Each has a title and a one-line description. The **whole card is\ntappable** (navigates via `data-screen`) — there is no separate link/arrow\naffordance; press feedback comes from `:active` (this is a mobile prototype —\ntap/press, not a desktop rollover). Modules flagged `preview: true` add a\n**content-preview affordance**: skeleton placeholder rows (gray bars) standing in\nfor previewed items — **no fabricated posts/questions**. A module can be `disabled`\n(dead state, no destination yet — non-tappable, no press feedback) or carry a\n`note` (a small italic caveat).\n\n---\n\n## The generic hub (current placeholder)\n\nOne generic hub (`topic-hub`, titled \"Topic Hub\") stands in for every concern for\nnow. Five modules, fixed order:\n\n| # | Module | Description | Preview? | → screen |\n|---|---|---|---|---|\n| 1 | Top Q&A | Most helpful answered questions | yes | `com-questions` ¹ |\n| 2 | Active Conversations | Relevant peer discussions | yes | `com-activities` |\n| 3 | Expert Resources | Curated, medically-reviewed articles | yes | `lib-all` ² |\n| 4 | Groups & People | Circles and members walking the same path | no | `com-groups` |\n| 5 | Action-Oriented Tools | Guides, checklists, step-by-step programs | no | **none** ³ |\n\n¹ **Known placeholder limitation:** no topic-filtering exists yet, so Top Q&A\npoints at the general Q&A page rather than a topic-filtered view. Shown as an\nitalic note on the card.\n\n² Expert Resources is a *curated set of articles* (plural), so it points at **All\nResources** (`lib-all`) — the brief's fallback \"if no single article fits\". A\nsingle `article` (Article Show) detail didn't fit, and `lib-all` resolves in every\npersona.\n\n³ **Dead/disabled state:** no tools surface exists yet, so this module is a\nnon-interactive dashed box with a \"Coming soon\" chip — no destination.\n\n---\n\n## Reachability\n\nThe entry point is the **side panel**: each of the panel's topic-hub rows (under\nthe Home / Resources / Community tabs) opens the generic Topic Hub\n(`data-screen=\"topic-hub\"` — see [navigation.md](../system/patterns/navigation.md) §3).\nConceptually these sit in the **Resources** side of the panel. For now every row\nopens the same generic hub; wiring each row to its own per-concern hub (and the\nreal hub names) is later work.\n\n---\n\n## Fidelity / placeholders\n\nWireframe-level only — outlined boxes, no visual polish, **no fabricated stats,\ncounts, or content previews**. The stat is `[TBD]`; preview modules show skeleton\nrows, not real questions/posts/photos. There is no Figma source node yet — this is\na from-the-brief prototype of a new surface.\n"},{"path":"prototypes/navigation/ARCHITECTURE.md","title":"TIM System — architecture & persona model","section":"Prototypes","bytes":8227,"markdown":"# TIM System — architecture & persona model\n\nHow the prototype is built and how it renders: the `data-persona` model, the\nscreen/state machine, the device frame, and the file layout. This is the \"how it\nworks\" reference; the visual language (tokens, type, tinting) is in\n[design.md](../../system/foundation/design.md), and each content surface has its own doc. Code\n(`../main.js`, `../main.css`) is the source of truth — correct this doc if it\ndrifts.\n\n---\n\n## Render model (`data-persona`)\n\nNo framework, no build step — each flow page is a thin shell that sets one\nattribute, then loads the token layer, a site theme, and the two shared files:\n\n```html\n<link rel=\"stylesheet\" href=\"../../../system/tokens/tokens.css\" />\n<link rel=\"stylesheet\" href=\"../../../system/tokens/themes/menopause.css\" />\n<link rel=\"stylesheet\" href=\"../main.css\" />\n<body data-persona=\"logged-in-member\">\n  <script src=\"../main.js\"></script>\n</body>\n```\n\nThe pages sit at `prototypes/navigation/<persona>/index.html`, three levels\nbelow the repo root — hence `../../../system` for the token layer and `../` for\nthis prototype's own `main.css` / `main.js`. Swapping the theme link re-skins\nthe whole flow; see [the site & theme guide](../../system/tokens/themes/README.md).\n\n`main.js` reads `data-persona` (`LOCKED_PERSONA_KEY`), looks up that persona in the\n`PERSONAS` map (`persona = PERSONAS[key]`), and renders its nav/panel/dropdown and\nscreens. There is **no persona switcher** — each page is locked to whatever\n`data-persona` says, so a single flow can be shared standalone without exposing the\nothers. There are **no per-screen HTML files**; `main.js` renders every screen.\n\n---\n\n## Screen model\n\nEach persona has a `screens` array; `state.screenId` selects the active one and\n`render()` (~`main.js:568`) draws it. Screen shape: `{ id, label, type, title, … }`.\n\n**Types:**\n- `tabs` — the home screen (Splash Landing / hub); carries `modules: true` where it renders content modules.\n- `page` — a top-level page with **no** level-up pill; the nav logo returns home. All panel/dropdown destinations (`lib-*`, `com-*`, `acct-*`) and most flow screens are `page`.\n- `uplevel` — a detail screen with a level-up pill that steps one level up via `upTo` → `data-screen` (see [navigation.md](../../system/patterns/navigation.md) §2).\n- `chromeless: true` — a flag (not a type) that drops nav/level-up/footer and shows an ✕ that closes back to `state.prevScreenId` (the onboarding pages — see [onboarding.md](../../product/onboarding.md)).\n\n**State** (`state` object): `screenId`, `panelOpen`, `dropdownOpen`, `prevScreenId`\n(remembered on every navigation so chromeless pages can close \"back\"). Navigation\nis delegated: any element with `data-screen=\"<id>\"` jumps to that screen and closes\noverlays; `data-action` handles `toggle-panel` / `toggle-dropdown` / `go-home` /\n`go-profile` / `close-flow`. The top nav auto-hides on scroll-down / returns on\nscroll-up (`attachAutoHide()`); the level-up pill stays pinned independently.\n\n---\n\n## Device frame\n\nThe phone is a fixed **iPhone 16 Pro** device frame, **402 × 874**, set by the\n`--device-width` / `--device-height` tokens in `main.css` (single source of truth).\nThe 8px bezel sits *outside* the screen (`box-sizing: content-box`) so the usable\nwidth is a true 402px. `fitPhone()` (~`main.js:753`) scales the whole device down\nwith a `transform` to fit shorter/narrower windows (capped at 1× — never upscales),\ncollapsing the leftover layout box with negative margins so the page never\nscrolls; skipped at ≤430px width, where a media query full-bleeds the phone.\n\n---\n\n## Personas\n\nFour auth states, each with its own nav treatment and panel content (nav-variant\ndetails in [navigation.md](../../system/patterns/navigation.md) §1):\n\nThe **side panel is now shared** across all four personas (Home / Resources /\nCommunity tabs + topic hubs + Explore — see [navigation.md](../../system/patterns/navigation.md) §3),\nso there is no longer a `panelType`. Only the top-nav treatment still varies:\n\n| Persona | `navVariant` | Home | Dropdown? |\n|---|---|---|---|\n| **Anonymous Visitor** | `visitor` (full logotype + Join) | Splash Landing | No |\n| **Logged Out Member** | `member` (logomark + generic profile) | Gated home | Icon only, not built |\n| **Logged In Member** | `member-photo` (logomark + photo) | Hub (placeholder) | Yes |\n| **Subscriber** | `visitor` (mirrors Visitor) | Splash Landing (mirrors Visitor) | No |\n\n**Open question:** should Subscriber have its own top-nav frame, or is reusing the\nVisitor nav correct? No Subscriber tabs/uplevel frame exists in the Figma file to\nconfirm either way — flagged, not decided.\n\n---\n\n## Screens per persona\n\nPanel/dropdown destinations are prototype-added placeholder screens so the menu\nitems lead *somewhere*. They're top-level `page`s (no level-up pill). Library\ndestinations are shared across personas (`lib-*`); Community are `com-*`;\nDropdown/account are `acct-*`. Detailed in [library.md](../../product/library.md),\n[community.md](../../product/community.md), [account.md](../../product/account.md); the Splash Landing home and\nits CTA deep-links are in [landing.md](../../product/landing.md).\n\nThe **Community Overview hub** (`community-overview`) and its five feature targets\n(`com-activities`, `com-questions`, `com-groups`, `com-meet`, `com-values`) are now\n**shared across all four personas** (injected via `SHARED_TARGET_SCREENS`), since\nevery persona's panel Community tab opens the hub. The per-persona lists below note\nCommunity only where it was persona-specific historically.\n\n**Anonymous Visitor**\n- Splash Landing (`tabs`), Article Show (levels up to a Topic Hub), Article Show in a Collection\n- Resources (panel dest.): `lib-hrt`, `lib-mood`, `lib-sleep`, `lib-diet`, `lib-family`, `lib-all`\n\n**Logged Out Member**\n- Gated home (`tabs`; wireframe placeholder labelled \"Home as a hub (gated)\")\n- Resources (panel dest.): `lib-hrt`, `lib-mood`, `lib-sleep`, `lib-all`\n- Community (via the Community Overview hub): `com-activities`, `com-questions`, `com-groups`, `com-meet`, `com-values`\n\n**Logged In Member**\n- Hub home (`tabs`, photo profile) + dropdown-open variant\n- Detail screens: Article Show, Group Detail, Program Detail, Someone's Member Profile, Question Show, Activity Show (`uplevel`)\n- Resources (panel dest.): `lib-hrt`, `lib-mood`, `lib-sleep`, `lib-all`\n- Community (via the Community Overview hub): `com-activities`, `com-questions`, `com-groups`, `com-meet`, `com-values`\n- Account (dropdown dest.): `acct-health`, `acct-messages`, `acct-notifications`, `acct-settings`, `my-profile` (Log out is a no-op, see [onboarding.md](../../product/onboarding.md))\n\n**Subscriber**\n- Mirrors the Visitor Splash Landing as its home (see [landing.md](../../product/landing.md))\n- Resources (panel dest.): `lib-hrt`, `lib-mood`, `lib-sleep`, `lib-diet`, `lib-family`, `lib-all`\n- Adds a Subscriber-only `registration-step` (see [onboarding.md](../../product/onboarding.md))\n\nContent behind each nav is placeholder in the *Figma source itself* — the real\ndesign surface there is the nav/panel/dropdown states, not full page layouts. The\nprototype reflects that: content areas are intentionally labelled placeholders.\n\n---\n\n## File layout\n\n```\nindex.html                     Launcher — static picker linking to each flow\nmain.css                       All styles (shared)\nmain.js                        All behavior + content data + renderers\nassets/                        Flat folder of Figma-exported assets (see design.md)\nvisitor/ logged-out-member/    One folder per persona; each index.html is a thin\nlogged-in-member/ subscriber/    shell (data-persona + ../main.css + ../main.js)\nfoundation/                    These spec docs\n.claude/                       Optional local static-server preview helper\n```\n\nEverything runs from `file://` (no server needed). Inline SVG icons (not external\n`<img>`/CSS-`mask`) are used deliberately so icons render over `file://` — external\nmask/url refs are blocked there (see [design.md](../../system/foundation/design.md) → icon tinting). Built\nplain HTML/CSS/JS to match `tim-splash-landing` conventions and ease a later Rails\nERB port.\n"},{"path":"system/AUTHORITY.md","title":"Authority and preference","section":"Foundations","bytes":3562,"markdown":"# Authority and preference\n\nTwo small conventions that apply everywhere in this repo. Adapted from the\nDesign OS handoff; see [decisions/DECISIONS.md](../decisions/DECISIONS.md) for\nwhy they were adopted.\n\n---\n\n## Authority model\n\nWhen two artifacts disagree, there is **no universal winner**. Each source is\nauthoritative about a different thing:\n\n| Source | Authoritative about |\n|---|---|\n| Running code (`prototypes/`, `system/tokens/`) | What actually behaves this way today |\n| Checks (`evals/`) | Invariants that are actually enforced |\n| Contracts (`system/components/*/docs.md`, `system/patterns/`) | Intended semantics, states, boundaries, component API |\n| Decisions (`decisions/DECISIONS.md`) | Why a rule, exception or tradeoff exists |\n| Product context (`product/`) | Users, activities, outcomes, terminology |\n| Figma | Intended visual direction — not shipped behavior |\n| Live pages (`system/icons/sheet.html`, `system/tokens/reference.html`) | Rendered specimens of the real implementation |\n| Brand (`system/brand/`) | Voice, positioning, messaging |\n| External references | Evidence and inspiration only, never a product rule |\n\n**This replaces the older \"code always wins\" line** that used to close\n`MANUAL.md`. That rule was convenient but wrong: it meant any bug in\n`main.css` could silently become the specification.\n\n### When artifacts disagree\n\n1. **Detect** the difference.\n2. **Classify** it — foundation, component, pattern, visual, contract or intent\n   drift.\n3. **Determine intent** — which artifact expresses what was meant?\n4. **Reconcile** every affected representation, not just the convenient one.\n5. **Verify** with the checks in `evals/`.\n6. **Record** the decision if it was a judgment call.\n\nDo not resolve a disagreement by editing whichever file is easiest.\n\n### Worked example\n\n`--color-magenta` was a *value* name for a token that is really the brand\naccent. Code said \"magenta\"; intent said \"accent\"; the Legacy site made the\nvalue name plainly wrong. The reconciliation was not \"code wins\" — it was to\nintroduce the role name `--color-accent`, keep `--color-magenta` as a\ndeprecated alias so nothing broke, and record why. Three representations\nupdated, none silently overwritten.\n\n---\n\n## Preference grammar\n\nWhen documenting how something should be used, use these six headings rather\nthan an undifferentiated list. An agent can act on the difference between\n\"allowed\" and \"preferred\"; it cannot act on a bullet list of valid options.\n\n| Term | Meaning |\n|---|---|\n| **Preferred** | The default. Reach for this unless there is a reason not to. |\n| **Allowed** | Valid, under the stated condition. |\n| **Avoid** | Usually harmful or inconsistent. Needs a reason. |\n| **Prohibited** | Violates a hard requirement. No exceptions without a decision entry. |\n| **Exception** | A named situation where the normal rule does not apply. |\n| **Rationale** | Why the boundary exists. Without this the rule decays into cargo cult. |\n\n### Example\n\n> **Preferred** — `var(--color-accent)` for brand-accent color.\n> **Allowed** — `var(--color-magenta)`, in code not yet migrated.\n> **Avoid** — introducing a new accent token for a one-off tint.\n> **Prohibited** — a raw hex value in any file outside `system/tokens/`.\n> **Exception** — `-webkit-mask` values, which are luminance not color; and\n> `prototypes/entry-points/`, which mocks other companies' brand colors.\n> **Rationale** — one hue swap has to re-skin every site. A hex literal is\n> invisible to that swap, and `evals/lint-tokens.js` enforces it.\n"},{"path":"system/brand/brand-source-of-truth.md","title":"ThisIsMenopause Brand Source of Truth","section":"Brand","bytes":18087,"markdown":"# ThisIsMenopause Brand Source of Truth\n\n## Document goal\n\nCreate one canonical markdown document that design, content, PMM, lifecycle, and product teams can use as the shared reference for how **ThisIsMenopause** should look, sound, and behave across site, articles, email, social, paid, and future product touchpoints.\n\n## Recommended document principles\n\n- Be opinionated enough to reduce drift.\n- Separate evergreen brand rules from channel-specific execution guidance.\n- Distinguish clearly between:\n  - brand foundations\n  - editorial/copy rules\n  - visual system rules\n  - UX/product usage rules\n  - campaign/channel adaptations\n- Include examples, do/don’ts, and links to source assets wherever possible.\n- Mark each rule as either:\n  - Required\n  - Recommended\n  - Contextual / channel-specific\n\n---\n\n## 1. About this document\n\n### 1.1 Purpose\nExplain that this document is the single source of truth for the Menopause brand across design, content, and PMM.\n\n### 1.2 Who this is for\nList the primary users:\n- Design\n- Content Ops / Editorial\n- PMM\n- Lifecycle / Email\n- Social / Paid acquisition\n- Product / UX / Engineering partners\n- External freelancers and agencies\n\n### 1.3 How to use it\nDefine how teams should use the doc:\n- Use brand foundations for strategic decisions.\n- Use copy rules for writing.\n- Use visual guidance for creative execution.\n- Use channel sections for formatting and adaptation.\n- Use governance section when rules conflict.\n\n### 1.4 Source stack\nInclude the main source docs that informed the system:\n- ThisIsMenopause Brand Foundations\n- MyHealthTeam Style Guide\n- ThisIsMenopause Brand Guide\n- Menopause Review / research and concept testing\n- Menopause Marketing Planning\n\n---\n\n## 2. Brand at a glance\n\n### 2.1 Official brand name\nDefine the official spelling and usage of **ThisIsMenopause** as one word with title-case capitalization, and note that it should not be written as “This Is Menopause” in branded usage.\n\n### 2.2 Core tagline\nDocument the preferred current tagline and note any conflicts or alternates found in source material:\n- Primary: “Making sense of menopause, together.”\n- Alternate historical phrasing to preserve but not default to: “Expert guidance and real experiences from women at every stage / every age.”\n\n### 2.3 One-liner / elevator pitch\nCapture the current short description:\n- “Straight talk, expert guidance, and real experiences for every stage of menopause.”\n\n### 2.4 Brand essence\nInclude the one-sentence strategic expression of the brand:\n- “We make menopause make sense—so women feel informed, supported, and in control.”\n\n### 2.5 Brand promise\nSummarize the user promise:\n- clarity on what is normal\n- practical trustworthy guidance\n- real experiences plus expert input\n- a safe, judgment-free space\n- increased confidence and control\n\n### 2.6 Positioning statement\nDraft a fuller positioning statement for cross-functional use:\n- category\n- who it serves first\n- what it uniquely combines\n- how it differs from telehealth-only, clinical-only, or wellness-only brands\n\n---\n\n## 3. Audience definition and jobs to be done\n\n### 3.1 Primary audience\nDefine the primary initial audience as people in perimenopause, especially those early in the journey and unsure whether what they are experiencing is menopause-related.\n\n### 3.2 Secondary audiences\nCapture adjacent audiences:\n- postmenopausal people\n- premenopausal people who are earlier in the journey and curious what to expect\n\n### 3.3 Emotional starting point\nOutline the dominant emotional context:\n- confusion\n- uncertainty\n- dismissal\n- overwhelm\n- isolation\n- desire for reassurance\n\n### 3.4 Functional needs\nDocument what users most want:\n- “Is this normal?” validation\n- symptom understanding\n- practical management tips\n- trusted treatment comparisons\n- real experiences\n- safe community\n\n### 3.5 Core jobs to be done\nRecommend framing around these jobs:\n1. Help me understand what is happening.\n2. Help me know what is normal.\n3. Help me understand my options without judgment.\n4. Help me hear from women like me.\n5. Help me take a practical next step.\n\n---\n\n## 4. Strategic brand foundations\n\n### 4.1 Brand values\nCreate a section for each value with:\n- definition\n- what it looks like in practice\n- what it should never become\n\nCore values to cover:\n- Empathy and validation\n- Direct, open, and unafraid\n- Credible, not intimidating\n- Warm, not flippant\n- Judgment-free support\n- Inclusive by design\n\n### 4.2 Personality\nTranslate values into practical personality traits:\n- smart\n- calm\n- candid\n- human\n- approachable\n- lightly cheeky when appropriate\n- never infantilizing or sterile\n\n### 4.3 What makes the brand different\nDefine differentiation against adjacent spaces:\n- not clinical and cold\n- not fluffy wellness\n- not selling a single fix\n- not fear-led\n- not only expert-downstream\n- combines lived experience with expert-backed clarity\n\n---\n\n## 5. Voice and tone system\n\n### 5.1 Core voice pillars\nBuild a durable voice model around:\n- reassuring and validating\n- bold, direct, and grounded\n- practical and action-oriented\n- warm, not flippant\n- empowering\n\n### 5.2 Tone spectrum\nShow where the brand should sit on key axes:\n- warm ←→ clinical\n- direct ←→ soft\n- candid ←→ euphemistic\n- hopeful ←→ alarmist\n- lightly witty ←→ jokey\n- credible ←→ academic\n\n### 5.3 Tone by channel\nSpecify how tone flexes by surface:\n- Product / UX: calmest, clearest, most supportive\n- Articles: compassionate, practical, plain-language\n- Email: warm and helpful, less embellished\n- Social organic: more voice, more irreverence allowed\n- Paid: tighter, clearer, emotionally immediate\n- PMM / decks: confident and strategic\n\n### 5.4 Voice dos and don’ts\nInclude operational rules:\n- Do normalize experiences.\n- Do use simple language and practical takeaways.\n- Do acknowledge uncertainty honestly.\n- Don’t fear-monger.\n- Don’t overpromise outcomes.\n- Don’t use heavy medical jargon.\n- Don’t shame any path or choice.\n\n### 5.5 Reading level and clarity standard\nAdopt the MyHealthTeam guidance for clear, compassionate, plain language and target roughly an 8th grade reading level for consumer-facing content.\n\n---\n\n## 6. Messaging architecture\n\n### 6.1 Primary message\nAnchor the brand on clarity, reassurance, and next steps rather than symptom panic or a single treatment promise.\n\n### 6.2 Messaging pillars\nRecommend the master narrative pillars:\n- Is this normal?\n- You’re not alone.\n- Ease and understanding.\n- Real experiences plus expert guidance.\n- Choice without judgment.\n\n### 6.3 Proof points\nCreate a section for allowable proof:\n- user research signals\n- SEO demand\n- community scale\n- expert-reviewed content\n- practical tools / symptom tips\n\n### 6.4 Key message hierarchy\nRecommend a hierarchy:\n1. Clarify what’s happening\n2. Validate the experience\n3. Offer a useful next step\n4. Reinforce that support exists\n5. Introduce options without prescribing one path\n\n### 6.5 Channel message mapping\nInclude a matrix showing how each pillar expresses in:\n- hero copy\n- article headlines\n- onboarding\n- email subject lines\n- social captions\n- paid ads\n- community prompts\n\n---\n\n## 7. Copywriting rules and language guardrails\n\n### 7.1 Naming and capitalization rules\nInclude exact formatting rules for:\n- ThisIsMenopause\n- URL usage\n- bolding / emphasis rules when needed\n\n### 7.2 Approved language patterns\nDocument preferred patterns such as:\n- “what’s normal”\n- “make sense of menopause”\n- “clear answers”\n- “practical tips”\n- “real experiences”\n- “expert-reviewed” / “expert guidance”\n- “judgment-free”\n- “supportive community”\n- “symptom relief” where appropriate\n\n### 7.3 Restricted or avoid language\nDocument explicit avoid lists:\n- cure / fix / eliminate symptom claims\n- shame-based framing\n- overly negative or “everything sucks” framing\n- panic / urgency language\n- one-size-fits-all treatment claims\n- weight-loss-led headlines\n- polarizing anti-HRT or pro-HRT absolutism\n\n### 7.4 Medical terminology rules\nInclude a Menopause-specific editorial section pulling from the MHT style guide, especially:\n- use **menopause hormone therapy** on first reference\n- “hormone therapy” is OK afterward\n- avoid relying on “hormone replacement therapy” or “HRT” except for SEO or orientation in intros\n\n### 7.5 Inclusive language rules\nInclude the MHT guidance adapted for this brand:\n- default to gender-neutral language in editorial guidance where possible\n- use the precise source language when accuracy requires gendered terms\n- avoid unnecessary assumptions about identity\n\n### 7.6 Plain-language conversion rules\nInclude a short Menopause-specific version of the simple-language chart for common topics:\n- symptom vs sign\n- menopause hormone therapy vs HRT\n- “high blood pressure” vs “hypertension”\n- “doctor” vs “clinician”\n- “what you need to know” vs “overview”\n\n### 7.7 Structural writing rules\nAdd formatting standards for readable copy:\n- short paragraphs\n- clear subheads\n- bullets where useful\n- practical takeaway emphasis\n- internal and external source-linking standards for article content teams\n\n---\n\n## 8. Content strategy and editorial application\n\n### 8.1 Core content pillars\nDocument the major content buckets:\n- Menopause 101\n- symptom understanding\n- treatment options\n- living with perimenopause\n- workplace / family / relationships\n- mood and mental health\n- advocating with doctors\n- costs and coverage\n\n### 8.2 Content forms\nSpecify approved forms:\n- articles\n- guides\n- expert Q&A\n- personal stories\n- short-form video\n- interactive modules / quizzes / surveys\n\n### 8.3 Article copy guidance\nDefine how article voice should differ from ads and social:\n- more grounded\n- more explanatory\n- less irreverent\n- still warm and validating\n- still plain-language and next-step oriented\n\n### 8.4 Social copy guidance\nDefine the social expression:\n- more candid\n- more culturally fluent\n- more humor allowed\n- still never fear-based or mean\n- still on-brand and helpful\n\n### 8.5 Lifecycle / email guidance\nCreate a section for:\n- onboarding tone\n- newsletter framing\n- “confused → informed → confident” progression\n- community and practical-value balance\n\n---\n\n## 9. Visual identity system\n\n### 9.1 Visual brand intent\nOpen with the strategic visual brief:\n- modern\n- trusted\n- calm\n- confident\n- human\n- not cold or clinical\n- not depressing\n- not overly embellished\n\n### 9.2 Logo system\nDocument:\n- horizontal and stacked logos\n- full-color vs white vs black usage\n- logo mark meaning and usage\n- spacing / presentation rules\n\n### 9.3 Color palette\nInclude:\n- primary brand colors and hex values\n- dominant usage guidance\n- channel/application notes\n- contrast and accessibility notes\n- distinction between marketing/content colors and site UX colors\n\n### 9.4 Typography\nDocument:\n- logo fonts\n- social fonts\n- site font\n- typography usage by channel\n- where Lato is expected vs where Mermaid / Ballmain / Museo are used\n\n### 9.5 Shapes, icons, and graphic language\nInclude:\n- approved icons only\n- approved graphic elements\n- sparing use of on-page graphics\n- shapes / lines / motifs as enhancers, not clutter\n\n### 9.6 Imagery and art direction\nDefine what imagery should do:\n- uplifting\n- approachable\n- real\n- a bit cheeky\n- diverse in age, race, and body type\n- often use props or objects\n- avoid sad, cold, or overly stock-wellness imagery\n\n---\n\n## 10. Product and article experience application\n\n### 10.1 Website vs marketing distinction\nClarify where marketing brand styling applies directly and where product UX intentionally diverges or adapts.\n\n### 10.2 Article art system\nDocument:\n- photo-forward approach\n- use of templates\n- graphic treatments\n- in-article graphics\n- hero image guidance\n- hot topic templates\n\n### 10.3 Page modules\nInclude specific rules for:\n- title/body color treatment\n- bullet and number color\n- allowed module background colors\n- contrast requirements\n\n### 10.4 Mini modules and UGC modules\nDefine:\n- mini-module styling\n- UGC module treatment\n- icon placement\n- when to use each\n\n### 10.5 Infographics, quizzes, and interactive modules\nDocument:\n- dark navy titles\n- photo-forward preference\n- when illustration is acceptable\n- template reuse\n- consistency with article brand treatment\n\n---\n\n## 11. Channel-specific execution sections\n\n### 11.1 Site and product\nCapture product-facing implications:\n- trust through design\n- clarity before depth\n- reveal over time\n- progressive disclosure\n- emotional reassurance\n- faster paths to value\n\n### 11.2 Articles and resource hubs\nDefine headline, module, imagery, and tone rules for long-form content.\n\n### 11.3 Email\nDocument the intended email expression:\n- simple\n- clean\n- elegant\n- not overly embellished\n- not washed in pink\n\n### 11.4 Social organic\nDefine templates, categories, copy patterns, logo usage, and degrees of variation permitted.\n\n### 11.5 Paid acquisition\nCreate a section for:\n- supported creative spaces\n- message hooks\n- headline / CTA constraints\n- platform adaptations\n- what not to do\n\n### 11.6 Influencer / partner content\nDefine how the brand should show up through creators:\n- medically aligned first\n- accurate, empathetic, science-backed\n- diverse voices\n- genuine fit over templated sponsorship language\n\n### 11.7 PMM / decks / external storytelling\nInclude a strategic messaging mode for decks, briefs, and partner-facing materials.\n\n---\n\n## 12. Compliance, safety, and healthcare guardrails\n\n### 12.1 Claims policy\nCreate a concise rules section covering:\n- no diagnosis claims\n- no treatment promises\n- no cure / fix language\n- no second-person medical assertions in ad-style copy when risky\n\n### 12.2 Treatment balance policy\nState that the brand must present options without positioning a single path as the answer.\n\n### 12.3 Weight and body messaging\nDocument that weight gain can be covered, but should not lead the brand or define its promise.\n\n### 12.4 Stigma and shame prevention\nCreate guardrails for:\n- no blame\n- no “doing menopause wrong”\n- no infantilizing\n- no dehumanizing wording\n\n### 12.5 Accuracy and sourcing expectations\nReference MHT standards for:\n- evidence-based claims\n- contextualized statistics\n- source quality\n- reference management\n\n---\n\n## 13. Examples library\n\n### 13.1 Good examples\nInclude a curated section with short examples of:\n- hero headlines\n- article intros\n- email copy\n- ad copy\n- onboarding microcopy\n- social captions\n\n### 13.2 Do / don’t pairs\nFor each major area, show:\n- on-brand\n- off-brand\n- why\n\n### 13.3 Reusable copy blocks\nConsider including:\n- brand blurb\n- short description\n- influencer pitch paragraph\n- email promo blurb\n- CTA bank\n\n---\n\n## 14. Asset library and source links\n\n### 14.1 Required asset links\nProvide direct links or locations for:\n- logo files\n- font downloads\n- email design files\n- social branding\n- site/landing page files\n- hot topic templates\n- approved icons\n- article page graphic assets\n\n### 14.2 Working file references\nAdd a maintained appendix for:\n- Figma style guide\n- Drive folders\n- approved templates\n- Asana request forms / design intake\n\n---\n\n## 15. Governance and maintenance\n\n### 15.1 Decision owners\nAssign likely owners by area:\n- Brand / PMM\n- Creative / Design\n- Content / Editorial\n- Product UX\n- Lifecycle\n\n### 15.2 Rule hierarchy\nRecommend a precedence order:\n1. Legal / medical / compliance\n2. ThisIsMenopause brand-specific rules\n3. MHT editorial rules\n4. channel-specific exceptions\n5. local campaign decisions\n\n### 15.3 Change log\nAdd a simple version history:\n- date\n- section changed\n- owner\n- reason\n\n### 15.4 Open questions to resolve in v1\nCapture unresolved items such as:\n- final tagline lock\n- when to use women-specific vs more inclusive phrasing by channel\n- exact typography mapping by channel\n- what belongs in product UX vs marketing-only brand expression\n- approved humor threshold by surface\n\n---\n\n## Recommended appendix structure\n\n### Appendix A. Brand quick reference\nA compressed summary for everyday users.\n\n### Appendix B. Copy reference\nTerminology, claims, and phrase bank.\n\n### Appendix C. Visual reference\nColors, typography, logo, art direction, icon rules.\n\n### Appendix D. Channel templates\nSocial, email, article, paid, product microcopy.\n\n### Appendix E. Source links\nLiving links to the original materials and asset libraries.\n\n---\n\n## Suggested next-draft build order\n\n1. Brand at a glance  \n2. Audience and jobs to be done  \n3. Brand foundations  \n4. Voice and tone  \n5. Messaging architecture  \n6. Copy rules and compliance  \n7. Visual system  \n8. Product/article application  \n9. Channel adaptations  \n10. Governance and appendices\n\n## Notes for the final full draft\n\n- The final version should read as a real operating document, not a brainstorm.\n- It should consolidate conflicting source language and explicitly call the chosen standard.\n- It should include examples throughout, especially in voice/tone, copy, social, article, and paid sections.\n- It should preserve the strongest throughline found across sources: **clarity, validation, practical help, and expert-backed real experiences without fear, shame, or one-size-fits-all treatment framing**.\n\n\n---\n\n## Sources\n\n- [ThisIsMenopause_Brand Foundations](https://docs.google.com/document/d/1eCpL74WqipurWR0ktr2VD_WjM-m8-GbBR8jddQXi7qY)\n- [ThisIsMenopause_BrandGuide.pptx](https://docs.google.com/presentation/d/1LdS80Iy_4_8ICtrgGO3Ip7rX9oGuZiYJ)\n- [Menopause Marketing planning 12.16.25](https://docs.google.com/presentation/d/1AyJ4sG43o0TngcX10F54cMaaE6n4X0qJOnM0-dYV8ws)\n- [MyHealthTeam Style Guide.docx](https://docs.google.com/document/d/1yl-3yQJNjcPwyxdGHrYjwgpD_Q9ihtrK)\n- [Menopause review 12_5_25](https://docs.google.com/presentation/d/1UMP-sENbhxb-MPaCzjodAu6H-YTzFnp9T-Q560EYFVk)\n- [MHT UX – ThisIsMenopause.pptx](https://docs.google.com/presentation/d/1FrdFdRdeA3VRncJauOXJusDtdSKZZmFC)\n- [Onboarding | Principles & Application to Menopause](https://docs.google.com/presentation/d/1yjbxuch0KL0sv0o8QIPxkx5fuO3JXVlc3m6nkKincC4)\n"},{"path":"system/brand/site-design-language.md","title":"Design System Inspired by This Is Menopause","section":"Brand","bytes":23268,"markdown":"# Design System Inspired by This Is Menopause\n\n## 1. Visual Theme & Atmosphere\n\nThis Is Menopause embodies a modern, approachable, and inclusive digital community designed to destigmatize menopause conversations. The visual identity balances professional expertise with warmth and relatability—combining bold, energetic purples and blues with clean, spacious layouts that feel inviting rather than clinical. The design prioritizes clarity and accessibility, using generous whitespace and straightforward navigation to make complex health information digestible. Photography and imagery emphasize real women's experiences and connection, while the typography conveys both confidence and empathy. The overall atmosphere is one of supportive intelligence: authoritative without being alarmist, modern without being cold, and distinctly designed for adult women seeking genuine guidance.\n\n**Key Characteristics**\n\n- Bold, vibrant accent colors (rich purple and cobalt blue) paired with dark navy anchors\n- Clean, minimal layouts with generous whitespace and breathing room\n- Rounded, soft corner radii that feel approachable and contemporary\n- High-contrast text on light backgrounds for legibility and focus\n- Organized hierarchy that guides users through complex health topics\n- Warm, inclusive photography and illustrated elements\n- Community-driven visual language emphasizing peer connection\n- Accessible color and typography combinations throughout\n\n## 2. Color Palette & Roles\n\n### Primary\n\n- **Navy Dark** (`#0D1B29`): Dominant text color, primary headings, body copy, high-confidence elements; creates strong visual foundation across the interface\n- **Deep Navy** (`#071527`): Darkest brand element, used sparingly for maximum emphasis on critical content\n\n### Accent Colors\n\n- **Menopause Purple** (`#A441BC`): Brand signature accent, used for logo lockup, featured section highlights, and thematic brand moments; conveys energy and inclusion\n- **Cobalt Blue** (`#0F57A8`): Secondary brand accent, primary interactive elements (buttons, links), calls-to-action; suggests trust and expertise\n- **Light Periwinkle** (`#4F81E5`): Softer blue variant, hover states, secondary interactive treatments\n- **Light Lavender** (`#DBE5F9`): Very light accent, background tints for informational sections, gentle visual separation\n\n### Interactive\n\n- **Primary CTA Blue** (`#0F57A8`): Main buttons, primary navigation links, primary actions\n- **Secondary CTA Blue** (`#4F81E5`): Hover states, secondary actions, loading indicators\n- **Ghost Link Blue** (`#0F57A8`): Text-only links with no background, underlined or inline styling\n\n### Neutral Scale\n\n- **Black** (`#000000`): Text hierarchy secondary, bold statements, dark mode considerations\n- **Dark Gray** (`#626B74`): Secondary body text, muted captions, disabled states\n- **Medium Gray** (`#666666`): Tertiary text, subtle annotations, hints\n- **Light Gray** (`#999999`): Divider lines, very light UI elements\n- **Lighter Gray** (`#C1C1C1`): Soft borders, light backgrounds\n- **Pale Gray** (`#CCCCCC`): Input borders, light dividers\n- **Softest Gray** (`#808080`): Alternative secondary text\n- **White** (`#FFFFFF`): Primary background, card fills, contrast surfaces\n\n### Surface & Borders\n\n- **Light Border Gray** (`#DBDDDF`): Card borders, subtle dividers, form field separators; creates visual containment without heaviness\n- **Input Border** (`#C1C1C1`): Form field borders, search input separators\n- **Card Background** (`#FFFFFF`): Primary content containers, elevated surfaces\n\n## 3. Typography Rules\n\n### Font Family\n\n**Primary:** Lato, -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, \"Helvetica Neue\", sans-serif\n\n**Secondary:** Times New Roman, Georgia, serif (minimal use for specific contexts)\n\n**Monospace:** \"Courier New\", Courier, monospace (for code or technical snippets if needed)\n\n### Hierarchy\n\n| Role | Font | Size | Weight | Line Height | Letter Spacing | Notes |\n|------|------|------|--------|-------------|-----------------|-------|\n| Display / H1 | Lato | 28px | 600 | 36px | 0px | Hero headline, major section titles; bold and commanding presence |\n| Heading / H2 | Lato | 20px | 600 | 24px | 0px | Section subheadings, card titles; clear hierarchy without overwhelming |\n| Heading / H3 | Lato | 18px | 600 | 26px | 0px | Subsection titles, component headings; balance of prominence and readability |\n| Body / Paragraph | Lato | 18px | 400 | 26px | 0px | Primary body text, form labels, content blocks; spacious line height aids readability |\n| Body / Small | Lato | 16px | 400 | 16px | 0px | UI labels, metadata, caption text, secondary information; compact but clear |\n| Button Text | Lato | 16px | 600 | 18px | 0px | Calls-to-action, button labels; weight increase adds visual emphasis |\n| Link Text | Lato | 18px | 400 | 26px | 0px | Inline and standalone links; inherit body styling with color and underline override |\n| Caption / Micro | Lato | 14px | 400 | 16px | 0px | Timestamps, bylines, small form hints; minimized but not cramped |\n\n### Principles\n\n- **Clarity over decoration:** Lato's geometric sans-serif ensures excellent legibility across sizes and contexts\n- **Generous line height:** 1.3–1.5 ratio creates breathing room for complex health content\n- **Weight hierarchy:** Only H1, H2, H3, and buttons use weight 600; body remains 400 for contrast and focus\n- **Consistent metrics:** All sizes align to the 4px spacing grid for visual harmony\n- **Accessible contrast:** All text meets WCAG AA standards (navy on white, dark gray on light backgrounds)\n\n## 4. Component Stylings\n\n### Buttons\n\n#### Primary Button\n- **Background:** `#0F57A8`\n- **Text Color:** `#FFFFFF`\n- **Font:** Lato, 16px, weight 600\n- **Padding:** `16px 32px`\n- **Border Radius:** `36px`\n- **Border:** `1px solid #0F57A8`\n- **Height:** 52px (minimum touch target)\n- **Line Height:** 18px\n- **Box Shadow:** `none`\n- **Hover State:** Background `#0D4A8C`, border `#0D4A8C`, text `#FFFFFF`\n- **Active State:** Background `#0A3A6F`, border `#0A3A6F`, text `#FFFFFF`\n- **Disabled State:** Background `#D4D4D4`, border `#D4D4D4`, text `#999999`\n\n#### Secondary Button\n- **Background:** `#FFFFFF`\n- **Text Color:** `#0F57A8`\n- **Font:** Lato, 16px, weight 600\n- **Padding:** `16px 32px`\n- **Border Radius:** `36px`\n- **Border:** `1px solid #0F57A8`\n- **Height:** 52px\n- **Line Height:** 18px\n- **Box Shadow:** `none`\n- **Hover State:** Background `#F0F4F9`, border `#0D4A8C`, text `#0D4A8C`\n- **Active State:** Background `#DBE5F9`, border `#0A3A6F`, text `#0A3A6F`\n- **Disabled State:** Background `#FFFFFF`, border `#D4D4D4`, text `#999999`\n\n#### Ghost Button\n- **Background:** transparent\n- **Text Color:** `#0F57A8`\n- **Font:** Lato, 16px, weight 400\n- **Padding:** `0px 0px`\n- **Border Radius:** `0px`\n- **Border:** `none`\n- **Height:** auto\n- **Line Height:** 18px\n- **Box Shadow:** `none`\n- **Hover State:** Text Color `#0D4A8C`, underline `1px solid #0D4A8C`\n- **Active State:** Text Color `#0A3A6F`, underline `1px solid #0A3A6F`\n\n#### Icon Button (Small)\n- **Background:** `#F3F4F4`\n- **Text Color:** `#929899`\n- **Font Size:** 16px\n- **Padding:** `0px 0px`\n- **Border Radius:** `36px`\n- **Border:** `1px solid transparent`\n- **Height:** 32px\n- **Width:** 32px\n- **Box Shadow:** `none`\n- **Hover State:** Background `#E8EAEB`, text `#626B74`\n- **Active State:** Background `#D4D7D9`, text `#0D1B29`\n\n### Cards & Containers\n\n#### Standard Card\n- **Background:** `#FFFFFF`\n- **Text Color:** `#0D1B29`\n- **Font:** Lato, 18px, weight 400\n- **Padding:** `24px 24px 24px 24px`\n- **Border Radius:** `16px`\n- **Border:** `1px solid #DBDDDF`\n- **Box Shadow:** `none` (or `rgb(219, 221, 223) 0px 0px 16px 0px` on hover/elevated state)\n- **Line Height:** 26px\n- **Hover State:** Box Shadow `rgb(219, 221, 223) 0px 2px 16px 0px`\n\n#### Content Card with Image\n- **Background:** `#FFFFFF`\n- **Border Radius:** `16px` for the container; `16px 16px 0px 0px` for image top corners\n- **Padding:** `0px` (image flush to top), `24px` for text content below\n- **Border:** `1px solid #DBDDDF`\n- **Box Shadow:** `rgb(219, 221, 223) 0px 0px 16px 0px`\n\n#### Section Container\n- **Background:** `#FFFFFF` or transparent\n- **Padding:** `60px 48px 60px 48px`\n- **Border Radius:** `0px` (full-width sections)\n- **Border:** `none`\n- **Max Width:** 1440px (centered with auto margins)\n\n#### Q&A / Question Card\n- **Background:** `#FFFFFF`\n- **Text Color:** `#0D1B29`\n- **Font:** Lato, 18px, weight 400\n- **Padding:** `24px 24px`\n- **Border Radius:** `16px`\n- **Border:** `1px solid #DBDDDF`\n- **Box Shadow:** `rgb(219, 221, 223) 0px 0px 16px 0px`\n- **Hover State:** Border color `#C1C1C1`, box shadow enhanced\n\n### Inputs & Forms\n\n#### Text Input (Search / Standard)\n- **Background:** `#FFFFFF`\n- **Text Color:** `#0D1B29`\n- **Font:** Lato, 18px, weight 400\n- **Padding:** `0px 13.5px` (horizontal), `0px` (vertical, height set explicitly)\n- **Border Radius:** `36px` or `36px 0px 0px 36px` (for search input with button)\n- **Border:** `1px solid #C1C1C1`\n- **Height:** 40px\n- **Width:** 250px (or 360px for full search bar)\n- **Line Height:** 22px\n- **Placeholder Color:** `#999999`\n- **Focus State:** Border `1px solid #0F57A8`, box shadow `0px 0px 8px rgba(15, 87, 168, 0.2)`\n- **Disabled State:** Background `#F3F4F4`, border `#D4D4D4`, text `#999999`\n- **Error State:** Border `1px solid #D9534F`, box shadow `0px 0px 8px rgba(217, 83, 79, 0.2)`\n\n#### Textarea\n- **Background:** `#FFFFFF`\n- **Text Color:** `#0D1B29`\n- **Font:** Lato, 16px, weight 400\n- **Padding:** `12px 16px`\n- **Border Radius:** `8px`\n- **Border:** `1px solid #C1C1C1`\n- **Min Height:** 120px\n- **Line Height:** 22px\n- **Resize:** vertical\n- **Focus State:** Border `1px solid #0F57A8`, box shadow `0px 0px 8px rgba(15, 87, 168, 0.2)`\n\n#### Form Label\n- **Font:** Lato, 16px, weight 600\n- **Color:** `#0D1B29`\n- **Margin Bottom:** `8px`\n- **Display:** block\n\n#### Helper Text / Form Hint\n- **Font:** Lato, 14px, weight 400\n- **Color:** `#626B74`\n- **Margin Top:** `4px`\n- **Line Height:** 16px\n\n### Navigation\n\n#### Top Navigation Bar\n- **Background:** `#FFFFFF` (with subtle bottom border `1px solid #DBDDDF`)\n- **Height:** 64px\n- **Padding:** `0px 48px`\n- **Display:** flex, align-items center, justify-content space-between\n\n#### Navigation Link (Text)\n- **Font:** Lato, 18px, weight 400\n- **Color:** `#0D1B29`\n- **Text Decoration:** none\n- **Padding:** `8px 0px`\n- **Border Bottom:** `2px solid transparent`\n- **Hover State:** Color `#0F57A8`, border-bottom `2px solid #0F57A8`\n- **Active State:** Color `#0F57A8`, border-bottom `2px solid #0F57A8`\n\n#### Dropdown Navigation\n- **Background:** `#FFFFFF`\n- **Border:** `1px solid #DBDDDF`\n- **Border Radius:** `8px`\n- **Padding:** `12px 0px`\n- **Box Shadow:** `rgb(219, 221, 223) 0px 0px 16px 0px`\n- **Z Index:** 100\n- **Min Width:** 200px\n\n#### Dropdown Item\n- **Font:** Lato, 16px, weight 400\n- **Color:** `#0D1B29`\n- **Padding:** `12px 16px`\n- **Hover State:** Background `#F0F4F9`, color `#0F57A8`\n\n### Badges\n\n#### Badge (Informational)\n- **Background:** `#DBE5F9`\n- **Text Color:** `#0F57A8`\n- **Font:** Lato, 12px, weight 600\n- **Padding:** `4px 8px`\n- **Border Radius:** `2px`\n- **Border:** `1px solid #4F81E5`\n- **Display:** inline-block\n\n#### Badge (Primary)\n- **Background:** `#A441BC`\n- **Text Color:** `#FFFFFF`\n- **Font:** Lato, 12px, weight 600\n- **Padding:** `4px 8px`\n- **Border Radius:** `2px`\n- **Border:** `1px solid #A441BC`\n\n### Tabs\n\n#### Tab Container\n- **Background:** `#FFFFFF`\n- **Border Bottom:** `1px solid #DBDDDF`\n- **Display:** flex\n- **Height:** 48px\n\n#### Tab Button\n- **Font:** Lato, 16px, weight 400\n- **Color:** `#626B74`\n- **Background:** transparent\n- **Padding:** `12px 20px`\n- **Border:** none\n- **Border Bottom:** `2px solid transparent`\n- **Cursor:** pointer\n- **Hover State:** Color `#0D1B29`, border-bottom `2px solid #DBDDDF`\n- **Active State:** Color `#0F57A8`, border-bottom `2px solid #0F57A8`\n\n## 5. Layout Principles\n\n### Spacing System\n\n**Base Unit:** 4px\n\n**Scale:**\n- 4px: Micro spacing (text kerning, tight padding)\n- 8px: Dense spacing (button internal, form hints)\n- 12px: Compact spacing (form margins, list gaps)\n- 16px: Standard spacing (component gaps, medium padding)\n- 20px: Comfortable spacing (input/form padding)\n- 24px: Standard padding (card content, section padding)\n- 28px: Accent spacing (heading margins)\n- 32px: Section gaps (between content blocks)\n- 36px: Large margins (related section separations)\n- 48px: Generous padding (page margins, container sides)\n- 60px: Maximum section padding (hero and major sections)\n- 180px: Extra-large margin (page breaks, major layout shifts)\n\n**Usage Context:**\n- Micro (4–8px): Form field internals, text spacing, tight component margins\n- Comfortable (12–24px): Card padding, input fields, button groups\n- Section (28–60px): Between major sections, page margins, hero spacing\n- Break (180px): Full-page layout transitions\n\n### Grid & Container\n\n- **Max Width:** 1440px (main content container)\n- **Page Padding:** `48px` left and right (reducing to `24px` on tablet, `16px` on mobile)\n- **Column Strategy:** 12-column grid for flexibility; major sections typically span 12 columns\n- **Gutter:** `16px` between columns (doubled to `32px` for major content blocks)\n- **Section Patterns:**\n  - Full-width hero with max-width centered text overlay\n  - Card grids using 3–4 columns on desktop, 2 on tablet, 1 on mobile\n  - List sections with consistent left alignment and staggered cards\n  - Hero + Content + CTA pattern for landing sections\n\n### Whitespace Philosophy\n\nGenerous, intentional whitespace is central to This Is Menopause' approachability. Large margins (36–60px) between major sections prevent information overload and create visual rest points. Padding within cards (24px) and sections (48–60px) ensures content doesn't feel cramped. Navigation and CTAs benefit from breathing room (8–12px internal padding), making them feel inviting rather than dense. The overall density is low: content never exceeds 60–70% of viewport height on larger screens, forcing vertical scrolling and discovery.\n\n### Border Radius Scale\n\n- **2px:** Badges, tags, micro components (tight, precise feel)\n- **8px:** Modals, dropdowns, secondary containers (approachable but structured)\n- **16px:** Cards, content containers, standard surfaces (warm and modern)\n- **36px:** Buttons, inputs, primary interactive elements (maximally rounded, touchable feel)\n- **0px:** Full-width sections, section dividers, structural elements (clean edges)\n\n## 6. Depth & Elevation\n\n| Level | Treatment | Use |\n|-------|-----------|-----|\n| Base (0) | No shadow, solid background, 1px border `#DBDDDF` | Cards at rest, inputs, standard surfaces |\n| Raised (1) | `rgb(219, 221, 223) 0px 0px 16px 0px` | Hovered cards, floating action buttons, popovers |\n| Floating (2) | `rgb(219, 221, 223) 0px 4px 24px 0px` | Modals, dropdowns, overlays, toast notifications |\n| Overlay (3) | `rgb(219, 221, 223) 0px 8px 32px 0px` with semi-transparent dark backdrop | Full-screen modals, dialogs, critical overlays |\n\n**Shadow Philosophy:** This Is Menopause uses subtle, minimal shadows based on neutral grays rather than black. Shadows are understated and serve functional purposes (elevation clarification) rather than aesthetic impact. All shadows use the same gray tone (`rgb(219, 221, 223)`) for consistency. Shadows increase in vertical offset and blur radius as elevation increases, but never dominate the interface. The design prioritizes clarity and cleanliness over dramatic depth effects.\n\n## 7. Do's and Don'ts\n\n### Do\n\n- **Use navy (`#0D1B29`) for primary headings and body text.** It's the brand anchor and ensures readability and trust.\n- **Accent interactive elements with cobalt blue (`#0F57A8`) or purple (`#A441BC`).** These colors signal engagement and brand identity.\n- **Maintain 24px–60px padding in card and section containers.** Whitespace is essential to the brand's approachability.\n- **Use Lato at 18px for body text with 26px line height.** This specific combination ensures the content feels accessible without being patronizing.\n- **Apply `36px` border radius to all buttons and primary inputs.** This soft, rounded aesthetic is key to the friendly-expert tone.\n- **Implement full-width sections with centered max-width containers.** This creates visual breathing room while maintaining focus.\n- **Use subtle shadows (`rgb(219, 221, 223)`) only for elevation and clarity.** Shadows should never dominate or feel heavy.\n- **Test all text on white backgrounds for WCAG AA contrast ratios.** Accessibility is non-negotiable for health content.\n- **Group related elements with consistent padding and subtle borders.** Visual hierarchy must be immediate and clear.\n\n### Don't\n\n- **Don't use pure black (`#000000`) for primary body text.** Navy (`#0D1B29`) is warmer and more approachable while maintaining contrast.\n- **Don't exceed 60px of horizontal padding on desktop.** Content should not feel boxed-in or cramped.\n- **Don't use sharp corners (0px radius) on interactive elements.** Buttons and inputs must feel soft and inviting.\n- **Don't mix multiple shadow values inconsistently.** Use only the defined levels (Base, Raised, Floating, Overlay) for clarity.\n- **Don't apply color overlays or filters to photography.** Real images of women should remain authentic and unfiltered.\n- **Don't reduce line-height below 1.3x (26px for 18px body text).** Complex health content requires generous spacing for clarity.\n- **Don't use font weights below 400 or above 600 for body content.** This range maintains hierarchy and readability.\n- **Don't create hover states with color changes alone.** Combine color changes with subtle shadow, border, or background shifts for multi-sensory feedback.\n- **Don't nest modals or create complex overlay hierarchies.** Single-layer overlays maintain clarity and reduce cognitive load.\n- **Don't use accent colors (purple, blue) for body text.** Reserve them exclusively for interactive elements and brand moments.\n\n## 8. Responsive Behavior\n\n### Breakpoints\n\n| Name | Width | Key Changes |\n|------|-------|-------------|\n| Mobile | 320px–639px | Single column, 16px page padding, 20px section padding, font sizes reduce by 2px, collapsed navigation |\n| Tablet | 640px–1023px | 2-column grid, 24px page padding, 24px section padding, font sizes reduce by 1–2px, hamburger menu primary |\n| Desktop | 1024px–1439px | 3–4 column grid, 48px page padding, 48px–60px section padding, full font hierarchy, full navigation bar |\n| Wide | 1440px+ | 12-column grid with centered max-width container, full spacing, optional sidebar or additional columns |\n\n### Touch Targets\n\n- **Minimum Size:** 44px × 44px for all interactive elements (buttons, links, form controls)\n- **Minimum Spacing:** 8px between adjacent touch targets to prevent accidental interaction\n- **Button Height:** 52px standard (40px for compact variants in forms)\n- **Button Width:** 32px–120px depending on label length (full-width on mobile)\n- **Icon Size:** 24px minimum for touch-friendly icons\n- **Form Input Height:** 40px minimum for keyboard visibility and touch accessibility\n\n### Collapsing Strategy\n\n- **Hero Sections:** Text and CTA stack vertically; image moves below text on mobile\n- **Card Grids:** 4 columns → 2 columns (tablet) → 1 column (mobile)\n- **Navigation:** Full horizontal menu bar → hamburger icon + slide-out drawer on tablet/mobile\n- **Forms:** Side-by-side labels → stacked labels (font size remains constant)\n- **Padding:** 48px–60px desktop → 24px tablet → 16px mobile (maintains breathing room)\n- **Typography:** H1 28px → 24px (tablet) → 20px (mobile); body 18px → 16px (mobile)\n- **Images:** Full aspect ratio on desktop → constrained to 100% width on mobile, max-height adjustments\n- **Buttons:** Fixed width on desktop → full-width on mobile (except icon buttons)\n\n## 9. Agent Prompt Guide\n\n### Quick Color Reference\n\n- **Primary CTA:** Cobalt Blue (`#0F57A8`)\n- **Primary CTA (Hover):** Dark Cobalt (`#0D4A8C`)\n- **Secondary CTA:** Menopause Purple (`#A441BC`)\n- **Background (Card/Surface):** White (`#FFFFFF`)\n- **Background (Page):** White (`#FFFFFF`)\n- **Primary Text:** Navy Dark (`#0D1B29`)\n- **Secondary Text:** Dark Gray (`#626B74`)\n- **Tertiary Text:** Medium Gray (`#666666`)\n- **Borders & Dividers:** Light Gray (`#DBDDDF`)\n- **Input Borders:** Light Gray (`#C1C1C1`)\n- **Brand Accent (Logo/Highlight):** Menopause Purple (`#A441BC`)\n- **Disabled State:** Pale Gray (`#D4D4D4`)\n- **Error State:** Deep Red (`#D9534F`)\n\n### Iteration Guide\n\n1. **All primary headings and body text must use navy `#0D1B29` on white `#FFFFFF` backgrounds** for trust, legibility, and brand consistency.\n\n2. **Interactive elements (buttons, links, form inputs) default to cobalt blue `#0F57A8` with white `#FFFFFF` text.** Hover states shift to `#0D4A8C`; disabled states use `#D4D4D4`.\n\n3. **Card and container padding is always 24px minimum.** Section padding is 48px–60px. Full-width sections have centered max-width containers (1440px) with auto side margins.\n\n4. **Typography must follow the defined hierarchy: H1 28px/600, H2 20px/600, Body 18px/400, Caption 14px/400.** All fonts are Lato; line-height is 1.3x–1.5x font size.\n\n5. **All buttons and primary inputs use 36px border-radius.** Buttons are minimum 52px height; inputs are 40px height. Padding is 16px horizontal and 0px vertical (height is explicit).\n\n6. **Shadows are subtle and limited: base (0px), raised (`rgb(219, 221, 223) 0px 0px 16px 0px`), floating (`rgb(219, 221, 223) 0px 4px 24px 0px`).** Never use black or strong shadows.\n\n7. **Whitespace is abundant: 36px–60px between major sections, 24px internal card padding, 8px between UI elements.** Low visual density creates approachability.\n\n8. **Focus and hover states must include visual feedback beyond color: combine color change with shadow shift or border enhancement.** Example: button hover adds `0px 0px 8px rgba(15, 87, 168, 0.2)` shadow.\n\n9. **All text must meet WCAG AA contrast ratios.** Navy on white, dark gray on white, and cobalt blue on white all pass. Avoid light text on light backgrounds.\n\n10. **Responsive breakpoints: Mobile 320–639px (1 column, 16px padding), Tablet 640–1023px (2 columns, 24px padding), Desktop 1024px+ (3–4 columns, 48px padding).** Font sizes and card grid counts adjust per breakpoint.\n\n11. **Form labels are Lato 16px/600 in navy, placed above inputs with 8px margin-bottom.** Helper text is Lato 14px/400 in dark gray, placed below inputs with 4px margin-top.\n\n12. **Navigation links are 18px/400 navy; active links have a 2px solid cobalt blue underline.** Dropdown items use 16px/400 with hover background `#F0F4F9`.\n\n13. **Accent purple `#A441BC` is reserved for brand moments (logo, featured highlights, secondary CTAs).** It is not used for body text or background fills—only highlights.\n\n14. **All borders and dividers default to `#DBDDDF` (light gray).** Input borders use `#C1C1C1` (slightly darker). Stroked elements follow the 1px standard.\n\n15. **Card hover states add the raised shadow (`rgb(219, 221, 223) 0px 0px 16px 0px`) and do not change background color.** This maintains visual clarity while signaling interactivity."},{"path":"system/components/README.md","title":"Components","section":"Components","bytes":4682,"markdown":"# Components\n\n## What this is\n\nThere is **no extracted component layer yet.** Styles live in one ~2,700-line\n`prototypes/navigation/main.css` and behavior in one ~6,000-line `main.js`.\nExtracting 79 blocks at once would be a large, risky change with no immediate\npayoff.\n\nSo the first step is to make the monolith *addressable*:\n\n- **[index.html](index.html)** — the component gallery. Live specimens rendered\n  from the real `main.css`, plus the full inventory by tier, with a theme\n  switcher. Start here.\n- **[INDEX.md](INDEX.md)** — the same inventory as markdown, for reading in an\n  editor or a diff. **Generated** from the stylesheet by\n  `evals/gen-component-index.js`, so it cannot drift.\n- **`<block>.md`** — a hand-written contract for blocks that earn one.\n- **[_TEMPLATE.md](_TEMPLATE.md)** — the contract schema.\n\nPer [AUTHORITY.md](../AUTHORITY.md), the code is authoritative about what\nbehaves this way today; a contract is authoritative about what was *intended*.\nA contract that merely restates CSS values is waste.\n\n## Tiers\n\n| Tier | What it means | Extract? |\n|---|---|---|\n| **primitive** | Small reusable control | Yes — these are the real candidates |\n| **chrome** | Global nav furniture, one per page | Probably not; document instead |\n| **module** | Splash Landing content module (`mod-*`) | Composed, not primitive |\n| **surface** | Belongs to one product surface | No — documented in `product/` |\n\nThe tier list is the one human judgment in the generator; it lives in\n`evals/gen-component-index.js` so it is reviewable in a diff.\n\n## Adding a contract\n\n1. Copy `_TEMPLATE.md` to `<block>.md`, using the block name exactly as\n   INDEX.md spells it (no leading dot).\n2. Fill it in. Delete sections that do not apply. State missing states\n   explicitly — a visible gap is worth more than a silent one.\n3. Regenerate: `node evals/gen-component-index.js`\n4. Run `node evals/lint-links.js`.\n\n## Which to write first\n\nOrder by reuse, not by size. `.ai-btn` went first because it has real states,\nmotion, and accessibility surface. Writing it immediately surfaced a\nsystem-wide blocker — the prototype had no keyboard focus styling at all across\n64 buttons — which is the argument for contracts in the first place: the act of\ndescribing intended behavior exposes what is missing.\n\nGood next candidates: `.mod-btn-primary` / `.mod-btn-secondary` (real buttons,\nsharing a silhouette), `.uplevel` (the level-up pill), `.icon-btn` (composed\ninto several others).\n\n## What writing contracts has found\n\nEvery contract so far has surfaced something the code did not say out loud:\n\n| Contract | Found |\n|---|---|\n| `ai-btn` | **No keyboard focus styling anywhere** — zero `:focus` rules across 64 buttons |\n| `uplevel` | Its dock animation ignored `prefers-reduced-motion`; all four existing blocks were narrowly scoped elsewhere |\n| `uplevel` | `font-weight: 400` commented `/* Lato Semibold */` — an unresolved contradiction, surfaced not guessed |\n| `mod-btn-*` | Both variants are **dark-surface only**, which the class names do not say; and the secondary's border measures 2.90:1, under the 3:1 minimum |\n| `icon-btn` | Composed into `.ai-btn` and others — a real relationship that was undocumented |\n\nThat is the argument for contracts: describing intended behavior exposes what\nis missing far faster than reading the implementation does.\n\n## Figma parity, and why this is not Code Connect\n\n[system/parity.json](../parity.json) ties a Figma node to the CSS block that\nimplements it. `evals/lint-parity.js` checks the structure, that every mapped\nblock still exists, and reports coverage.\n\n**Code Connect was considered and not adopted.** It would give richer mapping,\nbut it needs a Node toolchain plus `figma connect publish` running in CI — which\nends this repo's no-build-step property, the thing that lets every page open\nstraight from `file://`. `parity.json` gets detectable drift without that, and\nis the natural input if Code Connect is adopted later.\n\nCoverage is deliberately partial. An entry is added only when the node has\nactually been read from Figma; a guessed mapping is worse than none, because it\nlooks authoritative. The four contracted components without an entry are\nreported on every run rather than quietly filled in — see `openQuestions` in\n`parity.json` for what each one is waiting on.\n\n## Known gaps\n\n- The secondary module button's border is under the 3:1 non-text minimum.\n- `.uplevel`'s font-weight contradiction is unresolved — needs the Figma frame.\n- 3 of 81 blocks are mapped to Figma. The nav controls cannot be mapped yet:\n  the accessible page exposes them only inside a `hidden` reference copy.\n"},{"path":"system/components/_TEMPLATE.md","title":"<Component name>","section":"Components","bytes":1810,"markdown":"# <Component name>\n\n> Copy this file to `system/components/<block-name>/docs.md`, where\n> `<block-name>` is the CSS block exactly as it appears in\n> [INDEX.md](INDEX.md) (without the leading dot). Then regenerate the index:\n> `node evals/gen-component-index.js`.\n>\n> Delete any section that genuinely does not apply — an empty heading is worse\n> than no heading. Do **not** restate CSS values that the stylesheet already\n> says; per [AUTHORITY.md](../AUTHORITY.md), code is authoritative about what\n> behaves this way today. This file is authoritative about what was *intended*.\n\n## Purpose\n\nWhat job does this do for the user? One or two sentences.\n\n## Use\n\nUse the preference grammar from [AUTHORITY.md](../AUTHORITY.md):\n\n- **Preferred** —\n- **Allowed** —\n- **Avoid** —\n- **Prohibited** —\n- **Exception** —\n- **Rationale** —\n\n## Anatomy\n\nRequired and optional parts, by BEM element.\n\n## Variants\n\nSupported choices and when each applies.\n\n## States\n\nOnly the states that actually exist. Default, hover, focus, active, selected,\nloading, disabled, error, empty — and say explicitly which are *not*\nimplemented, so a gap is visible rather than assumed.\n\n## Behavior\n\nInteraction, transitions, persistence, recovery.\n\n## Content\n\nLabel wording, length limits, terminology.\n\n## Accessibility\n\nSemantic element, accessible name, keyboard operation, visible focus, contrast,\ntarget size, reduced motion, announcements.\n\n## Dependencies\n\nTokens, other components, assets.\n\n## Implementation\n\nWhere the styles and the markup actually live, by file and selector.\n\n## Figma\n\nNode id and file, when known. `parity.json` (Phase 4) will make this\nmachine-checkable.\n\n## Rationale and decisions\n\nWhy the important constraints exist. Link to\n[decisions/DECISIONS.md](../../decisions/DECISIONS.md) entries.\n"},{"path":"system/components/ai-btn.md","title":"Ask AI button (.ai-btn)","section":"Components","bytes":4684,"markdown":"# Ask AI button (`.ai-btn`)\n\n## Purpose\n\nOpens the Ask AI assistant from the global navigation. It is the single entry\npoint to the assistant, and the only control in the nav that draws attention to\nitself rather than waiting to be found.\n\n## Use\n\n- **Preferred** — one instance, in the top nav, on every persona.\n- **Allowed** — the pulse, while the assistant has something to offer\n  (`<body data-flow>`).\n- **Avoid** — a second instance on the same screen. The pulse targets\n  `.ai-btn` by selector and animating two at once reads as an error.\n- **Prohibited** — pulsing indefinitely with no nudge to follow. The animation\n  exists to lead somewhere.\n- **Exception** — the narrow-phone layout drops the label and keeps the icon\n  (see Variants), because the nav overflows below ~360px.\n- **Rationale** — the assistant is new and unfamiliar. The ring earns attention\n  once; a permanent pulse becomes noise and trains people to ignore it.\n\n## Anatomy\n\n| Part | Required | Notes |\n|---|:-:|---|\n| `.ai-btn` | yes | the pill; also carries `.icon-btn` |\n| `.ai-btn__stars` | yes | gradient duo-star, 22px, inline SVG |\n| `.ai-btn__label` | no | the word \"Ask\"; dropped on narrow phones |\n| `::before` | — | the gradient ring, as an overlay so it animates independently of the icon |\n| `::after` | — | the second ripple ring |\n\n## Variants\n\nNo `--` modifiers. The two forms are driven by viewport, not by a class:\nicon + label by default, icon only below the narrow-phone breakpoint.\n\n## States\n\n| State | Implemented | Behavior |\n|---|:-:|---|\n| Default | yes | gradient ring, transparent fill |\n| Active | yes | `scale(0.95)` |\n| Pulsing | yes | `ai-pulse`, 2.6s, infinite while `body[data-flow]` |\n| One-shot pulse | yes | a single farewell pulse after the greeting closes |\n| Quiet | yes | `.assistant-quiet` suppresses the animation |\n| Typing | yes | `body.assistant-typing` drives `::after` |\n| **Hover** | **no** | there is no distinct hover treatment |\n| Focus-visible | yes | 2px `--color-primary` ring at pill radius, from the global rule |\n| **Disabled** | **no** | the assistant is never unavailable in the prototype |\n\n## Behavior\n\nThe pulse keyframes are built so the `100% → 0%` boundary lands at rest\n(transparent, `scale(1)`). JS shows the nudge on that boundary, so a nudge can\nnever appear mid-ripple. Changing the keyframe timing without preserving that\nresting boundary will make the nudge pop in over an expanding ring.\n\n## Content\n\nLabel is **\"Ask\"** — one word, so the pill stays narrow enough to sit beside\nsearch and the avatar. The accessible name is the fuller \"Ask AI\".\n\n## Accessibility\n\n- Semantic `<button>`.\n- Accessible name `aria-label=\"Ask AI\"`, which is deliberately longer than the\n  visible \"Ask\" because the icon carries meaning the word alone does not.\n- `@media (prefers-reduced-motion: reduce)` disables both the pulse and the\n  `::after` ripple. This is required, not optional: the pulse is a large\n  repeating scale-and-glow.\n- Focus is a 2px `--color-primary` ring at pill radius, inherited from the\n  global `:focus-visible` rule in `main.css`. Writing this contract is what\n  surfaced its absence: the prototype had **no** focus styling at all — zero\n  `:focus` rules across 64 rendered buttons — which was a system-wide blocker,\n  not an `.ai-btn` problem. Fixed globally rather than here.\n- Target size is 44px tall, meeting the minimum; width hugs content. **This is\n  deliberate and confirmed (2026-09-18).** The Figma reference copy reachable\n  today draws the control at 36×36 with a matching 22×22 glyph; 44px is an\n  intentional uplift to the accessibility minimum, not drift. The Figma frames\n  will be refreshed to match rather than the code being pulled back.\n\n## Dependencies\n\n- Tokens: `--radius-pill`, and `--color-accent` / `--color-primary` via\n  `rgb(from …)` in the pulse keyframes.\n- Composes `.icon-btn`.\n- The duo-star gradient SVG is generated by `aiDuoStarsGradientSVG()` in\n  `main.js`, which takes a unique gradient id per instance.\n\n## Implementation\n\n- Styles — `prototypes/navigation/main.css`, `.ai-btn*` and `@keyframes ai-pulse`.\n- Markup — `prototypes/navigation/main.js`, in the mobile and desktop nav builders.\n- Motion study — [system/motion/ai-pulse-spec.html](../motion/ai-pulse-spec.html).\n\n## Rationale and decisions\n\n- The label was shortened to \"Ask\" and the nav overflow fixed on narrow phones\n  (commit `474c533`).\n- The pulse ring colors were converted from baked `rgba()` to\n  `rgb(from var(--color-accent) …)` so the ring re-skins with the site; before\n  that it stayed purple on a blue site. See\n  [decisions/DECISIONS.md](../../decisions/DECISIONS.md).\n"},{"path":"system/components/icon-btn.md","title":"Icon button (.icon-btn)","section":"Components","bytes":2068,"markdown":"# Icon button (`.icon-btn`)\n\n## Purpose\n\nA 44px circular hit target holding a single glyph and nothing else. The base\ncontrol for every icon-only action in the navigation.\n\n## Use\n\n- **Preferred** — icon-only actions in the nav: menu, search, profile.\n- **Allowed** — as a base for a composed control. `.ai-btn` extends it into a\n  labelled pill by overriding width, padding and radius.\n- **Avoid** — adding a visible text label. Once it has one it is no longer this\n  component; extend it the way `.ai-btn` does.\n- **Prohibited** — using it without an accessible name. It has no text, so\n  `aria-label` is the only thing a screen reader has.\n- **Rationale** — 44px is the minimum comfortable touch target, and a fixed\n  size is what lets several of these sit in a row without the nav reflowing.\n\n## Anatomy\n\n| Part | Required | Notes |\n|---|:-:|---|\n| `.icon-btn` | yes | 44×44, `border-radius: 50%`, transparent |\n| `.icon` span | yes | injected by `icon()`; holds the inline SVG |\n\nNo BEM elements or modifiers of its own.\n\n## States\n\n| State | Implemented | Behavior |\n|---|:-:|---|\n| Default | yes | transparent, `--color-text` |\n| Active | yes | 5% ink wash |\n| Focus-visible | yes | 2px `--color-primary` ring at pill radius, from the global rule |\n| **Hover** | **no** | no distinct hover; the active wash is the only feedback |\n| **Disabled** | **no** | no styling exists |\n\n## Accessibility\n\n- Semantic `<button>`.\n- `aria-label` is mandatory — there is no visible text.\n- The glyph inherits `color`, so contrast is whatever the surrounding context\n  sets. On the nav's white background that is `--color-text`.\n- 44×44 meets the minimum target size.\n\n## Dependencies\n\nTokens `--color-text`; the `icon()` helper and `window.MHT_ICONS.ui`.\n\n## Implementation\n\nStyles `prototypes/navigation/main.css` `.icon-btn`. Markup is built inline in\n`main.js` wherever an icon-only action is rendered.\n\n## Rationale and decisions\n\nComposed into `.ai-btn` — see [ai-btn](ai-btn.md). That relationship is\nreal but was undocumented before this file existed.\n"},{"path":"system/components/mod-btn-primary.md","title":"Module buttons (.mod-btn-primary, .mod-btn-secondary)","section":"Components","bytes":2421,"markdown":"# Module buttons (`.mod-btn-primary`, `.mod-btn-secondary`)\n\nOne component with two variants. `.mod-btn-secondary` shares this contract.\n\n## Purpose\n\nThe call-to-action pair inside content modules — the primary action and its\nlower-emphasis companion.\n\n## Use\n\n- **Preferred** — a primary alone, or a primary with one secondary beside it.\n- **Allowed** — secondary alone, where the action is genuinely optional.\n- **Avoid** — two primaries in one module. Emphasis stops meaning anything.\n- **Prohibited** — using either on a light background. **Both are designed for\n  dark surfaces**: the primary is a white fill with dark text, and the secondary\n  is transparent with a white border and inverse text. On white, the secondary\n  is invisible.\n- **Exception** — none currently.\n- **Rationale** — they were drawn for the splash modules, which sit on the\n  purple gradient. That constraint is invisible in the class name, which is why\n  it is stated here.\n\n## Anatomy\n\nShared base: 48px tall, 24px radius, Lato 18px, `-0.25px` tracking.\n\n| Variant | Fill | Border | Text |\n|---|---|---|---|\n| `.mod-btn-primary` | `--color-surface` | none | `--color-text` |\n| `.mod-btn-secondary` | transparent | 1px white at 35% | `--color-text-inverse` |\n\n## States\n\n| State | Primary | Secondary |\n|---|---|---|\n| Default | yes | yes |\n| Active | `brightness(0.97)` | 8% white wash |\n| Focus-visible | yes — global rule, pill radius | yes |\n| **Hover** | **no** | **no** |\n| **Disabled** | **no** | **no** |\n\nNeither has a loading state, which matters if one is ever wired to a real\nrequest.\n\n## Accessibility\n\n- Semantic `<button>`; the label is the accessible name.\n- 48px tall, above the minimum target size.\n- The secondary's border is `rgba(255,255,255,0.35)`. Composited over the\n  darkest gradient anchor (`--color-navy`) that is `#75759d` — a measured\n  **2.90:1**, just under the 3:1 non-text minimum. The label itself is\n  **12.74:1**, so the control is perceivable and the failure is limited to the\n  boundary. Raising the border to ~40% alpha would clear it.\n\n## Content\n\nSentence case, short. At 18px in a 48px pill, anything past ~4 words wraps and\nbreaks the fixed height.\n\n## Implementation\n\nStyles `prototypes/navigation/main.css`, shared rule then per-variant. Rendered\nin `main.js` content modules (4 primary, 2 secondary at time of writing).\nDesktop widens padding to 28px at the 1024px breakpoint.\n"},{"path":"system/components/mod-btn-secondary.md","title":".mod-btn-secondary","section":"Components","bytes":415,"markdown":"# `.mod-btn-secondary`\n\nThe lower-emphasis variant of the module button pair. Documented together with\nits primary, since they share a base rule and are designed to appear together:\n\n**→ [Module buttons](mod-btn-primary.md)**\n\nKey constraint worth repeating here: this variant is **dark-surface only**. It\nis transparent with a white border and inverse text, so on a light background it\nis effectively invisible.\n"},{"path":"system/components/uplevel.md","title":"Level-up pill (.uplevel)","section":"Components","bytes":4247,"markdown":"# Level-up pill (`.uplevel`)\n\n> **Status: dormant — not rendered today.**\n>\n> `renderUplevel()` exists in `main.js` and is **never called**; `.screen__uplevel`\n> is only referenced by a `querySelector` that always returns null. The desktop\n> breadcrumb was dropped on 2026-08-04 with the note *\"Re-add via\n> `screen.type === \"uplevel\"` when the up-nav treatment is decided\"*, and the\n> pill went with it. The CSS, the render function and the per-screen `upTo` /\n> `upIcon` data all remain, so it is parked rather than deleted.\n>\n> This contract therefore describes **intent**, not shipped behavior. Treat the\n> rendering details as what it would do when re-enabled, and verify against the\n> design before switching it back on. Found while verifying this contract in a\n> browser — the component index's \"JS\" column says `•` because the string\n> appears in `main.js`, which does not distinguish a definition from a call.\n\n## Purpose\n\nA floating pill that tells you where the current screen sits and takes you one\nlevel up. It docks into a bar once the page scrolls, so the way back is always\nreachable without occupying the header.\n\n## Use\n\n- **Preferred** — on a screen that has a clear parent (an article inside a hub).\n- **Allowed** — with an icon, or label only.\n- **Avoid** — on a top-level screen. There is nothing to go up to, and the pill\n  implies a hierarchy that is not there.\n- **Prohibited** — more than one per screen.\n- **Rationale** — it is a wayfinding aid, not navigation chrome. Two of them\n  would make \"up\" ambiguous.\n\n## Anatomy\n\n| Part | Required | Notes |\n|---|:-:|---|\n| `.uplevel` | yes | the pill; `pointer-events: auto` re-enables clicks inside a `pointer-events: none` container |\n| `.uplevel__icon` | no | 16px glyph |\n| `span` | yes | the label |\n\nIts container `.screen__uplevel` carries the `is-lifted` state.\n\n## Variants\n\nNot a class modifier — two forms driven by scroll position:\n\n| Form | Trigger | Appearance |\n|---|---|---|\n| Idle | default | 34px pill, soft blue fill, `--color-border-primary-soft` border |\n| Docked | `.screen__uplevel.is-lifted` | full-width 44px bar, translucent white, `backdrop-filter: blur(8px)`, square, bottom hairline |\n\nThe transition animates height, padding, radius, background and border-color\nover 0.2s.\n\n## States\n\n| State | Implemented |\n|---|:-:|\n| Idle / Docked | yes |\n| Focus-visible | yes — global rule, pill radius |\n| **Hover** | **no** |\n| **Active** | **no** |\n\n## Accessibility\n\n- The docked bar uses `backdrop-filter` over `rgba(255,255,255,0.8)`, so page\n  content shows faintly through. Contrast is not guaranteed against arbitrary\n  content scrolling beneath it — worth checking against the darkest screen.\n- The dock transition now honours `prefers-reduced-motion`. It did not before:\n  `main.css` had four reduced-motion blocks, but every one was narrowly scoped\n  to the assistant pill, its nudge, or the notification badge, so this animation\n  ran regardless. Found by writing this contract; fixed in the same change.\n\n## Resolved discrepancy\n\nThe label was `font-weight: 400` with the comment `/* Lato Semibold */` — a\ncontradiction this contract surfaced. **Confirmed 2026-09-18: 600 is correct.**\nThe comment carried the intent and the value had drifted from it. Now 600.\n\nNote that this has **no visible effect today**, because the component is\ndormant. It is correct for when it is re-enabled.\n\nWorth noting how it resolved: per [AUTHORITY.md](../AUTHORITY.md) the code\nwas authoritative about what rendered and the comment about what was meant, and\nthe repo could not settle between them. It took a decision from outside the\nrepo — which is exactly why it was recorded rather than guessed.\n\n## Implementation\n\nStyles `prototypes/navigation/main.css` `.uplevel*` and\n`.screen__uplevel.is-lifted`. The `is-lifted` toggle is in `attachAutoHide()` in\n`main.js`.\n\n## Figma\n\nGlobal Navigation `42yas7Q9FfwhL6xUocjEAl`, node **`7294:1952`** (\"Uplevel\").\nRead from the file and confirmed: 34px tall in both, a 16×16 `Icon` child\nmatching `.uplevel__icon`, and a `Label` text child matching the span. The frame\nis `hidden=true`, so it is a definition rather than a placed instance.\n\nRecorded in [system/parity.json](../parity.json).\n"},{"path":"system/foundation/design.md","title":"TIM Design — tokens, type & assets","section":"Foundations","bytes":18649,"markdown":"# TIM Design — tokens, type & assets\n\nThe visual language shared across every surface: color tokens, type, spacing,\nmotion, the icon-tinting system, and the chrome asset provenance. Surface docs\nreference this for the *system*; they keep only their own section-specific layout\nand their own icon tables. Type/style sources: the **MHT Style Guide**\n(`zV2gbuNONeoyeRVcFUFWeZ`, node `3941:17`) and the **Mobile_Splash_Landing** frame.\n`main.css` is the source of truth for token values — correct this doc if it drifts.\n\n---\n\n## Color tokens\n\nDefined in `:root` (`main.css`):\n\n| Token | Value | Use |\n|---|---|---|\n| `--color-primary` | `#0f57a8` | primary blue — Join button, links, level-up label |\n| `--color-primary-soft` | `#dee5f1` | soft blue — listicle & panel Explore rollover |\n| `--color-navy` | `#2b2b68` | hero / factoid gradient (was the old panel access-card bg) |\n| `--color-text` | `#0d1b29` | ink — body + nav icon tint |\n| `--color-text-secondary` | `#626b74` | secondary/label text |\n| `--color-text-muted` | `rgba(13,27,41,0.7)` | muted text |\n| `--color-border` | `#dbdddf` | hairlines (nav border, dropdown border) |\n| `--color-badge` | `#ff741d` | notification badge dot + Notifications chip (orange) |\n| `--color-badge-soft` | `rgba(255,116,29,0.05)` | Notifications dropdown chip background |\n| `--color-magenta` | `#a440bc` | Library / Community icon tint |\n| `--color-magenta-soft` | `rgba(164,65,188,0.08)` | pink circle behind panel icons |\n| `--color-magenta-soft-solid` | `#f8f0fa` | opaque equivalent, used for the pill rollover so nothing alpha-stacks |\n| `--color-purple-light` | `#d483cf` | Factoid headline highlight |\n| `--color-bg-purple-soft` | `#f6eff8` | Listicles / Community section bg |\n| `--color-bg-cool` | `#fafcff` | Articles section bg |\n| `--color-bg-blue-soft` | `#edf3f9` | Experts section bg |\n| `--color-card-border` | `rgba(13,27,41,0.15)` | card borders |\n| `--color-pill-border` | `rgba(13,27,41,0.44)` | pill borders |\n\n---\n\n## Type\n\n- `--font-display`: **\"DM Serif Display\"** — headlines.\n- `--font-body`: **\"Lato\"** — body/UI.\n\n**Display scale** (Splash Landing / headlines): hero **52px desktop / 40px\nmobile** (responsive, verified against the Splash Landing Figma file\n`EWsXKakhyFLhkse035AoHX` — mobile node `4101:14`, desktop node `4101:222`; this\nprototype is mobile-only, so only the 40px value is built, in\n`.mod-hero__title`), stat **40px**, section / factoid / CTA **32px mobile /\n36px desktop** (verified: mobile node `4113:...`/`4101:109`, desktop node\n`4113:44` and siblings), footer tagline **20px**. Sizes matched per-element to\nthe frame.\n\nDM Serif Display is now the standing headline treatment across the whole app —\nArticle, Advisors, Community Hub, and Topic Hub H1/H2 all use it too (not just\nSplash Landing), at their existing sizes (Article 28/26/24px, Advisors 28/24px,\nhubs 28px). This supersedes the Figma Style Guide's generic Lato SemiBold H1–H6\nscale (`zV2gbuNONeoyeRVcFUFWeZ`, node `21:74`) for this product — that page\ndocuments a Lato-based scale this app doesn't use for headings.\n\nThe actual DM Serif Display scale lives in its own specimen frame in the same\nfile: confirmed nodes `3942:2211` (32px / 40px line-height / -0.75px / weight\n400 — matches `.mod-section-text__title`, `.mod-factoid__title`) and `3942:2192`\n(28px / 36px line-height / -0.75px / weight 400 — matches `.art__title`,\n`.mod-advisors__title`). Both check out exactly against the code.\n\n**Section headers are also responsive**, mirroring the hero: mobile **32px /\n40px line-height** (built, confirmed above) → desktop **36px / 44px\nline-height** (confirmed via the Splash Landing file, `EWsXKakhyFLhkse035AoHX`\nnode `4113:44`, not yet built since this prototype is mobile-only). Same\n-0.75px letter-spacing, same weight 400, same size+8 line-height pattern as\nevery other DM Serif Display step.\n\n**Article / Advisors / Community Hub / Topic Hub titles are responsive too**\n(this is the Landing/Article distinction to watch — Landing tiers scale by a\nbigger desktop jump, this tier scales like Section Header): mobile **28px /\n36px line-height** (built, confirmed against the Entry-Points file\n`ugVVY70ovzigulEMouzHx7`, node `49:1947`) → desktop **32px / 40px\nline-height** — per team direction; no desktop frame exists in Figma yet to\nverify this against, so treat it as directional until a real frame confirms\nit. Notably this desktop value (32/40) is identical to Section Header's\n*mobile* value — the type scale has 5 shared steps (28, 32, 36, 40, 52) reused\nacross tiers/breakpoints, not 6 independent ones.\n\n**Responsive deltas are not uniform**: Hero jumps +12px desktop→mobile\n(52→40), but Section Header and this title tier both jump only +4px\n(36→32, 32→28). Don't extrapolate one tier's delta onto another.\n\n**Letter-spacing (size-based rule):** **≥20px → -0.75px**, **≤18px → -0.25px**.\n\n---\n\n## Responsive type scale — engineering reference\n\nThree DM Serif Display tiers, each with a mobile size and a desktop size. **Both\nare now built** — the device frame was removed and the prototype is a real\nresponsive product with a desktop layout at `min-width: 1024px` (see the tokens\nin `main.css` `:root` + the `@media (min-width:1024px)` override):\n\n| Tier | Mobile | Desktop | Jump | Verified? |\n|---|---|---|---|---|\n| Hero | 40px / 48px lh | 52px / 60px lh | +12px (+30%) | Figma (Splash Landing) |\n| Section header | 32px / 40px lh | 36px / 44px lh | +4px (+12.5%) | Figma (Splash Landing, all 5 sections) |\n| Article/Advisors/Hub title | 28px / 36px lh | 32px / 40px lh | +4px (+14%) | Mobile: Figma (Entry-Points). Desktop: per team direction, no frame yet |\n| Sub-heading | 24px / 32px lh | 28px / 36px lh | +4px (+17%) | Mobile: Figma (Entry-Points). Desktop: per team direction, no frame yet |\n\nLetter-spacing is `-0.75px` and weight is `400` across every cell — only size\nand line-height change per breakpoint. Line-height is always size **+8px**.\n\n**Is the uneven jump (+30% vs +12–14%) intentional and good?** Yes — this is a\nstandard pattern in responsive/fluid type systems (e.g. Utopia-style fluid\nscales): the top-level display/hero treatment gets the most dramatic scale-up\non wide viewports because it's a branding/impact moment with room to breathe,\nwhile secondary and tertiary headings stay closer to their mobile size so they\ndon't overwhelm line length or break rhythm with body copy at reading widths.\nDon't \"fix\" this into one flat ratio — the unevenness is the correct design,\nnot an inconsistency.\n\n**Implemented CSS** (this is now live in `main.css` — the `.phone` device mock\nwas removed and the app reflows into a real desktop layout at the `1024px`\nbreakpoint; the token block below drives the per-tier size/line-height swap):\n\n```css\n:root {\n  /* DM Serif Display responsive scale — mobile-first, desktop overrides below */\n  --display-hero-size: 40px;\n  --display-hero-lh: 48px;\n  --display-section-size: 32px;\n  --display-section-lh: 40px;\n  --display-title-size: 28px;\n  --display-title-lh: 36px;\n  --display-subhead-size: 24px;\n  --display-subhead-lh: 32px;\n  --display-tracking: -0.75px;\n}\n\n@media (min-width: 1024px) {\n  :root {\n    --display-hero-size: 52px;\n    --display-hero-lh: 60px;\n    --display-section-size: 36px;\n    --display-section-lh: 44px;\n    --display-title-size: 32px;\n    --display-title-lh: 40px;\n    --display-subhead-size: 28px;\n    --display-subhead-lh: 36px;\n  }\n}\n\n.mod-hero__title {\n  font-size: var(--display-hero-size);\n  line-height: var(--display-hero-lh);\n  letter-spacing: var(--display-tracking);\n}\n\n.mod-section-text__title,\n.mod-factoid__title {\n  font-size: var(--display-section-size);\n  line-height: var(--display-section-lh);\n  letter-spacing: var(--display-tracking);\n}\n\n.art__title,\n.mod-advisors__title,\n.comm-hub__title,\n.topic-hub__name {\n  font-size: var(--display-title-size);\n  line-height: var(--display-title-lh);\n  letter-spacing: var(--display-tracking);\n}\n\n.art-conv__title,\n.art-answered__title,\n.art-keep__h,\n.mod-advisors__watch-head {\n  font-size: var(--display-subhead-size);\n  line-height: var(--display-subhead-lh);\n  letter-spacing: var(--display-tracking);\n}\n```\n\nWhy this shape: one `:root` override block per breakpoint (not per-selector\nmedia queries scattered through the file) keeps the three tiers' mobile/desktop\npairs in one place, matches the existing token convention (`--color-*`,\n`--radius-*`), and means adding a 4th tier later is a 2-line diff instead of\na new media query block. `1024px` is a placeholder breakpoint — swap for\nwhatever value the desktop frame actually targets once one exists (the Splash\nLanding desktop canvas is ~1194px wide with 97px side margins).\n\n---\n\n## Lato / body-UI type scale\n\nUnlike DM Serif Display, Lato (body/UI text) never had a documented scale —\nsizes accumulated per-component. Auditing every `font-size` in `main.css`\noutside the DM Serif Display tiers found 8 distinct values in active use. Two\nwere near-duplicates and have been consolidated:\n\n**Fixed (2026-08-07):** `13px` → `14px` (7 call sites: `.mod-article__meta`,\n`.collection-callout__label`, `.comm-mod__badge`, `.topic-hub__stat`,\n`.footer__legal`, `.footer__link--dark`, `.footer__disclaimer` /\n`.footer__copyright`) and `15px` → `16px` (2 call sites: `.comm-mod__desc`,\n`.topic-hub-mod__desc`). `.launcher__section-title` keeps its `13px` — it's the\nroot `index.html` launcher, explicitly \"rig chrome, not part of the design\nsystem,\" same exemption as the entry-point mocks.\n\n**Resulting scale** (6 sizes, down from 8):\n\n| Size | Role | Matches Figma Typography (node `21:74`)? | Example components |\n|---|---|---|---|\n| 12px | Note / fine print | Yes — \"Note\", 18px lh, -0.25px | `.mod-article__eyebrow`, `.topic-hub-mod__note`, `.topic-hub-mod__soon` |\n| 14px | Caption | Yes — \"Caption\", 20px lh, -0.25px | `.mod-article__meta`, `.footer__legal`, `.art__byline`, badges/pills (post-fix) |\n| 16px | Body Small | Yes — \"Body Small\", 22px lh, -0.25px | `.mod-expert-card__role`, `.topic-hub__desc`, descriptions (post-fix) |\n| 18px | Body Large | Yes — \"Body Large\", 24px lh, -0.25px | `.mod-cta-card__sub`, `.art__lede p`, most paragraph/button text |\n| 20px | Card/component title | No — extends beyond the base Figma Body scale | `.mod-checker-card__title`, `.mod-listicle-card__title`, `.mod-view-all-link` |\n| 22px | Emphasis title | No | `.art-note__title` |\n| 24px | Quote (Lato SemiBold) | No — **same pixel value as the DM Serif Display 24px tier, different font/weight** | `.mod-quote-card__text` only |\n\nBase sizes (12/14/16/18) map directly onto the Lato Body scale already\ndocumented in the Figma Style Guide's Typography page (`zV2gbuNONeoyeRVcFUFWeZ`,\nnode `21:74` — Note / Caption / Body Small / Body Large), just never\ncross-referenced here before now. 20/22/24 are component-specific extensions\nbeyond that base scale, not yet formalized as named Figma text styles.\n\n**24px is overloaded — mitigated via naming, not by changing either value.**\n`.mod-quote-card__text` (Lato, weight 600, line-height `--quote-lh` 30px —\ntightened from an approximate `1.35` ratio to match the Figma Lato \"Headline\"\n24px swatch exactly, node `40:260`) and a fourth DM Serif Display tier — a \"sub-heading\" size used for `.art-conv__title`,\n`.art-answered__title`, `.art-keep__h`, and `.mod-advisors__watch-head`\n(weight 400, line-height 32px, same size+8 rule as every other DM Serif\nDisplay tier) — happen to share the same font-size number but are visually and\nsemantically unrelated. Both values are correct on their own (this sub-heading\ntier is now Figma-confirmed too — see below), so the fix isn't to change\neither number, just to stop referring to \"24px\" as if it were one thing.\n`main.css` now encodes this with two separately-named tokens instead of a\nshared bare literal: `--display-subhead-size` / `--display-subhead-lh` for the\nDM Serif tier, `--quote-size` for the Lato quote. Fixing\n`.mod-advisors__watch-head`'s line-height to use the shared token also caught\na real bug in the process — it was hardcoded to `40px` instead of the correct\n`32px` (size+8), inconsistent with the other three components in this tier.\n\n**DM Serif Display sub-heading tier (24px), now Figma-confirmed:** verified\nagainst the Entry-Points file (`ugVVY70ovzigulEMouzHx7`) — node `49:1845`\n(\"Join the conversation\"), `49:1855` (\"Menopause, answered\"), `49:1890`\n(\"Keep Reading\") — all 24px / 32px line-height / -0.75px / weight 400. This\ntier isn't in the Style Guide's generic DM Serif specimen frame (which only\nhas 28px and 32px), but it's confirmed directly against the real Article\nscreen, same as the Article title tier was. `.mod-advisors__watch-head`\n(\"Watch Now\") uses the same tier by convention; not independently verified\nsince Advisors isn't part of this file, but there's no reason to expect it\ndiffers.\n\n**This tier is responsive too:** mobile 24px / 32px line-height (built,\nFigma-confirmed above) → desktop 28px / 36px line-height — per team direction,\nsame unverified status as the Title tier's desktop value (no desktop frame\nexists to check it against). Notably this desktop value (28/36) is identical\nto the Title tier's *mobile* value — another instance of the shared-steps\npattern (the full DM Serif Display scale is 24, 28, 32, 36, 40, 52 — six\nsteps reused across four tiers × two breakpoints, not eight independent\npairs).\n\n**Dead code, found and removed (2026-08-07):** `.mod-section` /\n`.mod-section__title` were never referenced in `main.js` — the real, in-use\ncomponent is `.mod-section-text` / `.mod-section-text__title` (with \"-text-\"),\nlikely a leftover from a rename. Removed from `main.css`.\n\n---\n\n## Spacing, radius & motion\n\n- `--radius-pill`: `999px` (buttons, panel row rollover); `--radius-card`: `16px`.\n- **Section padding** (splash content modules): **48px** top/bottom, **12px** sides.\n- **Panel:** open slide/fade `0.24s` ease-out; close (`.is-closing`) slower `0.32s`\n  ease-out (`closePanel()`, removed on `animationend`). Row rollover is a flat pill\n  (`--color-magenta-soft-solid`); on hover the icon circle goes transparent so it\n  merges into the pill (no alpha-stacking).\n- **Dropdown:** Notifications bell **wiggles periodically** (`bell-wiggle`).\n- **Motion is gated:** animations are disabled under `prefers-reduced-motion`.\n- Scrollbars are hidden globally (`scrollbar-width: none` + `::-webkit-scrollbar`),\n  scrolling still works.\n\n---\n\n## Icon-tinting system\n\nIcons come in **two forms**:\n\n1. **Inline, CSS-tinted** — monochrome SVGs in the `ICON_SVGS` map in `main.js`\n   (normalized to `fill=\"currentColor\"`), tinted by each context's `color`. Used by\n   the **top nav** (menu / search / AI / grid) and **profile dropdown** (ink\n   `#0D1B29`); the **level-up pill's** section icons (blue `#0F57A8` via the pill's\n   `color`, incl. the article's `derm` icon); and the **topic-hub** icons (magenta\n   `#A440BC` — the pale `#F6EFF8` circle behind them is drawn in CSS by\n   `.panel__hub-icon`, not baked into the SVG). The nav/dropdown icons render via\n   `icon()` as `<span class=\"icon\">…</span>`; the pill and hub icons are inlined\n   directly into their own wrappers.\n2. **Pre-tinted images** (`<img src>`, color baked in) — only the **panel tabs**\n   (`tab-*.svg`, ink) now. The `up-*` / `hub-*` icons were migrated to form 1 on\n   2026-08-05 (glyphs only, no baked circle) and their standalone files removed.\n\nThe old currentColor-tinted Library/Community panel icons and the back-chevron were\n**removed** in the 2026-08-03 panel + level-up redesigns.\n\nInlining (rather than `<img>` or CSS `mask` of external files) is **deliberate**:\nCSS `mask`/`url()` refs to external SVGs are **blocked over `file://`**, so masked\nicons vanished when the prototype was opened directly. Inline SVG works over\n`file://` and still tints via `color`. The standalone `assets/icons/*.svg` files\nwere removed (redundant); the node-id tables (here + per surface) are their Figma\nprovenance.\n\nPer-surface icon tables live with their surfaces: Library `topic-*.svg`\n([library.md](../../product/library.md)), Community `community-*.svg` ([community.md](../../product/community.md)),\ndropdown `menu-*.svg` ([account.md](../../product/account.md)). The chrome assets are below.\n\n---\n\n## Chrome assets (`assets/`, flat folder)\n\nReal assets pulled from Figma **Global Navigation** `42yas7Q9FfwhL6xUocjEAl` into a\nsingle flat `assets/` folder, referenced by relative path from `main.js`. Logos and\nthe profile illustration are full-color `<img>` (load fine over `file://`). Node\nids are the leaf vector/frame node each file was exported from.\n\n### Logo (PNG, 3× — the Figma nav logo is a raster image-fill, not vector)\n| File | What | Source node |\n|---|---|---|\n| `logotype.png` | Full \"this is Menopause\" wordmark | 6950:211 |\n| `logomark.svg` | Compact circular logomark (vector) | 7082:926 |\n| `logotype.svg` | Horizontal wordmark (panel top + footer) | — |\n\n### Nav icons (inline, provenance node ids)\n| File | Source node |\n|---|---|\n| `menu.svg` | 6875:148 |\n| `search.svg` | 6875:153 |\n| `ai.svg` (sparkle) | 6875:155 |\n\n### Panel tab icons (pre-tinted `<img>`, Global Navigation)\n| File(s) | What | Source |\n|---|---|---|\n| `tab-home.svg` / `tab-resources.svg` / `tab-community.svg` | Panel tab icons (ink) | panel `7299:1987` |\n\nThe **topic-hub** (`hub-1…8`) and **level-up** (`up-activity` / `up-qa` / `up-hub` /\n`up-groups` / `up-programs` / `up-meet`) glyphs are now **inline** in `ICON_SVGS`\n(`fill=\"currentColor\"`, no baked circle — 2026-08-05); their standalone files were\nremoved. Figma provenance: hubs panel `7299:1987`, level-up icons uplevel\n`7294:1952`. The article's `derm` icon is inline too (Figma **Articles** file).\n\n### Profile\n| File | What | Source node |\n|---|---|---|\n| `placeholder_profile.svg` | Magenta silhouette on a light-magenta circle (128×128, self-contained circular avatar) | 7042:626 |\n\n### Footer\n| File | What |\n|---|---|\n| `privacy-choices.png` | CCPA \"Your Privacy Choices\" opt-out icon |\n\n### Still placeholder / not pulled — deliberate\n- **Notification badge**: CSS dot (`.badge`, `--color-badge`). The Figma badge (7042:629) is pixel-identical to the CSS version — better as a positioned element than an `<img>`.\n- **Panel close (X)**: inline SVG in `main.js` (`INLINE_ICONS.close`). The Figma panel has no close node (dismiss via scrim) — a prototype-only affordance.\n- **Logged-in member avatar**: reuses `placeholder_profile.svg`. The source `member-photo` variant shows a `Photo` image-fill, but there's no distinct curated headshot to export.\n"},{"path":"system/patterns/navigation.md","title":"TIM Navigation — spec (behavior + components)","section":"Foundations","bytes":15702,"markdown":"# TIM Navigation — spec (behavior + components)\n\nSource: Figma **Global Navigation** file `42yas7Q9FfwhL6xUocjEAl`.\nPrototype: plain HTML/CSS/JS in this repo (`../index.html`, `../main.css`, `../main.js`), no build step.\n\nThis doc is the spec for the **global navigation chrome** — the top nav, slide-out\npanel (container), level-up pill, and global footer: how they behave and their\ncomponent structure (props, states, Figma node ids). The nav is the *mechanism*;\nwhat surrounds it lives elsewhere: the render model, screen types, device frame,\nand persona/screen inventory are in [system.md](../../prototypes/navigation/ARCHITECTURE.md); color/type/motion\ntokens, the icon-tinting system, and asset provenance are in [design.md](../foundation/design.md);\nand the content **surfaces** the nav leads to have their own docs —\n[landing.md](../../product/landing.md) (Splash Landing), [library.md](../../product/library.md) (topics),\n[community.md](../../product/community.md), [account.md](../../product/account.md) (the profile dropdown + its\nscreens), and [onboarding.md](../../product/onboarding.md) (sign-up). History is in\n[decisions/DECISIONS.md](../../decisions/DECISIONS.md). Code is the source of truth if any of these disagree\n— correct the doc, don't erase the stale entry.\n\n---\n\n## Personas & screens\n\nThe four personas (nav treatment, `navVariant`; the panel is now shared, so\n`panelType` was removed) and the full per-persona screen inventory live in the architecture doc,\n[system.md](../../prototypes/navigation/ARCHITECTURE.md) → Personas / Screens per persona. Each persona's nav\n*variant* is detailed in §1 below; the content surfaces those screens belong to\nare in [landing.md](../../product/landing.md), [library.md](../../product/library.md),\n[community.md](../../product/community.md), and [account.md](../../product/account.md).\n\n---\n\n## Interactions (as built)\n\n- **Top nav** → sticky at the top, but **auto-hides on scroll-down and returns on scroll-up** (`attachAutoHide()` toggles `.is-hidden` on the sticky `.screen__nav`). The **level-up pill** (`.screen__uplevel`) is a separate sticky element that **stays pinned** — it docks below the nav when the nav shows and rises to the top edge when the nav hides.\n- **Hamburger (☰)** → opens the slide-out Panel for the current persona. Tapping the overlay (not the panel surface) closes it with a slower ease-out slide-out animation (`closePanel()`).\n- **Nav logotype / logomark** → returns to the persona's home screen (Splash Landing for Visitor and Subscriber, Home as a hub for the member states). In-page `go-home`.\n- **Hidden home hotspot** → a small circle fixed in the top-left corner of each flow page, invisible until hovered, that links back to the root launcher (`../index.html`). Uses a launchpad / 2×2 grid icon. Injected by `main.js`, so it only appears on flow pages (not the launcher).\n- **Panel tabs / hubs** → the shared panel's Home / Resources / Community tabs and its topic-hub rows each carry a `data-screen`; tapping navigates in-persona and closes the panel. See §3 for the full structure and targets.\n- **Profile avatar** (member nav variant) → opens the account **Dropdown** (wired for Logged In Member; tapping outside closes it). The dropdown's menu and its destination screens are spec'd in [account.md](../../product/account.md).\n- **Level-up pill** → only on *detail* screens (Article/Group/Program/Profile/Question/Activity). It steps **one level up** to the parent list/topic (via `upTo` → `data-screen`, e.g. Group Detail → Groups, Article Show → its topic page), not \"back\" and not necessarily home. Top-level pages (the panel/dropdown destinations) have no level-up pill — the nav logo returns home instead. (`program` has no list parent in the source, so it falls back to home.)\n- **Nav pill** → opens the chromeless sign-up flow **in-persona**. Persona-aware: **Join** → `Sign Up Start` for the Visitor; **Finish** → `Registration Step` for the Subscriber (both share the `visitor` nav variant). Full spec in [onboarding.md](../../product/onboarding.md). *(The panel's old Join for free / Finish up now access cards were removed in the 2026-08-03 shared-panel redesign; those roles moved to the nav pill + splash closing card — see §3.)*\n- **\"Log in now\"** (Logged Out Member's panel access card — see §3) and **\"Log out\"** (dropdown) → currently **no-ops** (auth not wired); see [onboarding.md](../../product/onboarding.md).\n\n---\n\n## 1. Top Nav\n\nFixed bar, 68px tall, white background, `inset 0 -1px 0 #dbdddf` hairline bottom border.\n\n**Layout:** `[ Menu icon | Logo ]  ...  [ Search | AI | Join-button OR Profile ]`\n\nThe logotype/logomark is a button that returns to the persona's home screen (`go-home`). The stacked visitor logotype renders at 40px, hugging the menu icon.\n\n### Variant: `visitor`\n- Logo slot: full logotype (`ThisIsMenopause_Logo_FullColor_Digital`), 133×44\n- Right slot: Search icon, AI icon, **Join** pill button (bg `#0f57a8`, white text)\n- No profile icon\n- Node refs: 6538:148 (Splash Landing), 7082:1269 (Topic Center), 6951:800 (Article Show)\n\n### Variant: `member` (logged-out and logged-in, non-photo state)\n- Logo slot: compact **Logomark** (circle mark, ~47×44), not the full wordmark\n- Right slot: Search icon, AI icon, **Profile** avatar (self-contained circular placeholder silhouette — no button border/background) + orange notification badge (`--color-badge`, `#ff741d`) that sits **outside, on top of** the avatar's top-right edge (11px dot with a 2px white ring). The circular crop is on an inner `.profile-avatar` wrapper so the badge (a sibling) isn't clipped by it.\n- No Join button\n- Node ref: 6960:52 (Logged Out Member — Home gated)\n\n### Variant: `member-photo` (logged-in member)\n- Same as `member`, but Profile avatar shows a real photo instead of the generic placeholder, still with the badge\n- Node refs: 7025:302 (Article Show), 7025:371 (Group Detail), 7031:509 (Program Detail), 7025:440 (Member Profile), 6951:734 (Question Show), 6951:531 (Activity Show), 7042:711 (Home + Dropdown)\n\n**Subscriber:** no subscriber-specific top-nav frame in the source file, so the prototype reuses the **`visitor`** variant (full logotype + Join CTA) and the Visitor landing. Its slide-out panel is the shared panel (the old subscriber \"Finish up now\" access card was removed in the 2026-08-03 redesign; the `registration-step` screen still exists for the sign-up flow). See [system.md](../../prototypes/navigation/ARCHITECTURE.md) → Personas.\n\n---\n\n## 2. Level-up pill (one level up)\n\n**Redesign (2026-08-03, Figma Global Navigation `42yas7Q9FfwhL6xUocjEAl`, pill `7294:1952`).** The old full-width level-up **bar** was replaced by a **pill** that sits in-page (top-left) but sticks to the top on scroll — same pinning behavior as the bar had.\n\n**Pill:** an opaque blue-tinted pill (`background` = `--color-bg-blue-soft` `#EDF3F9`, `border` = `1px solid #d6e2f1`, `border-radius: 48px`, `padding: 6px 12px`, height 34px) holding a **section icon** (16px, inline `currentColor` from `ICON_SVGS`, tinted blue by the pill's `color`) + the parent's name (Lato Semibold 14px, `#0f57a8`, `-0.25px`). No chevron — the section icon + label convey the destination. The pill **floats** over content (its sticky container is transparent + `pointer-events:none`, so the pill's own opaque fill is what keeps text from bleeding through) and picks up a soft blue drop-shadow only once scrolled over content (`.screen__uplevel.is-lifted`, toggled in `attachAutoHide` — no glow while idle at the top).\n\nOnly on **detail** screens. It steps **one level up** to the parent (`upTo` → `data-screen`), not \"back\" and not home. Top-level pages have no pill — the nav logo returns home. The nav and the level-up pill are **independent sticky elements**: the nav (`.screen__nav`) auto-hides on scroll-down and returns on scroll-up, while the pill's sticky container (`.screen__uplevel`, transparent so the pill floats in-page) **stays pinned**. While the nav shows, it pins just below it (its `top` = nav height); when the nav hides, it docks to the very top edge (`attachAutoHide` animates the `top`).\n\n| Screen | Pill label → up-to | Icon (inline, `ICON_SVGS`) |\n|---|---|---|\n| Visitor / Member — Article Show | Skin-related Hub → `topic-hub` | `derm` |\n| Member — Group Detail | Groups → `com-groups` | `up-groups` |\n| Member — Program Detail | Programs → home | `up-programs` |\n| Member — Someone's Member Profile | Meet Others → `com-meet` | `up-meet` |\n| Member — Question Show | Questions & Answers → `com-questions` | `up-qa` |\n| Member — Activity Show | Activity → `com-activities` | `up-activity` |\n\nArticle Show levels up to a **Topic Hub** (`topic-hub`, §3, [topic-hub.md](../../product/topic-hub.md)) — an article belongs to a hub; the built skin-care article's pill reads **\"Skin-related Hub\"** with the `derm` icon. Activity Show levels up to **Activity** (`com-activities`) — after `com-stories`/\"Posts\" was retired and folded into Activity (2026-08-03), the `up-posts.svg` icon was renamed `up-activity.svg`. `Program Detail` has no Programs list, so it falls back to home.\n\n**Topic Center folded into Collections (2026-08-04):** the old `topic` screen was retired. It was \"a collection of sponsored content,\" so it's now simply a **Collection** (the `collection` screen). An article links **either** up to a Topic Hub (the pill above) **or** to a Collection (the callout below) — never to a standalone \"Topic Center\" any more.\n\n**Collection callout (in-page, non-sticky).** An article that belongs to a **Collection** carries `collection: { screen }`. The **Article Show (in Collection)** screen renders the **full article template** (shared `renderArticleShell` — same as the standalone Article Show, with generic placeholder copy) and inserts a small **callout box** directly **above Key Takeaways** (`.collection-callout` — \"Part of a collection\" / a line of copy / an **\"Explore the full collection →\"** link that navigates via `data-screen`), scrolling **with** the content. The callout links to its **Collection** (the rebranded old Topic Center — a curated set of sponsored content).\n\n---\n\n## 3. Slide-out Panel\n\nFull-height overlay: `rgba(0,0,0,0.75)` scrim + 300px white panel sliding from left, 24px padding, 24px gap between blocks.\n\n**Aligned panel (2026-08-10, Figma Global Navigation `42yas7Q9FfwhL6xUocjEAl`).** Every persona's panel shares **one layout** — only the top card differs. (This supersedes the 2026-08-03 \"tab card + topic-hub list + Explore\" panel: the tabs and the in-panel Explore pill were dropped.)\n\n**Structure:** `Logotype → persona card → TOPICS list → Footer note`\n\n### Content slots\n\n**Persona card** (`.panel__promo` — navy, DM Serif title + sub + a white pill CTA + a secondary text link). The card is the only per-persona difference:\n\n| Persona | Title / sub | Pill CTA → | Link → |\n|---|---|---|---|\n| Visitor | \"Don't miss out!\" / join copy | **Join for free** → `signup-start` | Get a preview first → `community-overview` |\n| Subscriber | \"Don't miss out!\" / finish copy | **Finish up now** → `registration-step` | Get a preview first → `community-overview` |\n| **Logged Out Member** (Figma `7417:3673`, \"Access\") | \"Welcome back, Jannie123!\" / \"You're not logged in.\" | **Log in now** → `data-action=\"log-in\"` (no-op — auth not wired) | Join for free → `signup-start` |\n| **Logged In Member** (Figma `7417:3712`) | \"Jannie123, how are you today?\" / \"It's okay to open up.\" | **Share now** → `com-activities` (Activity) | Ask a question → `com-questions` (Q&A) |\n\nCard data lives in `PANEL_CARDS` (`main.js`); the whole panel is one `renderPanel()`.\n\n**TOPICS list** — the **same for every persona**: 3 library topics (HRT & Other Treatments, Mood & Mental Health, Sleep & Insomnia) + **All Articles** (`PANEL_TOPICS`). Each row is a pill (magenta glyph on a pale circle; All Articles has no icon) → its `lib-*` screen (All Articles → `lib-all`).\n\n`signup-start` is a shared injected screen (`SIGNUP_START_SCREEN` in `SHARED_TARGET_SCREENS`) so \"Join for free\" resolves for the logged-out member too, not just the splash personas. `com-activities` / `com-questions` are already shared (community sub-screens).\n\n**Footer** — \"Powered by\" / \"MyHealthTeam, a Swoop company\" (`#626b74`, 14px), pinned to bottom. There is **no close (X)** — the panel dismisses via the scrim (`closePanel()`).\n\n### Node refs\n- Persona cards: Logged-in `7417:3712`, Logged-out \"Access\" `7417:3673`.\n- *(Superseded)* 2026-08-03 shared panel `7299:1987` / `7299:2055` / `7299:2123` / `7287:1599`; older per-persona panels 6950:226, 6960:3, 6950:144, 6959:904.\n\n---\n\n## 4. Profile dropdown\n\nThe account menu opened from the top-nav profile avatar. It's a single-surface\ncomponent (it serves only Account), so its full spec — container styling, menu\nstructure, item→screen map, Notifications-above-divider, bell wiggle, Log-out\nno-op, \"View Profile\" → My Profile, dropdown icons, and persona availability —\nlives in **[account.md](../../product/account.md)**. The nav's role is just the trigger: the\nprofile avatar (§1, member/subscriber personas) toggles it open.\n\n---\n\n## 5. Global footer\n\nAdded 2026-07-24 to the bottom of **every screen's scroll area** (`renderFooter()` in `main.js`). Content mirrors the Figma mobile footer (`6371:29` / `6371:139`). It's separated from the page content by ample space (`72px` margin-top on `.footer`, `80px` on desktop) and a **thin full-width divider line** (`border-top: 1px solid var(--color-border)`, matching Figma `6371:29`) so content never crowds it. Two bands:\n\n- **Bar** (white): horizontal wordmark (`logotype.svg`) + headline \"Expert advice. Real women. Real talk.\" (matches the Splash frame footer, node 4101:162), then two link columns — About / Editorial Process / Partner with Us / **Medical Advisors** · Getting Started / Community Guidelines / Help Center / Crisis. All are non-navigating placeholders **except \"Medical Advisors\"**, which links to the built Advisors page (`data-screen=\"advisors\"`; replaced the former \"Accessibility\" placeholder). The footer is global, so `advisors` is injected into every persona (see §3).\n- **End** (gray `#f3f4f6`): legal line \"Terms of Use · Privacy Policy · Cookie Policy · Health Data · [icon] Your Privacy Choices · CA Notice at Collection\" (the CCPA opt-out icon is `assets/privacy-choices.png`), the medical disclaimer, and \"© 2026 MyHealthTeam, A Swoop Company.\"\n\nBecause the footer is tall, screens are now vertically scrollable: `.screen` is a flex column with a fixed nav/uplevel and a scrollable `.screen__scroll` holding the content + footer. Panel/dropdown overlays stay pinned to the phone viewport (absolute over `.screen`, don't scroll).\n\n**Screen content** is a labelled placeholder by default. The Visitor / Subscriber home screens carry a `modules: true` flag and render the real **Splash Landing** content via `renderModules()` — see [landing.md](../../product/landing.md). There is no screenshot-image path.\n\n---\n\n## Assets\n\nThe chrome assets (logo, nav/uplevel icons, profile illustration, footer CCPA\nicon), the **icon-tinting system**, and the color/type/motion tokens referenced\nthroughout this doc are catalogued in [design.md](../foundation/design.md). The nav icons\n(`menu`, `search`, `ai`, `back`) and logos (`logotype`, `logomark`) are there\nunder **Chrome assets**; per-surface panel icons live with their surfaces\n([library.md](../../product/library.md), [community.md](../../product/community.md), [account.md](../../product/account.md)).\n"},{"path":"system/tokens/themes/README.md","title":"Sites","section":"Foundations","bytes":4098,"markdown":"# Sites\n\nOne universal palette, one hue family per site. Components are identical\neverywhere — only the `--brand-*` values in this folder differ.\n\n## The two sites\n\n### ThisIsMenopause — `menopause.css`\n\nThe flagship site. Purple hue family, anchored on `#a440bc`, which the brand\ndocs reserve for brand moments: logo lockup, featured highlights and secondary\nCTAs. Values are production and Figma-confirmed. Every prototype in this repo\ncurrently runs this theme.\n\n### Legacy condition sites — `legacy.css`\n\nThe 80+ existing condition sites. They run older styling today and are\nmigrating onto Menopause styling with a blue hue family instead of purple —\nsame components, same layout, same type, different hue.\n\n**Status: placeholder.** Only `--brand-accent` (`#3f7ec1`) is sourced, from\n`line-icons/icon-sheet.html`. The other values are derived from it using the\nsame relationships the Menopause theme uses, so the system is coherent today\nand the real palette is a one-file swap later. `--brand-accent-pale` in\nparticular is **not** contrast-checked yet.\n\nThere is no Legacy site template yet — no prototype runs this theme end to\nend. That is tracked in the launcher under Foundations → Sites.\n\n| Theme | Hue family | Status |\n|---|---|---|\n| `menopause.css` | Purple | Production. Figma-confirmed. |\n| `legacy.css` | Blue | Placeholder. Only the accent is sourced. |\n\n## What belongs in a theme\n\nOnly the eight `--brand-*` hue tokens. Everything else — action blue, ink,\nsurfaces, borders, type, layout — is universal and lives in `../tokens.css`.\n\nIf you find yourself wanting to add a ninth token here, check first whether the\ndifference is really brand or really a component variant.\n\nDerivation used for a new hue family, matching Menopause exactly:\n\n| Token | Rule |\n|---|---|\n| `--brand-accent` | the site hue |\n| `--brand-accent-soft` | accent at 8% alpha |\n| `--brand-accent-soft-solid` | that 8% composited over white |\n| `--brand-accent-strong` | deeper accent for rings and edges |\n| `--brand-accent-light` | lighter accent for emphasis on dark |\n| `--brand-accent-pale` | pale tint that clears AA on a photo scrim |\n| `--brand-scrim` | dark backdrop behind a COLOR-blended photo |\n| `--brand-bg-accent-soft` | very light section wash |\n\n## Two loading models\n\nEach theme file declares its values twice: once on bare `:root` (so it works\nalone) and once on `:root[data-theme=\"<name>\"]` (so it works in a switcher).\nThe attribute selector has higher specificity, so it always wins when both are\npresent.\n\n**A product page loads exactly one theme.** This is the normal case, and what\nevery page in this repo does today:\n\n```html\n<link rel=\"stylesheet\" href=\"system/tokens/tokens.css\">\n<link rel=\"stylesheet\" href=\"system/tokens/themes/menopause.css\">\n<link rel=\"stylesheet\" href=\"main.css\">\n```\n\n**A page that compares hues loads several** and sets the attribute on `<html>`:\n\n```html\n<link rel=\"stylesheet\" href=\"system/tokens/tokens.css\">\n<link rel=\"stylesheet\" href=\"system/tokens/themes/menopause.css\">\n<link rel=\"stylesheet\" href=\"system/tokens/themes/legacy.css\">\n<script>document.documentElement.dataset.theme = 'menopause';</script>\n```\n\nSuch a page must set `data-theme` explicitly — with several theme files loaded,\nthe bare `:root` default is decided by link order, which is fragile. No page\ndoes this today; the launcher documents the sites rather than switching\nbetween them.\n\n## Adding a site\n\nCopy `legacy.css`, change the eight values and the selector name. That is the\nwhole job — no component, token or documentation change beyond listing it\nabove. This is the property that has to hold as the count grows toward 80+; if\nadding a site ever requires touching anything outside this folder, something\nhas leaked out of the theme layer.\n\n## Known gap\n\nBrand-colored raster and SVG assets do not theme. Applying the Legacy hue\nrecolors the UI but leaves the logo purple, because the wordmark is inlined\nSVG with baked fills in `main.js`. `evals/lint-tokens.js` reports these as\nwarnings. Fixing it means `currentColor` or per-site marks — Phase 3.\n"},{"path":"system/tokens/tokens.md","title":"TIM Design System","section":"Foundations","bytes":4074,"markdown":"# TIM Design System\n\nCanonical, cross-project design tokens and type rules for ThisIsMenopause\n(TIM). Implementation-agnostic — describes tokens and roles, not any one\nproject's class names. Map these onto whatever naming a consuming project\nalready uses.\n\nSource of truth: the **MHT Style Guide** Figma file (`zV2gbuNONeoyeRVcFUFWeZ`),\ncross-checked against real product surfaces where the Style Guide alone was\nincomplete. See [Verification legend](#verification-legend).\n\n---\n\n## Colors\n\n| Token | Value | Role |\n|---|---|---|\n| `color-primary` | `#0f57a8` | Primary action color — buttons, links |\n| `color-primary-soft` | `#dee5f1` | Soft primary tint — hover/rollover states |\n| `color-navy` | `#2b2b68` | Dark gradient anchor, display headline color |\n| `color-text` | `#0d1b29` | Primary ink |\n| `color-text-secondary` | `#626b74` | Secondary/label text |\n| `color-text-muted` | `rgba(13,27,41,0.7)` | Muted text |\n| `color-border` | `#dbdddf` | Hairlines |\n| `color-badge` | `#ff741d` | Notification/badge accent (orange) |\n| `color-badge-soft` | `rgba(255,116,29,0.05)` | Badge background tint |\n| `color-magenta` | `#a440bc` | Secondary brand accent — icon tints, highlights |\n| `color-magenta-soft` | `rgba(164,65,188,0.08)` | Magenta background tint (translucent) |\n| `color-magenta-soft-solid` | `#f8f0fa` | Magenta background tint (opaque equivalent) |\n| `color-purple-light` | `#d483cf` | Tertiary accent — headline emphasis on dark backgrounds |\n| `color-bg-purple-soft` | `#f6eff8` | Soft purple section background |\n| `color-bg-cool` | `#fafcff` | Cool neutral section background |\n| `color-bg-blue-soft` | `#edf3f9` | Soft blue section background |\n| `color-card-border` | `rgba(13,27,41,0.15)` | Card borders |\n| `color-pill-border` | `rgba(13,27,41,0.44)` | Pill/chip borders |\n\n---\n\n## Typography\n\nTwo font families: **Display** — \"DM Serif Display,\" weight 400 only, for\nheadline-level text. **Body/UI** — \"Lato,\" weights 400/600/700, for\neverything else, including a weight-600 \"Headline\" component family used for\nUI emphasis (card titles, quotes, callouts) that isn't a document heading.\n\n### Type rules\n\n1. Letter-spacing: **≥20px → `-0.75px`**, **≤18px → `-0.25px`**.\n2. DM Serif Display line-height is always **size + 8px**.\n3. Name tokens by role, never by pixel value — this scale has two unrelated\n   24px styles (Sub-heading, and the Lato \"Quote\" role) on different\n   families/weights.\n4. Heading rank (H1–H6) and visual size are independent decisions. A tag\n   depends on whether the text is a real, navigable section label — not on\n   how big it looks.\n\n### Display scale (DM Serif Display)\n\n| Tier | Mobile | Desktop |\n|---|---|---|\n| Hero | 40px / 48px lh | 52px / 60px lh |\n| Section header | 32px / 40px lh | 36px / 44px lh |\n| Title | 28px / 36px lh | 32px / 40px lh |\n| Sub-heading | 24px / 32px lh | 28px / 36px lh |\n\n### Body/UI scale (Lato)\n\n| Size | Role | Weight | Line-height | Letter-spacing |\n|---|---|---|---|---|\n| 12px | Note | 400 | 18px | -0.25px |\n| 14px | Caption | 400/600 | 20px | -0.25px |\n| 16px | Body Small | 400/600 | 22px | -0.25px |\n| 18px | Body Large | 400/600 | 24px | -0.25px |\n| 20px | Headline | 600 | 26px | -0.75px |\n| 22px | Headline | 600 | 28px | -0.75px |\n| 24px | Headline | 600 | 30px | -0.75px |\n| 28px | Headline | 600 | 34px | -0.75px |\n\n---\n\n## Spacing & radius\n\n| Token | Value | Role |\n|---|---|---|\n| `radius-pill` | `999px` | Buttons, chips, pill controls |\n| `radius-card` | `16px` | Cards, panels |\n| `section-padding` | `48px` top/bottom, `12px` sides | Standard content-module spacing |\n\n---\n\n## Verification legend\n\n- **Figma-confirmed** — pulled directly from a real Figma node via the API.\n- **Per team direction** — team-confirmed, but no Figma frame exists yet to\n  check it against (typically an unbuilt desktop layout). Treat as\n  provisional until a real frame confirms it.\n\nProject-specific Figma node IDs are intentionally omitted here to stay\nimplementation-agnostic — check the consuming project's own design docs for\nexact citations.\n"}];
